{
  "name": "mirror",
  "version": "2.0.0",
  "description": "Mirror DSL - A design-focused UI language",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=mirror-lang",
    "test": "npm run test --workspace=mirror-lang",
    "build": "npm run build --workspace=mirror-lang"
  },
  "license": "MIT"
}
