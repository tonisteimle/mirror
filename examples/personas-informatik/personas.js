// Generated by Mirror Compiler (DOM Backend)
// Do not edit manually

// Mirror Data Context (Tokens + Data Files)
// Exposed to window for debugging and two-way binding
const __mirrorData = (window.__mirrorData = {
  'yellow.bg': '#FDE70E',
  'soft.bg': '#F4F4F4',
  'ink.col': '#000',
  'white.col': '#fff',
  'muted.col': '#888',
  'hairline.boc': '#e5e5e5',
  'content.maxw': 1280,
  data: {},
})

// Collection Store for reactive CRUD
const __collections = (window.__collections = {})
function $collection(name) {
  const n = name.startsWith('$') ? name.slice(1) : name
  if (!__collections[n]) {
    const items = __mirrorData[n] || []
    const itemsArray = Array.isArray(items) ? items : Object.values(items)
    __collections[n] = {
      items: itemsArray,
      _current: null,
      _subscribers: new Set(),
      get current() {
        return this._current
      },
      set current(item) {
        if (this._current === item) return
        this._current = item
        this._subscribers.forEach(fn => fn())
      },
      subscribe(fn) {
        this._subscribers.add(fn)
        return () => this._subscribers.delete(fn)
      },
      add(item) {
        const newItem = { id: item.id || Date.now().toString(36), ...item }
        this.items.push(newItem)
        this._subscribers.forEach(fn => fn())
        return newItem
      },
      remove(item) {
        const idx = this.items.indexOf(item)
        if (idx > -1) this.items.splice(idx, 1)
        if (this._current === item) this._current = null
        this._subscribers.forEach(fn => fn())
      },
      update(item, changes) {
        Object.assign(item, changes)
        this._subscribers.forEach(fn => fn())
      },
    }
  }
  return __collections[n]
}

// Reference resolver for data relations
function $resolveRef(value) {
  if (value && typeof value === 'object' && value.__ref) {
    const resolved = __mirrorData[value.collection]?.[value.entry]
    return resolved !== undefined ? resolved : value
  }
  if (Array.isArray(value)) {
    return value.map(v => $resolveRef(v))
  }
  return value
}

// Aggregation methods for collections
// Helper to get nested property value: "data.stats.value" -> obj.data.stats.value
const $getField = (obj, path) =>
  path == null
    ? obj
    : String(path)
        .split('.')
        .reduce((o, k) => o?.[k], obj)
const $agg = {
  count: arr => (Array.isArray(arr) ? arr.length : 0),
  sum: (arr, field) =>
    Array.isArray(arr) ? arr.reduce((s, i) => s + (Number($getField(i, field)) || 0), 0) : 0,
  avg: (arr, field) => {
    const a = Array.isArray(arr) ? arr : []
    return a.length ? $agg.sum(a, field) / a.length : 0
  },
  unique: (arr, field) =>
    Array.isArray(arr) ? [...new Set(field ? arr.map(i => $getField(i, field)) : arr)] : [],
  min: (arr, field) =>
    Array.isArray(arr) && arr.length
      ? Math.min(...arr.map(i => Number($getField(i, field)) || 0))
      : 0,
  max: (arr, field) =>
    Array.isArray(arr) && arr.length
      ? Math.max(...arr.map(i => Number($getField(i, field)) || 0))
      : 0,
  first: arr => (Array.isArray(arr) ? arr[0] : undefined),
  last: arr => (Array.isArray(arr) ? arr[arr.length - 1] : undefined),
}

// $-variable accessor with aggregation support
function $get(name) {
  // Check for aggregation pattern: collection.count, collection.sum(field), items.first.name
  const aggMatch = name.match(
    /^(.+)\.(count|sum|avg|min|max|first|last|unique)(?:\(([^)]+)\))?(\..+)?$/
  )
  if (aggMatch) {
    const [, collectionPath, method, field, postAccessor] = aggMatch
    let collection = $get(collectionPath)
    // Handle both arrays and objects (convert objects to arrays via Object.values)
    if (collection && typeof collection === 'object' && !Array.isArray(collection)) {
      collection = Object.values(collection)
    }
    if (Array.isArray(collection)) {
      let result = field ? $agg[method](collection, field) : $agg[method](collection)
      // Handle post-accessor like .name after .first
      if (postAccessor && result != null) {
        const accessParts = postAccessor.slice(1).split('.')
        for (const part of accessParts) {
          if (result == null) break
          result = result[part]
        }
      }
      return result
    }
    return 0
  }

  // Check for .current pattern (CRUD selection)
  const currentMatch = name.match(/^([^.]+)\.current(\..*)?$/)
  if (currentMatch) {
    const [, collectionName, postAccessor] = currentMatch
    const coll = $collection(collectionName)
    let result = coll.current
    if (postAccessor && result != null) {
      const accessParts = postAccessor.slice(1).split('.')
      for (const part of accessParts) {
        if (result == null) break
        result = result[part]
      }
    }
    return result
  }

  if (window._mirrorState && name in window._mirrorState) return window._mirrorState[name]

  if (name in __mirrorData) return $resolveRef(__mirrorData[name])
  const parts = name.split('.')
  let val = __mirrorData[parts[0]] ?? globalThis[parts[0]]
  for (let i = 1; i < parts.length && val != null; i++) {
    val = $resolveRef(val[parts[i]])
  }
  return val
}

// $-variable mutator (two-way binding)
function $set(path, value) {
  if (path in __mirrorData) {
    __mirrorData[path] = value
  } else {
    const parts = path.split('.')
    let obj = __mirrorData
    for (let i = 0; i < parts.length - 1; i++) {
      if (obj[parts[i]] === undefined) obj[parts[i]] = {}
      obj = obj[parts[i]]
    }
    obj[parts[parts.length - 1]] = value
  }
  // Notify runtime of data change for reactive updates
  if (_runtime && _runtime.notifyDataChange) _runtime.notifyDataChange(path, value)
}
// Expose $set globally for runtime access (Select two-way binding)
if (typeof window !== 'undefined') window.$set = $set

// Design Tokens (for CSS variables)
const tokens = __mirrorData

export function createUI(data = {}) {
  const _elements = {}
  const _state = { ...data }
  const _listeners = new Map()

  // Root container
  const _root = document.createElement('div')
  _root.className = 'mirror-root'
  _root.dataset.mirrorRoot = 'true'

  // Inject CSS styles
  const _style = document.createElement('style')
  _style.textContent = `
/* Mirror Theme Tokens (auto-generated) */
:root {
  /* color */
  --accent-bg: #5BA8F5;
  --accent-hover-bg: #2b8ef2;
  --accent-active-bg: #1382f1;
  --accent-col: #ffffff;
  --surface-bg: #27272a;
  --surface-hover-bg: #333337;
  --surface-active-bg: #3b3b3f;
  --surface-selected-bg: #44444a;
  --canvas-bg: #18181b;
  --elevated-bg: #2a2a2e;
  --elevated-hover-bg: #36363b;
  --input-bg: #1f1f23;
  --input-hover-bg: #2b2b31;
  --overlay-bg: rgba(0, 0, 0, 0.5);
  --text-col: #ffffff;
  --muted-col: #888;
  --subtle-col: #71717a;
  --border-boc: #3f3f46;
  --border-hover-boc: #575761;
  --focus-boc: #5BA8F5;
  --error-bg: #ef4444;
  --error-col: #fca5a5;
  --success-bg: #22c55e;
  --success-col: #86efac;
  --warning-bg: #f59e0b;
  --warning-col: #fcd34d;

  /* spacing */
  --s-pad: 4px;
  --m-pad: 8px;
  --l-pad: 16px;
  --s-gap: 4px;
  --m-gap: 8px;
  --l-gap: 16px;

  /* border */
  --s-rad: 4px;
  --m-rad: 8px;
  --l-rad: 12px;

  /* typography */
  --font: Inter, system-ui, -apple-system, sans-serif;
  --s-fs: 12px;
  --m-fs: 14px;
  --l-fs: 18px;
  --xl-fs: 24px;
  --line-height: 1.5;

}
  
  /* User Tokens */
  :host, :root {
    --yellow-bg: #FDE70E;
    --soft-bg: #F4F4F4;
    --ink-col: #000;
    --white-col: #fff;
    --hairline-boc: #e5e5e5;
    --content-maxw: 1280;
  }
  
  .mirror-root {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  }
  .mirror-root * {
    box-sizing: border-box;
  }
  
  /* Animation Keyframes */
  @keyframes mirror-fade-in { from { opacity: 0; } to { opacity: 1; } }
  @keyframes mirror-fade-out { from { opacity: 1; } to { opacity: 0; } }
  @keyframes mirror-slide-in { from { transform: translateX(-20px); opacity: 0; } to { transform: translateX(0); opacity: 1; } }
  @keyframes mirror-slide-out { from { transform: translateX(0); opacity: 1; } to { transform: translateX(-20px); opacity: 0; } }
  @keyframes mirror-slide-up { from { transform: translateY(20px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
  @keyframes mirror-slide-down { from { transform: translateY(-20px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
  @keyframes mirror-slide-left { from { transform: translateX(20px); opacity: 0; } to { transform: translateX(0); opacity: 1; } }
  @keyframes mirror-slide-right { from { transform: translateX(-20px); opacity: 0; } to { transform: translateX(0); opacity: 1; } }
  @keyframes mirror-scale-in { from { transform: scale(0.9); opacity: 0; } to { transform: scale(1); opacity: 1; } }
  @keyframes mirror-scale-out { from { transform: scale(1); opacity: 1; } to { transform: scale(0.9); opacity: 0; } }
  @keyframes mirror-bounce { 0%, 20%, 50%, 80%, 100% { transform: translateY(0); } 40% { transform: translateY(-10px); } 60% { transform: translateY(-5px); } }
  @keyframes mirror-pulse { 0% { transform: scale(1); } 50% { transform: scale(1.05); } 100% { transform: scale(1); } }
  @keyframes mirror-shake { 0%, 100% { transform: translateX(0); } 10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); } 20%, 40%, 60%, 80% { transform: translateX(5px); } }
  @keyframes mirror-spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
  @keyframes mirror-reveal-up { from { transform: translateY(30px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
  @keyframes mirror-reveal-scale { from { transform: scale(0.8); opacity: 0; } to { transform: scale(1); opacity: 1; } }
  @keyframes mirror-reveal-fade { from { opacity: 0; } to { opacity: 1; } }
  `
  _root.appendChild(_style)

  // Focus state management for headless browser support
  _root.addEventListener('focusin', e => {
    const target = e.target
    if (target.dataset?.mirrorId) {
      target.setAttribute('data-focus', 'true')
    }
  })
  _root.addEventListener('focusout', e => {
    const target = e.target
    if (target.dataset?.mirrorId) {
      target.removeAttribute('data-focus')
    }
  })

  // desktop
  const node_1 = document.createElement('div')
  _elements['node-1'] = node_1
  node_1.dataset.mirrorId = 'node-1'
  node_1.dataset.mirrorRoot = 'true'
  node_1.dataset.mirrorName = 'desktop'
  Object.assign(node_1.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    background: '#fff',
    'font-family': 'system-ui, sans-serif',
    'font-size': '17px',
  })
  node_1.dataset.component = 'desktop'
  _root.appendChild(node_1)

  // Topbar
  const node_2 = document.createElement('div')
  _elements['node-2'] = node_2
  node_2.dataset.mirrorId = 'node-2'
  node_2.dataset.mirrorName = 'Topbar'
  Object.assign(node_2.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-items': 'center',
    padding: '32px 24px 24px 24px',
    width: '100%',
  })
  node_2.dataset.layout = 'flex'
  node_2.dataset.component = 'Topbar'
  // Logo
  const node_3 = document.createElement('div')
  _elements['node-3'] = node_3
  node_3.dataset.mirrorId = 'node-3'
  node_3.dataset.mirrorName = 'Logo'
  Object.assign(node_3.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
    gap: '14px',
  })
  node_3.dataset.layout = 'flex'
  node_3.dataset.component = 'Logo'
  // Frame
  const node_4 = document.createElement('div')
  _elements['node-4'] = node_4
  node_4.dataset.mirrorId = 'node-4'
  node_4.dataset.mirrorName = 'Frame'
  Object.assign(node_4.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
  })
  node_4.dataset.layout = 'flex'
  node_4.dataset.component = 'Frame'
  node_4.dataset.slot = 'Frame'
  // LogoMarkN
  const node_5 = document.createElement('span')
  _elements['node-5'] = node_5
  node_5.dataset.mirrorId = 'node-5'
  node_5.dataset.mirrorName = 'LogoMarkN'
  node_5.innerHTML = formatInlineMarkdown('n')
  Object.assign(node_5.style, {
    'font-size': '28px',
    'font-weight': '700',
    'line-height': '1',
  })
  node_5.dataset.component = 'LogoMarkN'
  node_4.appendChild(node_5)

  // LogoBar
  const node_6 = document.createElement('div')
  _elements['node-6'] = node_6
  node_6.dataset.mirrorId = 'node-6'
  node_6.dataset.mirrorName = 'LogoBar'
  Object.assign(node_6.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '2px',
    'flex-shrink': '0',
    height: '24px',
    'flex-shrink': '0',
    background: '$ink',
    margin: '0px 4px 0px 4px',
  })
  node_6.dataset.component = 'LogoBar'
  node_4.appendChild(node_6)

  // LogoMarkW
  const node_7 = document.createElement('span')
  _elements['node-7'] = node_7
  node_7.dataset.mirrorId = 'node-7'
  node_7.dataset.mirrorName = 'LogoMarkW'
  node_7.innerHTML = formatInlineMarkdown('w')
  Object.assign(node_7.style, {
    'font-size': '30px',
    'font-weight': '700',
    'font-style': 'italic',
    'font-family': 'Georgia, serif',
    'line-height': '1',
  })
  node_7.dataset.component = 'LogoMarkW'
  node_4.appendChild(node_7)

  node_3.appendChild(node_4)

  // Frame
  const node_8 = document.createElement('div')
  _elements['node-8'] = node_8
  node_8.dataset.mirrorId = 'node-8'
  node_8.dataset.mirrorName = 'Frame'
  Object.assign(node_8.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '2px',
  })
  node_8.dataset.component = 'Frame'
  node_8.dataset.slot = 'Frame'
  // LogoTextSmall
  const node_9 = document.createElement('span')
  _elements['node-9'] = node_9
  node_9.dataset.mirrorId = 'node-9'
  node_9.dataset.mirrorName = 'LogoTextSmall'
  node_9.innerHTML = formatInlineMarkdown('Fachhochschule Nordwestschweiz')
  Object.assign(node_9.style, {
    'font-size': '11px',
    'font-weight': '400',
  })
  node_9.dataset.component = 'LogoTextSmall'
  node_8.appendChild(node_9)

  // LogoTextBold
  const node_10 = document.createElement('span')
  _elements['node-10'] = node_10
  node_10.dataset.mirrorId = 'node-10'
  node_10.dataset.mirrorName = 'LogoTextBold'
  node_10.innerHTML = formatInlineMarkdown('Hochschule für Informatik')
  Object.assign(node_10.style, {
    'font-size': '11px',
    'font-weight': '700',
  })
  node_10.dataset.component = 'LogoTextBold'
  node_8.appendChild(node_10)

  node_3.appendChild(node_8)

  node_2.appendChild(node_3)

  _root.appendChild(node_2)

  // Hero
  const node_11 = document.createElement('div')
  _elements['node-11'] = node_11
  node_11.dataset.mirrorId = 'node-11'
  node_11.dataset.mirrorName = 'Hero'
  Object.assign(node_11.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    gap: '80px',
    padding: '96px 24px 96px 24px',
    width: '100%',
  })
  node_11.dataset.component = 'Hero'
  // Eyebrow
  const node_12 = document.createElement('span')
  _elements['node-12'] = node_12
  node_12.dataset.mirrorId = 'node-12'
  node_12.dataset.mirrorName = 'Eyebrow'
  node_12.innerHTML = formatInlineMarkdown('Internes Arbeitsdokument')
  Object.assign(node_12.style, {
    'font-size': '13px',
    'font-weight': '400',
  })
  node_12.dataset.component = 'Eyebrow'
  node_11.appendChild(node_12)

  // HeroVessel
  const node_13 = document.createElement('div')
  _elements['node-13'] = node_13
  node_13.dataset.mirrorId = 'node-13'
  node_13.dataset.mirrorName = 'HeroVessel'
  Object.assign(node_13.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--yellow-bg)',
    padding: '56px 56px 40px 56px',
    width: 'fit-content',
    'max-width': '820px',
  })
  node_13.dataset.component = 'HeroVessel'
  // H1
  const node_14 = document.createElement('span')
  _elements['node-14'] = node_14
  node_14.dataset.mirrorId = 'node-14'
  node_14.dataset.mirrorName = 'H1'
  node_14.innerHTML = formatInlineMarkdown('Personas')
  Object.assign(node_14.style, {
    'font-size': '96px',
    'font-weight': '700',
    'letter-spacing': '-0.025em',
    'line-height': '0.95',
  })
  node_14.dataset.component = 'H1'
  node_13.appendChild(node_14)

  // HeroSub
  const node_15 = document.createElement('span')
  _elements['node-15'] = node_15
  node_15.dataset.mirrorId = 'node-15'
  node_15.dataset.mirrorName = 'HeroSub'
  node_15.innerHTML = formatInlineMarkdown(
    'Studieninteressierte für den Bachelor Informatik — fünf Profile, die unterschiedliche Wege ins Studium abbilden.'
  )
  Object.assign(node_15.style, {
    'font-size': '22px',
    'font-weight': '300',
    'line-height': '1.45',
    'max-width': '60 ch',
  })
  node_15.dataset.component = 'HeroSub'
  node_13.appendChild(node_15)

  // MetaList
  const node_16 = document.createElement('div')
  _elements['node-16'] = node_16
  node_16.dataset.mirrorId = 'node-16'
  node_16.dataset.mirrorName = 'MetaList'
  Object.assign(node_16.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    'flex-wrap': 'wrap',
    gap: '10px 32px',
    'margin-top': '14px',
  })
  node_16.dataset.layout = 'flex'
  node_16.dataset.component = 'MetaList'
  // MetaPair
  const node_17 = document.createElement('div')
  _elements['node-17'] = node_17
  node_17.dataset.mirrorId = 'node-17'
  node_17.dataset.mirrorName = 'MetaPair'
  Object.assign(node_17.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '2px',
  })
  node_17.dataset.component = 'MetaPair'
  // MetaLabel
  const node_18 = document.createElement('span')
  _elements['node-18'] = node_18
  node_18.dataset.mirrorId = 'node-18'
  node_18.dataset.mirrorName = 'MetaLabel'
  node_18.innerHTML = formatInlineMarkdown('Hochschule')
  Object.assign(node_18.style, {
    'font-size': '12px',
    'font-weight': '700',
  })
  node_18.dataset.component = 'MetaLabel'
  node_17.appendChild(node_18)

  // MetaValue
  const node_19 = document.createElement('span')
  _elements['node-19'] = node_19
  node_19.dataset.mirrorId = 'node-19'
  node_19.dataset.mirrorName = 'MetaValue'
  node_19.innerHTML = formatInlineMarkdown('Hochschule für Informatik')
  Object.assign(node_19.style, {
    'font-size': '13px',
    'font-weight': '400',
  })
  node_19.dataset.component = 'MetaValue'
  node_17.appendChild(node_19)

  node_16.appendChild(node_17)

  // MetaPair
  const node_20 = document.createElement('div')
  _elements['node-20'] = node_20
  node_20.dataset.mirrorId = 'node-20'
  node_20.dataset.mirrorName = 'MetaPair'
  Object.assign(node_20.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '2px',
  })
  node_20.dataset.component = 'MetaPair'
  // MetaLabel
  const node_21 = document.createElement('span')
  _elements['node-21'] = node_21
  node_21.dataset.mirrorId = 'node-21'
  node_21.dataset.mirrorName = 'MetaLabel'
  node_21.innerHTML = formatInlineMarkdown('Stand')
  Object.assign(node_21.style, {
    'font-size': '12px',
    'font-weight': '700',
  })
  node_21.dataset.component = 'MetaLabel'
  node_20.appendChild(node_21)

  // MetaValue
  const node_22 = document.createElement('span')
  _elements['node-22'] = node_22
  node_22.dataset.mirrorId = 'node-22'
  node_22.dataset.mirrorName = 'MetaValue'
  node_22.innerHTML = formatInlineMarkdown('6. Mai 2026')
  Object.assign(node_22.style, {
    'font-size': '13px',
    'font-weight': '400',
  })
  node_22.dataset.component = 'MetaValue'
  node_20.appendChild(node_22)

  node_16.appendChild(node_20)

  // MetaPair
  const node_23 = document.createElement('div')
  _elements['node-23'] = node_23
  node_23.dataset.mirrorId = 'node-23'
  node_23.dataset.mirrorName = 'MetaPair'
  Object.assign(node_23.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '2px',
  })
  node_23.dataset.component = 'MetaPair'
  // MetaLabel
  const node_24 = document.createElement('span')
  _elements['node-24'] = node_24
  node_24.dataset.mirrorId = 'node-24'
  node_24.dataset.mirrorName = 'MetaLabel'
  node_24.innerHTML = formatInlineMarkdown('Status')
  Object.assign(node_24.style, {
    'font-size': '12px',
    'font-weight': '700',
  })
  node_24.dataset.component = 'MetaLabel'
  node_23.appendChild(node_24)

  // MetaValue
  const node_25 = document.createElement('span')
  _elements['node-25'] = node_25
  node_25.dataset.mirrorId = 'node-25'
  node_25.dataset.mirrorName = 'MetaValue'
  node_25.innerHTML = formatInlineMarkdown('Proto-Personas, hypothesenbasiert')
  Object.assign(node_25.style, {
    'font-size': '13px',
    'font-weight': '400',
  })
  node_25.dataset.component = 'MetaValue'
  node_23.appendChild(node_25)

  node_16.appendChild(node_23)

  // MetaPair
  const node_26 = document.createElement('div')
  _elements['node-26'] = node_26
  node_26.dataset.mirrorId = 'node-26'
  node_26.dataset.mirrorName = 'MetaPair'
  Object.assign(node_26.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '2px',
  })
  node_26.dataset.component = 'MetaPair'
  // MetaLabel
  const node_27 = document.createElement('span')
  _elements['node-27'] = node_27
  node_27.dataset.mirrorId = 'node-27'
  node_27.dataset.mirrorName = 'MetaLabel'
  node_27.innerHTML = formatInlineMarkdown('Scope')
  Object.assign(node_27.style, {
    'font-size': '12px',
    'font-weight': '700',
  })
  node_27.dataset.component = 'MetaLabel'
  node_26.appendChild(node_27)

  // MetaValue
  const node_28 = document.createElement('span')
  _elements['node-28'] = node_28
  node_28.dataset.mirrorId = 'node-28'
  node_28.dataset.mirrorName = 'MetaValue'
  node_28.innerHTML = formatInlineMarkdown('Studiengang Informatik')
  Object.assign(node_28.style, {
    'font-size': '13px',
    'font-weight': '400',
  })
  node_28.dataset.component = 'MetaValue'
  node_26.appendChild(node_28)

  node_16.appendChild(node_26)

  node_13.appendChild(node_16)

  // Domain
  const node_29 = document.createElement('span')
  _elements['node-29'] = node_29
  node_29.dataset.mirrorId = 'node-29'
  node_29.dataset.mirrorName = 'Domain'
  node_29.innerHTML = formatInlineMarkdown('www.fhnw.ch / informatik')
  Object.assign(node_29.style, {
    'font-size': '12px',
    'font-weight': '700',
  })
  node_29.dataset.component = 'Domain'
  node_13.appendChild(node_29)

  node_11.appendChild(node_13)

  // HeroBar
  const node_30 = document.createElement('div')
  _elements['node-30'] = node_30
  node_30.dataset.mirrorId = 'node-30'
  node_30.dataset.mirrorName = 'HeroBar'
  Object.assign(node_30.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '1140px',
    'flex-shrink': '0',
    height: '14px',
    'flex-shrink': '0',
    background: 'var(--yellow-bg)',
  })
  node_30.dataset.component = 'HeroBar'
  node_11.appendChild(node_30)

  _root.appendChild(node_11)

  // Section
  const node_31 = document.createElement('div')
  _elements['node-31'] = node_31
  node_31.dataset.mirrorId = 'node-31'
  node_31.dataset.mirrorName = 'Section'
  Object.assign(node_31.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    padding: '96px 24px 96px 24px',
    width: '100%',
  })
  node_31.dataset.component = 'Section'
  // Container
  const node_32 = document.createElement('div')
  _elements['node-32'] = node_32
  node_32.dataset.mirrorId = 'node-32'
  node_32.dataset.mirrorName = 'Container'
  Object.assign(node_32.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_32.dataset.component = 'Container'
  // H2
  const node_33 = document.createElement('span')
  _elements['node-33'] = node_33
  node_33.dataset.mirrorId = 'node-33'
  node_33.dataset.mirrorName = 'H2'
  node_33.innerHTML = formatInlineMarkdown(
    'Fünf Personas, die zusammen den Entscheidungsraum *aufspannen*.'
  )
  Object.assign(node_33.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_33.dataset.component = 'H2'
  node_32.appendChild(node_33)

  // TocList
  const node_34 = document.createElement('div')
  _elements['node-34'] = node_34
  node_34.dataset.mirrorId = 'node-34'
  node_34.dataset.mirrorName = 'TocList'
  Object.assign(node_34.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '0px',
  })
  node_34.dataset.component = 'TocList'
  // TocRow
  const node_35 = document.createElement('div')
  _elements['node-35'] = node_35
  node_35.dataset.mirrorId = 'node-35'
  node_35.dataset.mirrorName = 'TocRow'
  Object.assign(node_35.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
    gap: '56px',
    padding: '16px 8px',
  })
  node_35.dataset.layout = 'flex'
  node_35.dataset.component = 'TocRow'
  // TocNum
  const node_36 = document.createElement('span')
  _elements['node-36'] = node_36
  node_36.dataset.mirrorId = 'node-36'
  node_36.dataset.mirrorName = 'TocNum'
  node_36.innerHTML = formatInlineMarkdown('01')
  Object.assign(node_36.style, {
    'font-size': '22px',
    'font-weight': '300',
  })
  node_36.dataset.component = 'TocNum'
  node_35.appendChild(node_36)

  // TocName
  const node_37 = document.createElement('span')
  _elements['node-37'] = node_37
  node_37.dataset.mirrorId = 'node-37'
  node_37.dataset.mirrorName = 'TocName'
  node_37.innerHTML = formatInlineMarkdown('Lukas')
  Object.assign(node_37.style, {
    'font-size': '34px',
    'font-weight': '700',
    'letter-spacing': '-0.01em',
  })
  node_37.dataset.component = 'TocName'
  node_35.appendChild(node_37)

  // TocTag
  const node_38 = document.createElement('span')
  _elements['node-38'] = node_38
  node_38.dataset.mirrorId = 'node-38'
  node_38.dataset.mirrorName = 'TocTag'
  node_38.innerHTML = formatInlineMarkdown('der überzeugte Tüftler')
  Object.assign(node_38.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.35',
  })
  node_38.dataset.component = 'TocTag'
  node_35.appendChild(node_38)

  node_34.appendChild(node_35)

  // TocRow
  const node_39 = document.createElement('div')
  _elements['node-39'] = node_39
  node_39.dataset.mirrorId = 'node-39'
  node_39.dataset.mirrorName = 'TocRow'
  Object.assign(node_39.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
    gap: '56px',
    padding: '16px 8px',
  })
  node_39.dataset.layout = 'flex'
  node_39.dataset.component = 'TocRow'
  // TocNum
  const node_40 = document.createElement('span')
  _elements['node-40'] = node_40
  node_40.dataset.mirrorId = 'node-40'
  node_40.dataset.mirrorName = 'TocNum'
  node_40.innerHTML = formatInlineMarkdown('02')
  Object.assign(node_40.style, {
    'font-size': '22px',
    'font-weight': '300',
  })
  node_40.dataset.component = 'TocNum'
  node_39.appendChild(node_40)

  // TocName
  const node_41 = document.createElement('span')
  _elements['node-41'] = node_41
  node_41.dataset.mirrorId = 'node-41'
  node_41.dataset.mirrorName = 'TocName'
  node_41.innerHTML = formatInlineMarkdown('Sara')
  Object.assign(node_41.style, {
    'font-size': '34px',
    'font-weight': '700',
    'letter-spacing': '-0.01em',
  })
  node_41.dataset.component = 'TocName'
  node_39.appendChild(node_41)

  // TocTag
  const node_42 = document.createElement('span')
  _elements['node-42'] = node_42
  node_42.dataset.mirrorId = 'node-42'
  node_42.dataset.mirrorName = 'TocTag'
  node_42.innerHTML = formatInlineMarkdown('die zweifelnde Quereinsteigerin')
  Object.assign(node_42.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.35',
  })
  node_42.dataset.component = 'TocTag'
  node_39.appendChild(node_42)

  node_34.appendChild(node_39)

  // TocRow
  const node_43 = document.createElement('div')
  _elements['node-43'] = node_43
  node_43.dataset.mirrorId = 'node-43'
  node_43.dataset.mirrorName = 'TocRow'
  Object.assign(node_43.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
    gap: '56px',
    padding: '16px 8px',
  })
  node_43.dataset.layout = 'flex'
  node_43.dataset.component = 'TocRow'
  // TocNum
  const node_44 = document.createElement('span')
  _elements['node-44'] = node_44
  node_44.dataset.mirrorId = 'node-44'
  node_44.dataset.mirrorName = 'TocNum'
  node_44.innerHTML = formatInlineMarkdown('03')
  Object.assign(node_44.style, {
    'font-size': '22px',
    'font-weight': '300',
  })
  node_44.dataset.component = 'TocNum'
  node_43.appendChild(node_44)

  // TocName
  const node_45 = document.createElement('span')
  _elements['node-45'] = node_45
  node_45.dataset.mirrorId = 'node-45'
  node_45.dataset.mirrorName = 'TocName'
  node_45.innerHTML = formatInlineMarkdown('Marco')
  Object.assign(node_45.style, {
    'font-size': '34px',
    'font-weight': '700',
    'letter-spacing': '-0.01em',
  })
  node_45.dataset.component = 'TocName'
  node_43.appendChild(node_45)

  // TocTag
  const node_46 = document.createElement('span')
  _elements['node-46'] = node_46
  node_46.dataset.mirrorId = 'node-46'
  node_46.dataset.mirrorName = 'TocTag'
  node_46.innerHTML = formatInlineMarkdown('der pragmatische EFZ-Absolvent')
  Object.assign(node_46.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.35',
  })
  node_46.dataset.component = 'TocTag'
  node_43.appendChild(node_46)

  node_34.appendChild(node_43)

  // TocRow
  const node_47 = document.createElement('div')
  _elements['node-47'] = node_47
  node_47.dataset.mirrorId = 'node-47'
  node_47.dataset.mirrorName = 'TocRow'
  Object.assign(node_47.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
    gap: '56px',
    padding: '16px 8px',
  })
  node_47.dataset.layout = 'flex'
  node_47.dataset.component = 'TocRow'
  // TocNum
  const node_48 = document.createElement('span')
  _elements['node-48'] = node_48
  node_48.dataset.mirrorId = 'node-48'
  node_48.dataset.mirrorName = 'TocNum'
  node_48.innerHTML = formatInlineMarkdown('04')
  Object.assign(node_48.style, {
    'font-size': '22px',
    'font-weight': '300',
  })
  node_48.dataset.component = 'TocNum'
  node_47.appendChild(node_48)

  // TocName
  const node_49 = document.createElement('span')
  _elements['node-49'] = node_49
  node_49.dataset.mirrorId = 'node-49'
  node_49.dataset.mirrorName = 'TocName'
  node_49.innerHTML = formatInlineMarkdown('Nadia')
  Object.assign(node_49.style, {
    'font-size': '34px',
    'font-weight': '700',
    'letter-spacing': '-0.01em',
  })
  node_49.dataset.component = 'TocName'
  node_47.appendChild(node_49)

  // TocTag
  const node_50 = document.createElement('span')
  _elements['node-50'] = node_50
  node_50.dataset.mirrorId = 'node-50'
  node_50.dataset.mirrorName = 'TocTag'
  node_50.innerHTML = formatInlineMarkdown('die orientierungslose Maturandin')
  Object.assign(node_50.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.35',
  })
  node_50.dataset.component = 'TocTag'
  node_47.appendChild(node_50)

  node_34.appendChild(node_47)

  // TocRow
  const node_51 = document.createElement('div')
  _elements['node-51'] = node_51
  node_51.dataset.mirrorId = 'node-51'
  node_51.dataset.mirrorName = 'TocRow'
  Object.assign(node_51.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
    gap: '56px',
    padding: '16px 8px',
  })
  node_51.dataset.layout = 'flex'
  node_51.dataset.component = 'TocRow'
  // TocNum
  const node_52 = document.createElement('span')
  _elements['node-52'] = node_52
  node_52.dataset.mirrorId = 'node-52'
  node_52.dataset.mirrorName = 'TocNum'
  node_52.innerHTML = formatInlineMarkdown('05')
  Object.assign(node_52.style, {
    'font-size': '22px',
    'font-weight': '300',
  })
  node_52.dataset.component = 'TocNum'
  node_51.appendChild(node_52)

  // TocName
  const node_53 = document.createElement('span')
  _elements['node-53'] = node_53
  node_53.dataset.mirrorId = 'node-53'
  node_53.dataset.mirrorName = 'TocName'
  node_53.innerHTML = formatInlineMarkdown('Tim')
  Object.assign(node_53.style, {
    'font-size': '34px',
    'font-weight': '700',
    'letter-spacing': '-0.01em',
  })
  node_53.dataset.component = 'TocName'
  node_51.appendChild(node_53)

  // TocTag
  const node_54 = document.createElement('span')
  _elements['node-54'] = node_54
  node_54.dataset.mirrorId = 'node-54'
  node_54.dataset.mirrorName = 'TocTag'
  node_54.innerHTML = formatInlineMarkdown('der Game-begeisterte Spezialist')
  Object.assign(node_54.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.35',
  })
  node_54.dataset.component = 'TocTag'
  node_51.appendChild(node_54)

  node_34.appendChild(node_51)

  node_32.appendChild(node_34)

  node_31.appendChild(node_32)

  _root.appendChild(node_31)

  // SectionWide
  const node_55 = document.createElement('div')
  _elements['node-55'] = node_55
  node_55.dataset.mirrorId = 'node-55'
  node_55.dataset.mirrorName = 'SectionWide'
  Object.assign(node_55.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    padding: '112px 24px 112px 24px',
    width: '100%',
  })
  node_55.dataset.component = 'SectionWide'
  // Container
  const node_56 = document.createElement('div')
  _elements['node-56'] = node_56
  node_56.dataset.mirrorId = 'node-56'
  node_56.dataset.mirrorName = 'Container'
  Object.assign(node_56.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_56.dataset.component = 'Container'
  // TwoColumn
  const node_57 = document.createElement('div')
  _elements['node-57'] = node_57
  node_57.dataset.mirrorId = 'node-57'
  node_57.dataset.mirrorName = 'TwoColumn'
  Object.assign(node_57.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '64px',
  })
  node_57.dataset.layout = 'flex'
  node_57.dataset.component = 'TwoColumn'
  // ProseColumn
  const node_58 = document.createElement('div')
  _elements['node-58'] = node_58
  node_58.dataset.mirrorId = 'node-58'
  node_58.dataset.mirrorName = 'ProseColumn'
  Object.assign(node_58.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '280px',
    'flex-shrink': '0',
  })
  node_58.dataset.component = 'ProseColumn'
  // H2
  const node_59 = document.createElement('span')
  _elements['node-59'] = node_59
  node_59.dataset.mirrorId = 'node-59'
  node_59.dataset.mirrorName = 'H2'
  node_59.innerHTML = formatInlineMarkdown('Übergreifende Beobachtungen')
  Object.assign(node_59.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_59.dataset.component = 'H2'
  node_58.appendChild(node_59)

  node_57.appendChild(node_58)

  // ProseBody
  const node_60 = document.createElement('div')
  _elements['node-60'] = node_60
  node_60.dataset.mirrorId = 'node-60'
  node_60.dataset.mirrorName = 'ProseBody'
  Object.assign(node_60.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '18px',
    'flex-grow': '1',
    'max-width': '768px',
  })
  node_60.dataset.component = 'ProseBody'
  // DashItem
  const node_61 = document.createElement('div')
  _elements['node-61'] = node_61
  node_61.dataset.mirrorId = 'node-61'
  node_61.dataset.mirrorName = 'DashItem'
  Object.assign(node_61.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_61.dataset.layout = 'flex'
  node_61.dataset.component = 'DashItem'
  // DashMarker
  const node_62 = document.createElement('span')
  _elements['node-62'] = node_62
  node_62.dataset.mirrorId = 'node-62'
  node_62.dataset.mirrorName = 'DashMarker'
  node_62.innerHTML = formatInlineMarkdown('—')
  Object.assign(node_62.style, {
    width: '12px',
    'flex-shrink': '0',
    'font-weight': '400',
  })
  node_62.dataset.component = 'DashMarker'
  node_62.dataset.slot = 'DashMarker'
  node_61.appendChild(node_62)

  // BodyTxtCompact
  const node_63 = document.createElement('span')
  _elements['node-63'] = node_63
  node_63.dataset.mirrorId = 'node-63'
  node_63.dataset.mirrorName = 'BodyTxtCompact'
  node_63.innerHTML = formatInlineMarkdown(
    '**Universalfragen**, die durch fast alle Personas durchgehen, nur in unterschiedlicher Tonart:'
  )
  Object.assign(node_63.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_63.dataset.component = 'BodyTxtCompact'
  node_61.appendChild(node_63)

  // Unknown
  const node_64 = document.createElement('div')
  _elements['node-64'] = node_64
  node_64.dataset.mirrorId = 'node-64'
  node_64.dataset.mirrorName = 'Unknown'
  node_64.dataset.component = 'Unknown'
  node_61.appendChild(node_64)

  // Unknown
  const node_65 = document.createElement('div')
  _elements['node-65'] = node_65
  node_65.dataset.mirrorId = 'node-65'
  node_65.dataset.mirrorName = 'Unknown'
  node_65.dataset.component = 'Unknown'
  node_61.appendChild(node_65)

  // Unknown
  const node_66 = document.createElement('div')
  _elements['node-66'] = node_66
  node_66.dataset.mirrorId = 'node-66'
  node_66.dataset.mirrorName = 'Unknown'
  node_66.dataset.component = 'Unknown'
  node_61.appendChild(node_66)

  // Unknown
  const node_67 = document.createElement('div')
  _elements['node-67'] = node_67
  node_67.dataset.mirrorId = 'node-67'
  node_67.dataset.mirrorName = 'Unknown'
  node_67.dataset.component = 'Unknown'
  node_61.appendChild(node_67)

  node_60.appendChild(node_61)

  // DashItem
  const node_68 = document.createElement('div')
  _elements['node-68'] = node_68
  node_68.dataset.mirrorId = 'node-68'
  node_68.dataset.mirrorName = 'DashItem'
  Object.assign(node_68.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_68.dataset.layout = 'flex'
  node_68.dataset.component = 'DashItem'
  // DashMarker
  const node_69 = document.createElement('span')
  _elements['node-69'] = node_69
  node_69.dataset.mirrorId = 'node-69'
  node_69.dataset.mirrorName = 'DashMarker'
  node_69.innerHTML = formatInlineMarkdown('—')
  Object.assign(node_69.style, {
    width: '12px',
    'flex-shrink': '0',
    'font-weight': '400',
  })
  node_69.dataset.component = 'DashMarker'
  node_69.dataset.slot = 'DashMarker'
  node_68.appendChild(node_69)

  // BodyTxtCompact
  const node_70 = document.createElement('span')
  _elements['node-70'] = node_70
  node_70.dataset.mirrorId = 'node-70'
  node_70.dataset.mirrorName = 'BodyTxtCompact'
  node_70.innerHTML = formatInlineMarkdown(
    'Sehr viele Fragen haben **nichts mit Curriculum** zu tun. Eine Studiengangs-Webseite, die mit Modulübersicht startet, antwortet auf eine Frage, die niemand zuerst stellt.'
  )
  Object.assign(node_70.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_70.dataset.component = 'BodyTxtCompact'
  node_68.appendChild(node_70)

  node_60.appendChild(node_68)

  // DashItem
  const node_71 = document.createElement('div')
  _elements['node-71'] = node_71
  node_71.dataset.mirrorId = 'node-71'
  node_71.dataset.mirrorName = 'DashItem'
  Object.assign(node_71.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_71.dataset.layout = 'flex'
  node_71.dataset.component = 'DashItem'
  // DashMarker
  const node_72 = document.createElement('span')
  _elements['node-72'] = node_72
  node_72.dataset.mirrorId = 'node-72'
  node_72.dataset.mirrorName = 'DashMarker'
  node_72.innerHTML = formatInlineMarkdown('—')
  Object.assign(node_72.style, {
    width: '12px',
    'flex-shrink': '0',
    'font-weight': '400',
  })
  node_72.dataset.component = 'DashMarker'
  node_72.dataset.slot = 'DashMarker'
  node_71.appendChild(node_72)

  // BodyTxtCompact
  const node_73 = document.createElement('span')
  _elements['node-73'] = node_73
  node_73.dataset.mirrorId = 'node-73'
  node_73.dataset.mirrorName = 'BodyTxtCompact'
  node_73.innerHTML = formatInlineMarkdown(
    'Manche Fragen sind unangenehm für die Hochschule («Ist FH zweite Wahl?», «Werde ich als Quereinsteigerin auffallen?», «Kann man von Games leben?»). Wer sie ehrlich beantwortet statt umschifft, gewinnt Vertrauen.'
  )
  Object.assign(node_73.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_73.dataset.component = 'BodyTxtCompact'
  node_71.appendChild(node_73)

  node_60.appendChild(node_71)

  // DashItem
  const node_74 = document.createElement('div')
  _elements['node-74'] = node_74
  node_74.dataset.mirrorId = 'node-74'
  node_74.dataset.mirrorName = 'DashItem'
  Object.assign(node_74.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_74.dataset.layout = 'flex'
  node_74.dataset.component = 'DashItem'
  // DashMarker
  const node_75 = document.createElement('span')
  _elements['node-75'] = node_75
  node_75.dataset.mirrorId = 'node-75'
  node_75.dataset.mirrorName = 'DashMarker'
  node_75.innerHTML = formatInlineMarkdown('—')
  Object.assign(node_75.style, {
    width: '12px',
    'flex-shrink': '0',
    'font-weight': '400',
  })
  node_75.dataset.component = 'DashMarker'
  node_75.dataset.slot = 'DashMarker'
  node_74.appendChild(node_75)

  // BodyTxtCompact
  const node_76 = document.createElement('span')
  _elements['node-76'] = node_76
  node_76.dataset.mirrorId = 'node-76'
  node_76.dataset.mirrorName = 'BodyTxtCompact'
  node_76.innerHTML = formatInlineMarkdown(
    '**Neu seit 2024 — die AI-Frage:** «Was bedeutet AI für meinen Beruf? Werde ich noch gebraucht, wenn AI in fünf Jahren das Routine-Coding übernimmt?» Diese Sorge zieht sich durch alle Personas — am schärfsten bei denen, die Karriere-ROI rechnen (Marco, Sara), am abstraktesten bei den noch Suchenden (Nadia), spezifisch beim Spezialisten (Tim mit AI-generierten Spielen). Eine Studieninfo, die diese Frage umschifft, wirkt 2026 weltfremd.'
  )
  Object.assign(node_76.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_76.dataset.component = 'BodyTxtCompact'
  node_74.appendChild(node_76)

  node_60.appendChild(node_74)

  node_57.appendChild(node_60)

  node_56.appendChild(node_57)

  node_55.appendChild(node_56)

  _root.appendChild(node_55)

  // SectionWide
  const node_77 = document.createElement('div')
  _elements['node-77'] = node_77
  node_77.dataset.mirrorId = 'node-77'
  node_77.dataset.mirrorName = 'SectionWide'
  Object.assign(node_77.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    padding: '112px 24px 112px 24px',
    width: '100%',
  })
  node_77.dataset.component = 'SectionWide'
  // Container
  const node_78 = document.createElement('div')
  _elements['node-78'] = node_78
  node_78.dataset.mirrorId = 'node-78'
  node_78.dataset.mirrorName = 'Container'
  Object.assign(node_78.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_78.dataset.component = 'Container'
  // TwoColumn
  const node_79 = document.createElement('div')
  _elements['node-79'] = node_79
  node_79.dataset.mirrorId = 'node-79'
  node_79.dataset.mirrorName = 'TwoColumn'
  Object.assign(node_79.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '64px',
  })
  node_79.dataset.layout = 'flex'
  node_79.dataset.component = 'TwoColumn'
  // ProseColumn
  const node_80 = document.createElement('div')
  _elements['node-80'] = node_80
  node_80.dataset.mirrorId = 'node-80'
  node_80.dataset.mirrorName = 'ProseColumn'
  Object.assign(node_80.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '280px',
    'flex-shrink': '0',
  })
  node_80.dataset.component = 'ProseColumn'
  // H2
  const node_81 = document.createElement('span')
  _elements['node-81'] = node_81
  node_81.dataset.mirrorId = 'node-81'
  node_81.dataset.mirrorName = 'H2'
  node_81.innerHTML = formatInlineMarkdown('Warum diese fünf Personas?')
  Object.assign(node_81.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_81.dataset.component = 'H2'
  node_80.appendChild(node_81)

  node_79.appendChild(node_80)

  // ProseBody
  const node_82 = document.createElement('div')
  _elements['node-82'] = node_82
  node_82.dataset.mirrorId = 'node-82'
  node_82.dataset.mirrorName = 'ProseBody'
  Object.assign(node_82.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '18px',
    'flex-grow': '1',
    'max-width': '768px',
  })
  node_82.dataset.component = 'ProseBody'
  // BodyTxt
  const node_83 = document.createElement('span')
  _elements['node-83'] = node_83
  node_83.dataset.mirrorId = 'node-83'
  node_83.dataset.mirrorName = 'BodyTxt'
  node_83.innerHTML = formatInlineMarkdown(
    'Wir haben uns bewusst für fünf Personas entschieden — genug, um den Entscheidungsraum **breit aufzuspannen**, wenig genug, um sie im Alltag wirklich zu nutzen. Drei wären zu pauschal, sieben werden in der Praxis vergessen. Die Auswahl folgt einer Kontrast-Logik:'
  )
  Object.assign(node_83.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_83.dataset.component = 'BodyTxt'
  node_82.appendChild(node_83)

  // DashItem
  const node_84 = document.createElement('div')
  _elements['node-84'] = node_84
  node_84.dataset.mirrorId = 'node-84'
  node_84.dataset.mirrorName = 'DashItem'
  Object.assign(node_84.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_84.dataset.layout = 'flex'
  node_84.dataset.component = 'DashItem'
  // DashMarker
  const node_85 = document.createElement('span')
  _elements['node-85'] = node_85
  node_85.dataset.mirrorId = 'node-85'
  node_85.dataset.mirrorName = 'DashMarker'
  node_85.innerHTML = formatInlineMarkdown('—')
  Object.assign(node_85.style, {
    width: '12px',
    'flex-shrink': '0',
    'font-weight': '400',
  })
  node_85.dataset.component = 'DashMarker'
  node_85.dataset.slot = 'DashMarker'
  node_84.appendChild(node_85)

  // BodyTxtCompact
  const node_86 = document.createElement('span')
  _elements['node-86'] = node_86
  node_86.dataset.mirrorId = 'node-86'
  node_86.dataset.mirrorName = 'BodyTxtCompact'
  node_86.innerHTML = formatInlineMarkdown(
    '**Lukas und Nadia** stehen für die Gymi-Welt, an entgegengesetzten Enden: hochkompetent vs. orientierungslos.'
  )
  Object.assign(node_86.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_86.dataset.component = 'BodyTxtCompact'
  node_84.appendChild(node_86)

  node_82.appendChild(node_84)

  // DashItem
  const node_87 = document.createElement('div')
  _elements['node-87'] = node_87
  node_87.dataset.mirrorId = 'node-87'
  node_87.dataset.mirrorName = 'DashItem'
  Object.assign(node_87.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_87.dataset.layout = 'flex'
  node_87.dataset.component = 'DashItem'
  // DashMarker
  const node_88 = document.createElement('span')
  _elements['node-88'] = node_88
  node_88.dataset.mirrorId = 'node-88'
  node_88.dataset.mirrorName = 'DashMarker'
  node_88.innerHTML = formatInlineMarkdown('—')
  Object.assign(node_88.style, {
    width: '12px',
    'flex-shrink': '0',
    'font-weight': '400',
  })
  node_88.dataset.component = 'DashMarker'
  node_88.dataset.slot = 'DashMarker'
  node_87.appendChild(node_88)

  // BodyTxtCompact
  const node_89 = document.createElement('span')
  _elements['node-89'] = node_89
  node_89.dataset.mirrorId = 'node-89'
  node_89.dataset.mirrorName = 'BodyTxtCompact'
  node_89.innerHTML = formatInlineMarkdown(
    '**Sara und Marco** stehen für die Berufslehre-Welt, ebenfalls kontrastiv: Quereinstieg ohne Tech-Hintergrund vs. logischer nächster Karriereschritt von einem EFZ aus der Branche.'
  )
  Object.assign(node_89.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_89.dataset.component = 'BodyTxtCompact'
  node_87.appendChild(node_89)

  node_82.appendChild(node_87)

  // DashItem
  const node_90 = document.createElement('div')
  _elements['node-90'] = node_90
  node_90.dataset.mirrorId = 'node-90'
  node_90.dataset.mirrorName = 'DashItem'
  Object.assign(node_90.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_90.dataset.layout = 'flex'
  node_90.dataset.component = 'DashItem'
  // DashMarker
  const node_91 = document.createElement('span')
  _elements['node-91'] = node_91
  node_91.dataset.mirrorId = 'node-91'
  node_91.dataset.mirrorName = 'DashMarker'
  node_91.innerHTML = formatInlineMarkdown('—')
  Object.assign(node_91.style, {
    width: '12px',
    'flex-shrink': '0',
    'font-weight': '400',
  })
  node_91.dataset.component = 'DashMarker'
  node_91.dataset.slot = 'DashMarker'
  node_90.appendChild(node_91)

  // BodyTxtCompact
  const node_92 = document.createElement('span')
  _elements['node-92'] = node_92
  node_92.dataset.mirrorId = 'node-92'
  node_92.dataset.mirrorName = 'BodyTxtCompact'
  node_92.innerHTML = formatInlineMarkdown(
    '**Tim** vertritt eine eigene Logik: jemand, der nicht primär eine Hochschule, sondern eine **Studienrichtung** sucht — und FHNW nur erwägt, wenn die Studienrichtung wirklich trägt.'
  )
  Object.assign(node_92.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_92.dataset.component = 'BodyTxtCompact'
  node_90.appendChild(node_92)

  node_82.appendChild(node_90)

  // BodyTxt
  const node_93 = document.createElement('span')
  _elements['node-93'] = node_93
  node_93.dataset.mirrorId = 'node-93'
  node_93.dataset.mirrorName = 'BodyTxt'
  node_93.innerHTML = formatInlineMarkdown(
    'Zusammen decken sie vier Achsen ab: **Selbstvertrauen** (hoch/tief), **Klarheit der Wahl** (entschieden/suchend), **Treiber** (intrinsisch/extrinsisch), **Vorbildung** (Maturität/EFZ).'
  )
  Object.assign(node_93.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_93.dataset.component = 'BodyTxt'
  node_82.appendChild(node_93)

  node_79.appendChild(node_82)

  node_78.appendChild(node_79)

  node_77.appendChild(node_78)

  _root.appendChild(node_77)

  // PersonaBlock
  const node_94 = document.createElement('div')
  _elements['node-94'] = node_94
  node_94.dataset.mirrorId = 'node-94'
  node_94.dataset.mirrorName = 'PersonaBlock'
  Object.assign(node_94.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    padding: '0px 24px 80px 24px',
  })
  node_94.dataset.component = 'PersonaBlock'
  // PersonaHeaderBand
  const node_95 = document.createElement('div')
  _elements['node-95'] = node_95
  node_95.dataset.mirrorId = 'node-95'
  node_95.dataset.mirrorName = 'PersonaHeaderBand'
  Object.assign(node_95.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--yellow-bg)',
    padding: '72px 24px 64px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    margin: '0px 0px 80px 0px',
  })
  node_95.dataset.component = 'PersonaHeaderBand'
  // Container
  const node_96 = document.createElement('div')
  _elements['node-96'] = node_96
  node_96.dataset.mirrorId = 'node-96'
  node_96.dataset.mirrorName = 'Container'
  Object.assign(node_96.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_96.dataset.component = 'Container'
  // PersonaHeaderRow
  const node_97 = document.createElement('div')
  _elements['node-97'] = node_97
  node_97.dataset.mirrorId = 'node-97'
  node_97.dataset.mirrorName = 'PersonaHeaderRow'
  Object.assign(node_97.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-end',
    gap: '56px',
  })
  node_97.dataset.layout = 'flex'
  node_97.dataset.component = 'PersonaHeaderRow'
  // PersonaNumeral
  const node_98 = document.createElement('span')
  _elements['node-98'] = node_98
  node_98.dataset.mirrorId = 'node-98'
  node_98.dataset.mirrorName = 'PersonaNumeral'
  node_98.innerHTML = formatInlineMarkdown('01')
  Object.assign(node_98.style, {
    'font-size': '200px',
    'font-weight': '700',
    'letter-spacing': '-0.04em',
    'line-height': '0.82',
  })
  node_98.dataset.component = 'PersonaNumeral'
  node_97.appendChild(node_98)

  // PersonaTitleStack
  const node_99 = document.createElement('div')
  _elements['node-99'] = node_99
  node_99.dataset.mirrorId = 'node-99'
  node_99.dataset.mirrorName = 'PersonaTitleStack'
  Object.assign(node_99.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '6px',
    'flex-grow': '1',
  })
  node_99.dataset.component = 'PersonaTitleStack'
  // PersonaName
  const node_100 = document.createElement('span')
  _elements['node-100'] = node_100
  node_100.dataset.mirrorId = 'node-100'
  node_100.dataset.mirrorName = 'PersonaName'
  node_100.innerHTML = formatInlineMarkdown('Lukas, *der überzeugte Tüftler*')
  Object.assign(node_100.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_100.dataset.component = 'PersonaName'
  node_99.appendChild(node_100)

  node_97.appendChild(node_99)

  node_96.appendChild(node_97)

  node_95.appendChild(node_96)

  // Container
  const node_101 = document.createElement('div')
  _elements['node-101'] = node_101
  node_101.dataset.mirrorId = 'node-101'
  node_101.dataset.mirrorName = 'Container'
  Object.assign(node_101.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_101.dataset.component = 'Container'
  // PersonaSteckbrief
  const node_102 = document.createElement('span')
  _elements['node-102'] = node_102
  node_102.dataset.mirrorId = 'node-102'
  node_102.dataset.mirrorName = 'PersonaSteckbrief'
  node_102.innerHTML = formatInlineMarkdown(
    '18, Gymi (Schwerpunkt Physik & Anwendungen Mathematik), Notenschnitt 5.3. Programmiert seit der Sek, hat einen aktiven GitHub mit eigenen Projekten (Discord-Bots, kleine Web-Apps), liest in der Freizeit über Compiler und Systemprogrammierung. Vater Ingenieur, Mutter Lehrerin.'
  )
  Object.assign(node_102.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'max-width': '80 ch',
  })
  node_102.dataset.component = 'PersonaSteckbrief'
  node_101.appendChild(node_102)

  node_95.appendChild(node_101)

  node_94.appendChild(node_95)

  // Container
  const node_103 = document.createElement('div')
  _elements['node-103'] = node_103
  node_103.dataset.mirrorId = 'node-103'
  node_103.dataset.mirrorName = 'Container'
  Object.assign(node_103.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_103.dataset.component = 'Container'
  // PersonaBody
  const node_104 = document.createElement('div')
  _elements['node-104'] = node_104
  node_104.dataset.mirrorId = 'node-104'
  node_104.dataset.mirrorName = 'PersonaBody'
  Object.assign(node_104.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '64px',
  })
  node_104.dataset.component = 'PersonaBody'
  // WarumBlock
  const node_105 = document.createElement('div')
  _elements['node-105'] = node_105
  node_105.dataset.mirrorId = 'node-105'
  node_105.dataset.mirrorName = 'WarumBlock'
  Object.assign(node_105.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
    'max-width': '920px',
  })
  node_105.dataset.component = 'WarumBlock'
  // H4
  const node_106 = document.createElement('span')
  _elements['node-106'] = node_106
  node_106.dataset.mirrorId = 'node-106'
  node_106.dataset.mirrorName = 'H4'
  node_106.innerHTML = formatInlineMarkdown('Warum diese Persona?')
  Object.assign(node_106.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_106.dataset.component = 'H4'
  node_105.appendChild(node_106)

  // BodyTxt
  const node_107 = document.createElement('span')
  _elements['node-107'] = node_107
  node_107.dataset.mirrorId = 'node-107'
  node_107.dataset.mirrorName = 'BodyTxt'
  node_107.innerHTML = formatInlineMarkdown(
    'Lukas verkörpert das Segment der **hochkompetenten, schon technikaffinen Maturand:innen**, die FH und Universität ernsthaft gegeneinander abwägen. Lukas zwingt uns, eine unangenehme, aber zentrale Frage ernst zu nehmen: **«Ist FH zweite Wahl?»** Wer ihn nicht überzeugt, kommuniziert defensiv und verliert die selbstbewussten Profile an die ETH.'
  )
  Object.assign(node_107.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_107.dataset.component = 'BodyTxt'
  node_105.appendChild(node_107)

  node_104.appendChild(node_105)

  // DimGrid
  const node_108 = document.createElement('div')
  _elements['node-108'] = node_108
  node_108.dataset.mirrorId = 'node-108'
  node_108.dataset.mirrorName = 'DimGrid'
  Object.assign(node_108.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(4, 1fr)',
    gap: '32px 64px',
  })
  node_108.dataset.layout = 'grid'
  node_108.dataset.component = 'DimGrid'
  // Dim
  const node_109 = document.createElement('div')
  _elements['node-109'] = node_109
  node_109.dataset.mirrorId = 'node-109'
  node_109.dataset.mirrorName = 'Dim'
  Object.assign(node_109.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_109.dataset.component = 'Dim'
  // H4
  const node_110 = document.createElement('span')
  _elements['node-110'] = node_110
  node_110.dataset.mirrorId = 'node-110'
  node_110.dataset.mirrorName = 'H4'
  node_110.innerHTML = formatInlineMarkdown('Situation')
  Object.assign(node_110.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_110.dataset.component = 'H4'
  node_109.appendChild(node_110)

  // BodyTxt
  const node_111 = document.createElement('span')
  _elements['node-111'] = node_111
  node_111.dataset.mirrorId = 'node-111'
  node_111.dataset.mirrorName = 'BodyTxt'
  node_111.innerHTML = formatInlineMarkdown(
    'Steht vier Monate vor der Matura. Klassenlehrer empfehlen ihm «unbedingt ETH». Hat einen FHNW-Schnuppertag besucht und war positiv überrascht (mehr Praxis, weniger anonym als erwartet). Innerlich hin- und hergerissen — die Idee, «nur» an eine FH zu gehen, fühlt sich für ihn nach Statusverlust an, obwohl das fachliche Argument für die FHNW spricht.'
  )
  Object.assign(node_111.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_111.dataset.component = 'BodyTxt'
  node_109.appendChild(node_111)

  node_108.appendChild(node_109)

  // Dim
  const node_112 = document.createElement('div')
  _elements['node-112'] = node_112
  node_112.dataset.mirrorId = 'node-112'
  node_112.dataset.mirrorName = 'Dim'
  Object.assign(node_112.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_112.dataset.component = 'Dim'
  // H4
  const node_113 = document.createElement('span')
  _elements['node-113'] = node_113
  node_113.dataset.mirrorId = 'node-113'
  node_113.dataset.mirrorName = 'H4'
  node_113.innerHTML = formatInlineMarkdown('Ziel')
  Object.assign(node_113.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_113.dataset.component = 'H4'
  node_112.appendChild(node_113)

  // BodyTxt
  const node_114 = document.createElement('span')
  _elements['node-114'] = node_114
  node_114.dataset.mirrorId = 'node-114'
  node_114.dataset.mirrorName = 'BodyTxt'
  node_114.innerHTML = formatInlineMarkdown(
    'Ein Studium wählen, in dem er sich technisch wirklich weiterentwickeln kann. Langfristig sieht er sich entweder als Senior Engineer in einer technisch anspruchsvollen Firma oder in der Forschung.'
  )
  Object.assign(node_114.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_114.dataset.component = 'BodyTxt'
  node_112.appendChild(node_114)

  node_108.appendChild(node_112)

  // Dim
  const node_115 = document.createElement('div')
  _elements['node-115'] = node_115
  node_115.dataset.mirrorId = 'node-115'
  node_115.dataset.mirrorName = 'Dim'
  Object.assign(node_115.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_115.dataset.component = 'Dim'
  // H4
  const node_116 = document.createElement('span')
  _elements['node-116'] = node_116
  node_116.dataset.mirrorId = 'node-116'
  node_116.dataset.mirrorName = 'H4'
  node_116.innerHTML = formatInlineMarkdown('Herausforderung')
  Object.assign(node_116.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_116.dataset.component = 'H4'
  node_115.appendChild(node_116)

  // BodyTxtMuted
  const node_117 = document.createElement('span')
  _elements['node-117'] = node_117
  node_117.dataset.mirrorId = 'node-117'
  node_117.dataset.mirrorName = 'BodyTxtMuted'
  node_117.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_117.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_117.dataset.component = 'BodyTxtMuted'
  node_115.appendChild(node_117)

  node_108.appendChild(node_115)

  // Dim
  const node_118 = document.createElement('div')
  _elements['node-118'] = node_118
  node_118.dataset.mirrorId = 'node-118'
  node_118.dataset.mirrorName = 'Dim'
  Object.assign(node_118.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_118.dataset.component = 'Dim'
  // H4
  const node_119 = document.createElement('span')
  _elements['node-119'] = node_119
  node_119.dataset.mirrorId = 'node-119'
  node_119.dataset.mirrorName = 'H4'
  node_119.innerHTML = formatInlineMarkdown('Needs')
  Object.assign(node_119.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_119.dataset.component = 'H4'
  node_118.appendChild(node_119)

  // BodyTxtMuted
  const node_120 = document.createElement('span')
  _elements['node-120'] = node_120
  node_120.dataset.mirrorId = 'node-120'
  node_120.dataset.mirrorName = 'BodyTxtMuted'
  node_120.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_120.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_120.dataset.component = 'BodyTxtMuted'
  node_118.appendChild(node_120)

  node_108.appendChild(node_118)

  node_104.appendChild(node_108)

  // SoftBox
  const node_121 = document.createElement('div')
  _elements['node-121'] = node_121
  node_121.dataset.mirrorId = 'node-121'
  node_121.dataset.mirrorName = 'SoftBox'
  Object.assign(node_121.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--soft-bg)',
    padding: '22px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
  })
  node_121.dataset.component = 'SoftBox'
  // Unknown
  const node_122 = document.createElement('div')
  _elements['node-122'] = node_122
  node_122.dataset.mirrorId = 'node-122'
  node_122.dataset.mirrorName = 'Unknown'
  node_122.dataset.component = 'Unknown'
  node_121.appendChild(node_122)

  node_104.appendChild(node_121)

  // ImplikationenBlock
  const node_123 = document.createElement('div')
  _elements['node-123'] = node_123
  node_123.dataset.mirrorId = 'node-123'
  node_123.dataset.mirrorName = 'ImplikationenBlock'
  Object.assign(node_123.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '32px',
    'margin-top': '40px',
  })
  node_123.dataset.component = 'ImplikationenBlock'
  // H3
  const node_124 = document.createElement('span')
  _elements['node-124'] = node_124
  node_124.dataset.mirrorId = 'node-124'
  node_124.dataset.mirrorName = 'H3'
  node_124.innerHTML = formatInlineMarkdown('**Implikationen für FHNW-Kommunikation**')
  Object.assign(node_124.style, {
    'font-size': '28px',
    'font-weight': '700',
    'letter-spacing': '-0.015em',
    'line-height': '1.2',
  })
  node_124.dataset.component = 'H3'
  node_123.appendChild(node_124)

  // DimGridTwo
  const node_125 = document.createElement('div')
  _elements['node-125'] = node_125
  node_125.dataset.mirrorId = 'node-125'
  node_125.dataset.mirrorName = 'DimGridTwo'
  Object.assign(node_125.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(2, 1fr)',
    gap: '32px 64px',
  })
  node_125.dataset.layout = 'grid'
  node_125.dataset.component = 'DimGridTwo'
  // Dim
  const node_126 = document.createElement('div')
  _elements['node-126'] = node_126
  node_126.dataset.mirrorId = 'node-126'
  node_126.dataset.mirrorName = 'Dim'
  Object.assign(node_126.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_126.dataset.component = 'Dim'
  // H4
  const node_127 = document.createElement('span')
  _elements['node-127'] = node_127
  node_127.dataset.mirrorId = 'node-127'
  node_127.dataset.mirrorName = 'H4'
  node_127.innerHTML = formatInlineMarkdown('Webseite')
  Object.assign(node_127.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_127.dataset.component = 'H4'
  node_126.appendChild(node_127)

  // BodyTxtMuted
  const node_128 = document.createElement('span')
  _elements['node-128'] = node_128
  node_128.dataset.mirrorId = 'node-128'
  node_128.dataset.mirrorName = 'BodyTxtMuted'
  node_128.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_128.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_128.dataset.component = 'BodyTxtMuted'
  node_126.appendChild(node_128)

  node_125.appendChild(node_126)

  // Dim
  const node_129 = document.createElement('div')
  _elements['node-129'] = node_129
  node_129.dataset.mirrorId = 'node-129'
  node_129.dataset.mirrorName = 'Dim'
  Object.assign(node_129.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_129.dataset.component = 'Dim'
  // H4
  const node_130 = document.createElement('span')
  _elements['node-130'] = node_130
  node_130.dataset.mirrorId = 'node-130'
  node_130.dataset.mirrorName = 'H4'
  node_130.innerHTML = formatInlineMarkdown('Infoveranstaltung')
  Object.assign(node_130.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_130.dataset.component = 'H4'
  node_129.appendChild(node_130)

  // BodyTxtMuted
  const node_131 = document.createElement('span')
  _elements['node-131'] = node_131
  node_131.dataset.mirrorId = 'node-131'
  node_131.dataset.mirrorName = 'BodyTxtMuted'
  node_131.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_131.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_131.dataset.component = 'BodyTxtMuted'
  node_129.appendChild(node_131)

  node_125.appendChild(node_129)

  node_123.appendChild(node_125)

  node_104.appendChild(node_123)

  node_103.appendChild(node_104)

  node_94.appendChild(node_103)

  _root.appendChild(node_94)

  // PersonaBlock
  const node_132 = document.createElement('div')
  _elements['node-132'] = node_132
  node_132.dataset.mirrorId = 'node-132'
  node_132.dataset.mirrorName = 'PersonaBlock'
  Object.assign(node_132.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    padding: '0px 24px 80px 24px',
  })
  node_132.dataset.component = 'PersonaBlock'
  // PersonaHeaderBand
  const node_133 = document.createElement('div')
  _elements['node-133'] = node_133
  node_133.dataset.mirrorId = 'node-133'
  node_133.dataset.mirrorName = 'PersonaHeaderBand'
  Object.assign(node_133.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--yellow-bg)',
    padding: '72px 24px 64px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    margin: '0px 0px 80px 0px',
  })
  node_133.dataset.component = 'PersonaHeaderBand'
  // Container
  const node_134 = document.createElement('div')
  _elements['node-134'] = node_134
  node_134.dataset.mirrorId = 'node-134'
  node_134.dataset.mirrorName = 'Container'
  Object.assign(node_134.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_134.dataset.component = 'Container'
  // PersonaHeaderRow
  const node_135 = document.createElement('div')
  _elements['node-135'] = node_135
  node_135.dataset.mirrorId = 'node-135'
  node_135.dataset.mirrorName = 'PersonaHeaderRow'
  Object.assign(node_135.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-end',
    gap: '56px',
  })
  node_135.dataset.layout = 'flex'
  node_135.dataset.component = 'PersonaHeaderRow'
  // PersonaNumeral
  const node_136 = document.createElement('span')
  _elements['node-136'] = node_136
  node_136.dataset.mirrorId = 'node-136'
  node_136.dataset.mirrorName = 'PersonaNumeral'
  node_136.innerHTML = formatInlineMarkdown('02')
  Object.assign(node_136.style, {
    'font-size': '200px',
    'font-weight': '700',
    'letter-spacing': '-0.04em',
    'line-height': '0.82',
  })
  node_136.dataset.component = 'PersonaNumeral'
  node_135.appendChild(node_136)

  // PersonaTitleStack
  const node_137 = document.createElement('div')
  _elements['node-137'] = node_137
  node_137.dataset.mirrorId = 'node-137'
  node_137.dataset.mirrorName = 'PersonaTitleStack'
  Object.assign(node_137.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '6px',
    'flex-grow': '1',
  })
  node_137.dataset.component = 'PersonaTitleStack'
  // PersonaName
  const node_138 = document.createElement('span')
  _elements['node-138'] = node_138
  node_138.dataset.mirrorId = 'node-138'
  node_138.dataset.mirrorName = 'PersonaName'
  node_138.innerHTML = formatInlineMarkdown('Sara, *die zweifelnde Quereinsteigerin*')
  Object.assign(node_138.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_138.dataset.component = 'PersonaName'
  node_137.appendChild(node_138)

  node_135.appendChild(node_137)

  node_134.appendChild(node_135)

  node_133.appendChild(node_134)

  // Container
  const node_139 = document.createElement('div')
  _elements['node-139'] = node_139
  node_139.dataset.mirrorId = 'node-139'
  node_139.dataset.mirrorName = 'Container'
  Object.assign(node_139.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_139.dataset.component = 'Container'
  // PersonaSteckbrief
  const node_140 = document.createElement('span')
  _elements['node-140'] = node_140
  node_140.dataset.mirrorId = 'node-140'
  node_140.dataset.mirrorName = 'PersonaSteckbrief'
  node_140.innerHTML = formatInlineMarkdown(
    '22, vor vier Jahren KV-Lehre abgeschlossen (Versicherung), berufsbegleitend BM nachgeholt vor einem Jahr. Arbeitet 100 % als Sachbearbeiterin in einer Krankenversicherung. Hat im Job angefangen, mit Excel-VBA Auswertungen zu automatisieren — und gemerkt, dass ihr das mehr Spass macht, als sie erwartet hätte.'
  )
  Object.assign(node_140.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'max-width': '80 ch',
  })
  node_140.dataset.component = 'PersonaSteckbrief'
  node_139.appendChild(node_140)

  node_133.appendChild(node_139)

  node_132.appendChild(node_133)

  // Container
  const node_141 = document.createElement('div')
  _elements['node-141'] = node_141
  node_141.dataset.mirrorId = 'node-141'
  node_141.dataset.mirrorName = 'Container'
  Object.assign(node_141.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_141.dataset.component = 'Container'
  // PersonaBody
  const node_142 = document.createElement('div')
  _elements['node-142'] = node_142
  node_142.dataset.mirrorId = 'node-142'
  node_142.dataset.mirrorName = 'PersonaBody'
  Object.assign(node_142.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '64px',
  })
  node_142.dataset.component = 'PersonaBody'
  // WarumBlock
  const node_143 = document.createElement('div')
  _elements['node-143'] = node_143
  node_143.dataset.mirrorId = 'node-143'
  node_143.dataset.mirrorName = 'WarumBlock'
  Object.assign(node_143.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
    'max-width': '920px',
  })
  node_143.dataset.component = 'WarumBlock'
  // H4
  const node_144 = document.createElement('span')
  _elements['node-144'] = node_144
  node_144.dataset.mirrorId = 'node-144'
  node_144.dataset.mirrorName = 'H4'
  node_144.innerHTML = formatInlineMarkdown('Warum diese Persona?')
  Object.assign(node_144.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_144.dataset.component = 'H4'
  node_143.appendChild(node_144)

  // BodyTxt
  const node_145 = document.createElement('span')
  _elements['node-145'] = node_145
  node_145.dataset.mirrorId = 'node-145'
  node_145.dataset.mirrorName = 'BodyTxt'
  node_145.innerHTML = formatInlineMarkdown(
    'Sara vertritt **Quereinsteiger:innen aus nicht-technischen Lehrberufen** — ein Segment, das für die FH besonders relevant ist. Sie macht alle Fragen sichtbar, die mit **Selbstvertrauen, Zugehörigkeit und finanziellem Risiko** zusammenhängen — Themen, die in einer rein fachlichen Kommunikation systematisch unsichtbar bleiben.'
  )
  Object.assign(node_145.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_145.dataset.component = 'BodyTxt'
  node_143.appendChild(node_145)

  node_142.appendChild(node_143)

  // DimGrid
  const node_146 = document.createElement('div')
  _elements['node-146'] = node_146
  node_146.dataset.mirrorId = 'node-146'
  node_146.dataset.mirrorName = 'DimGrid'
  Object.assign(node_146.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(4, 1fr)',
    gap: '32px 64px',
  })
  node_146.dataset.layout = 'grid'
  node_146.dataset.component = 'DimGrid'
  // Dim
  const node_147 = document.createElement('div')
  _elements['node-147'] = node_147
  node_147.dataset.mirrorId = 'node-147'
  node_147.dataset.mirrorName = 'Dim'
  Object.assign(node_147.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_147.dataset.component = 'Dim'
  // H4
  const node_148 = document.createElement('span')
  _elements['node-148'] = node_148
  node_148.dataset.mirrorId = 'node-148'
  node_148.dataset.mirrorName = 'H4'
  node_148.innerHTML = formatInlineMarkdown('Situation')
  Object.assign(node_148.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_148.dataset.component = 'H4'
  node_147.appendChild(node_148)

  // BodyTxt
  const node_149 = document.createElement('span')
  _elements['node-149'] = node_149
  node_149.dataset.mirrorId = 'node-149'
  node_149.dataset.mirrorName = 'BodyTxt'
  node_149.innerHTML = formatInlineMarkdown(
    'Macht aktuell einen Online-Python-Kurs am Wochenende, kommt aber nur langsam voran. Eltern unterstützen die Idee Studium, fragen aber wiederholt: «Bist du dir sicher?» Sie hat noch nie eine Hochschule von innen gesehen.'
  )
  Object.assign(node_149.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_149.dataset.component = 'BodyTxt'
  node_147.appendChild(node_149)

  node_146.appendChild(node_147)

  // Dim
  const node_150 = document.createElement('div')
  _elements['node-150'] = node_150
  node_150.dataset.mirrorId = 'node-150'
  node_150.dataset.mirrorName = 'Dim'
  Object.assign(node_150.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_150.dataset.component = 'Dim'
  // H4
  const node_151 = document.createElement('span')
  _elements['node-151'] = node_151
  node_151.dataset.mirrorId = 'node-151'
  node_151.dataset.mirrorName = 'H4'
  node_151.innerHTML = formatInlineMarkdown('Ziel')
  Object.assign(node_151.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_151.dataset.component = 'H4'
  node_150.appendChild(node_151)

  // BodyTxt
  const node_152 = document.createElement('span')
  _elements['node-152'] = node_152
  node_152.dataset.mirrorId = 'node-152'
  node_152.dataset.mirrorName = 'BodyTxt'
  node_152.innerHTML = formatInlineMarkdown(
    'Den Sprung aus der Sachbearbeitung in eine Tech-nahe Rolle schaffen — ohne sich finanziell oder emotional zu überfordern.'
  )
  Object.assign(node_152.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_152.dataset.component = 'BodyTxt'
  node_150.appendChild(node_152)

  node_146.appendChild(node_150)

  // Dim
  const node_153 = document.createElement('div')
  _elements['node-153'] = node_153
  node_153.dataset.mirrorId = 'node-153'
  node_153.dataset.mirrorName = 'Dim'
  Object.assign(node_153.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_153.dataset.component = 'Dim'
  // H4
  const node_154 = document.createElement('span')
  _elements['node-154'] = node_154
  node_154.dataset.mirrorId = 'node-154'
  node_154.dataset.mirrorName = 'H4'
  node_154.innerHTML = formatInlineMarkdown('Herausforderung')
  Object.assign(node_154.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_154.dataset.component = 'H4'
  node_153.appendChild(node_154)

  // BodyTxtMuted
  const node_155 = document.createElement('span')
  _elements['node-155'] = node_155
  node_155.dataset.mirrorId = 'node-155'
  node_155.dataset.mirrorName = 'BodyTxtMuted'
  node_155.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_155.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_155.dataset.component = 'BodyTxtMuted'
  node_153.appendChild(node_155)

  node_146.appendChild(node_153)

  // Dim
  const node_156 = document.createElement('div')
  _elements['node-156'] = node_156
  node_156.dataset.mirrorId = 'node-156'
  node_156.dataset.mirrorName = 'Dim'
  Object.assign(node_156.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_156.dataset.component = 'Dim'
  // H4
  const node_157 = document.createElement('span')
  _elements['node-157'] = node_157
  node_157.dataset.mirrorId = 'node-157'
  node_157.dataset.mirrorName = 'H4'
  node_157.innerHTML = formatInlineMarkdown('Needs')
  Object.assign(node_157.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_157.dataset.component = 'H4'
  node_156.appendChild(node_157)

  // BodyTxtMuted
  const node_158 = document.createElement('span')
  _elements['node-158'] = node_158
  node_158.dataset.mirrorId = 'node-158'
  node_158.dataset.mirrorName = 'BodyTxtMuted'
  node_158.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_158.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_158.dataset.component = 'BodyTxtMuted'
  node_156.appendChild(node_158)

  node_146.appendChild(node_156)

  node_142.appendChild(node_146)

  // SoftBox
  const node_159 = document.createElement('div')
  _elements['node-159'] = node_159
  node_159.dataset.mirrorId = 'node-159'
  node_159.dataset.mirrorName = 'SoftBox'
  Object.assign(node_159.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--soft-bg)',
    padding: '22px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
  })
  node_159.dataset.component = 'SoftBox'
  // Unknown
  const node_160 = document.createElement('div')
  _elements['node-160'] = node_160
  node_160.dataset.mirrorId = 'node-160'
  node_160.dataset.mirrorName = 'Unknown'
  node_160.dataset.component = 'Unknown'
  node_159.appendChild(node_160)

  node_142.appendChild(node_159)

  // ImplikationenBlock
  const node_161 = document.createElement('div')
  _elements['node-161'] = node_161
  node_161.dataset.mirrorId = 'node-161'
  node_161.dataset.mirrorName = 'ImplikationenBlock'
  Object.assign(node_161.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '32px',
    'margin-top': '40px',
  })
  node_161.dataset.component = 'ImplikationenBlock'
  // H3
  const node_162 = document.createElement('span')
  _elements['node-162'] = node_162
  node_162.dataset.mirrorId = 'node-162'
  node_162.dataset.mirrorName = 'H3'
  node_162.innerHTML = formatInlineMarkdown('**Implikationen für FHNW-Kommunikation**')
  Object.assign(node_162.style, {
    'font-size': '28px',
    'font-weight': '700',
    'letter-spacing': '-0.015em',
    'line-height': '1.2',
  })
  node_162.dataset.component = 'H3'
  node_161.appendChild(node_162)

  // DimGridTwo
  const node_163 = document.createElement('div')
  _elements['node-163'] = node_163
  node_163.dataset.mirrorId = 'node-163'
  node_163.dataset.mirrorName = 'DimGridTwo'
  Object.assign(node_163.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(2, 1fr)',
    gap: '32px 64px',
  })
  node_163.dataset.layout = 'grid'
  node_163.dataset.component = 'DimGridTwo'
  // Dim
  const node_164 = document.createElement('div')
  _elements['node-164'] = node_164
  node_164.dataset.mirrorId = 'node-164'
  node_164.dataset.mirrorName = 'Dim'
  Object.assign(node_164.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_164.dataset.component = 'Dim'
  // H4
  const node_165 = document.createElement('span')
  _elements['node-165'] = node_165
  node_165.dataset.mirrorId = 'node-165'
  node_165.dataset.mirrorName = 'H4'
  node_165.innerHTML = formatInlineMarkdown('Webseite')
  Object.assign(node_165.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_165.dataset.component = 'H4'
  node_164.appendChild(node_165)

  // BodyTxtMuted
  const node_166 = document.createElement('span')
  _elements['node-166'] = node_166
  node_166.dataset.mirrorId = 'node-166'
  node_166.dataset.mirrorName = 'BodyTxtMuted'
  node_166.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_166.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_166.dataset.component = 'BodyTxtMuted'
  node_164.appendChild(node_166)

  node_163.appendChild(node_164)

  // Dim
  const node_167 = document.createElement('div')
  _elements['node-167'] = node_167
  node_167.dataset.mirrorId = 'node-167'
  node_167.dataset.mirrorName = 'Dim'
  Object.assign(node_167.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_167.dataset.component = 'Dim'
  // H4
  const node_168 = document.createElement('span')
  _elements['node-168'] = node_168
  node_168.dataset.mirrorId = 'node-168'
  node_168.dataset.mirrorName = 'H4'
  node_168.innerHTML = formatInlineMarkdown('Infoveranstaltung')
  Object.assign(node_168.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_168.dataset.component = 'H4'
  node_167.appendChild(node_168)

  // BodyTxtMuted
  const node_169 = document.createElement('span')
  _elements['node-169'] = node_169
  node_169.dataset.mirrorId = 'node-169'
  node_169.dataset.mirrorName = 'BodyTxtMuted'
  node_169.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_169.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_169.dataset.component = 'BodyTxtMuted'
  node_167.appendChild(node_169)

  node_163.appendChild(node_167)

  node_161.appendChild(node_163)

  node_142.appendChild(node_161)

  node_141.appendChild(node_142)

  node_132.appendChild(node_141)

  _root.appendChild(node_132)

  // PersonaBlock
  const node_170 = document.createElement('div')
  _elements['node-170'] = node_170
  node_170.dataset.mirrorId = 'node-170'
  node_170.dataset.mirrorName = 'PersonaBlock'
  Object.assign(node_170.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    padding: '0px 24px 80px 24px',
  })
  node_170.dataset.component = 'PersonaBlock'
  // PersonaHeaderBand
  const node_171 = document.createElement('div')
  _elements['node-171'] = node_171
  node_171.dataset.mirrorId = 'node-171'
  node_171.dataset.mirrorName = 'PersonaHeaderBand'
  Object.assign(node_171.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--yellow-bg)',
    padding: '72px 24px 64px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    margin: '0px 0px 80px 0px',
  })
  node_171.dataset.component = 'PersonaHeaderBand'
  // Container
  const node_172 = document.createElement('div')
  _elements['node-172'] = node_172
  node_172.dataset.mirrorId = 'node-172'
  node_172.dataset.mirrorName = 'Container'
  Object.assign(node_172.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_172.dataset.component = 'Container'
  // PersonaHeaderRow
  const node_173 = document.createElement('div')
  _elements['node-173'] = node_173
  node_173.dataset.mirrorId = 'node-173'
  node_173.dataset.mirrorName = 'PersonaHeaderRow'
  Object.assign(node_173.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-end',
    gap: '56px',
  })
  node_173.dataset.layout = 'flex'
  node_173.dataset.component = 'PersonaHeaderRow'
  // PersonaNumeral
  const node_174 = document.createElement('span')
  _elements['node-174'] = node_174
  node_174.dataset.mirrorId = 'node-174'
  node_174.dataset.mirrorName = 'PersonaNumeral'
  node_174.innerHTML = formatInlineMarkdown('03')
  Object.assign(node_174.style, {
    'font-size': '200px',
    'font-weight': '700',
    'letter-spacing': '-0.04em',
    'line-height': '0.82',
  })
  node_174.dataset.component = 'PersonaNumeral'
  node_173.appendChild(node_174)

  // PersonaTitleStack
  const node_175 = document.createElement('div')
  _elements['node-175'] = node_175
  node_175.dataset.mirrorId = 'node-175'
  node_175.dataset.mirrorName = 'PersonaTitleStack'
  Object.assign(node_175.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '6px',
    'flex-grow': '1',
  })
  node_175.dataset.component = 'PersonaTitleStack'
  // PersonaName
  const node_176 = document.createElement('span')
  _elements['node-176'] = node_176
  node_176.dataset.mirrorId = 'node-176'
  node_176.dataset.mirrorName = 'PersonaName'
  node_176.innerHTML = formatInlineMarkdown('Marco, *der pragmatische EFZ-Absolvent*')
  Object.assign(node_176.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_176.dataset.component = 'PersonaName'
  node_175.appendChild(node_176)

  node_173.appendChild(node_175)

  node_172.appendChild(node_173)

  node_171.appendChild(node_172)

  // Container
  const node_177 = document.createElement('div')
  _elements['node-177'] = node_177
  node_177.dataset.mirrorId = 'node-177'
  node_177.dataset.mirrorName = 'Container'
  Object.assign(node_177.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_177.dataset.component = 'Container'
  // PersonaSteckbrief
  const node_178 = document.createElement('span')
  _elements['node-178'] = node_178
  node_178.dataset.mirrorId = 'node-178'
  node_178.dataset.mirrorName = 'PersonaSteckbrief'
  node_178.innerHTML = formatInlineMarkdown(
    '23, Informatiker EFZ Plattformentwicklung, nachher zwei Jahre als Systemtechniker bei einem KMU in Olten. Berufsmaturität während der Lehre. Verdient aktuell ca. 78 000 CHF/Jahr, lebt mit Partnerin in einer 3.5-Zimmer-Wohnung. Rechnet jeden grösseren Schritt durch.'
  )
  Object.assign(node_178.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'max-width': '80 ch',
  })
  node_178.dataset.component = 'PersonaSteckbrief'
  node_177.appendChild(node_178)

  node_171.appendChild(node_177)

  node_170.appendChild(node_171)

  // Container
  const node_179 = document.createElement('div')
  _elements['node-179'] = node_179
  node_179.dataset.mirrorId = 'node-179'
  node_179.dataset.mirrorName = 'Container'
  Object.assign(node_179.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_179.dataset.component = 'Container'
  // PersonaBody
  const node_180 = document.createElement('div')
  _elements['node-180'] = node_180
  node_180.dataset.mirrorId = 'node-180'
  node_180.dataset.mirrorName = 'PersonaBody'
  Object.assign(node_180.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '64px',
  })
  node_180.dataset.component = 'PersonaBody'
  // WarumBlock
  const node_181 = document.createElement('div')
  _elements['node-181'] = node_181
  node_181.dataset.mirrorId = 'node-181'
  node_181.dataset.mirrorName = 'WarumBlock'
  Object.assign(node_181.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
    'max-width': '920px',
  })
  node_181.dataset.component = 'WarumBlock'
  // H4
  const node_182 = document.createElement('span')
  _elements['node-182'] = node_182
  node_182.dataset.mirrorId = 'node-182'
  node_182.dataset.mirrorName = 'H4'
  node_182.innerHTML = formatInlineMarkdown('Warum diese Persona?')
  Object.assign(node_182.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_182.dataset.component = 'H4'
  node_181.appendChild(node_182)

  // BodyTxt
  const node_183 = document.createElement('span')
  _elements['node-183'] = node_183
  node_183.dataset.mirrorId = 'node-183'
  node_183.dataset.mirrorName = 'BodyTxt'
  node_183.innerHTML = formatInlineMarkdown(
    'Marco verkörpert das **historische Kernsegment der Fachhochschule** — Berufsleute mit EFZ und Praxiserfahrung. Marco zwingt uns zu **harten Daten** statt Marketing-Floskeln, weil er jeden Schritt rechnet und mit Alternativen vergleicht.'
  )
  Object.assign(node_183.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_183.dataset.component = 'BodyTxt'
  node_181.appendChild(node_183)

  node_180.appendChild(node_181)

  // DimGrid
  const node_184 = document.createElement('div')
  _elements['node-184'] = node_184
  node_184.dataset.mirrorId = 'node-184'
  node_184.dataset.mirrorName = 'DimGrid'
  Object.assign(node_184.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(4, 1fr)',
    gap: '32px 64px',
  })
  node_184.dataset.layout = 'grid'
  node_184.dataset.component = 'DimGrid'
  // Dim
  const node_185 = document.createElement('div')
  _elements['node-185'] = node_185
  node_185.dataset.mirrorId = 'node-185'
  node_185.dataset.mirrorName = 'Dim'
  Object.assign(node_185.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_185.dataset.component = 'Dim'
  // H4
  const node_186 = document.createElement('span')
  _elements['node-186'] = node_186
  node_186.dataset.mirrorId = 'node-186'
  node_186.dataset.mirrorName = 'H4'
  node_186.innerHTML = formatInlineMarkdown('Situation')
  Object.assign(node_186.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_186.dataset.component = 'H4'
  node_185.appendChild(node_186)

  // BodyTxt
  const node_187 = document.createElement('span')
  _elements['node-187'] = node_187
  node_187.dataset.mirrorId = 'node-187'
  node_187.dataset.mirrorName = 'BodyTxt'
  node_187.innerHTML = formatInlineMarkdown(
    'Spürt, dass er an seiner aktuellen Stelle nicht mehr weiterkommt. Will mehr Architektur, mehr Verantwortung, weniger Routine-Tickets. Steht vor einer rein ökonomisch-strategischen Frage, mit emotionaler Komponente.'
  )
  Object.assign(node_187.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_187.dataset.component = 'BodyTxt'
  node_185.appendChild(node_187)

  node_184.appendChild(node_185)

  // Dim
  const node_188 = document.createElement('div')
  _elements['node-188'] = node_188
  node_188.dataset.mirrorId = 'node-188'
  node_188.dataset.mirrorName = 'Dim'
  Object.assign(node_188.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_188.dataset.component = 'Dim'
  // H4
  const node_189 = document.createElement('span')
  _elements['node-189'] = node_189
  node_189.dataset.mirrorId = 'node-189'
  node_189.dataset.mirrorName = 'H4'
  node_189.innerHTML = formatInlineMarkdown('Ziel')
  Object.assign(node_189.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_189.dataset.component = 'H4'
  node_188.appendChild(node_189)

  // BodyTxt
  const node_190 = document.createElement('span')
  _elements['node-190'] = node_190
  node_190.dataset.mirrorId = 'node-190'
  node_190.dataset.mirrorName = 'BodyTxt'
  node_190.innerHTML = formatInlineMarkdown(
    'Klarer Karrieresprung mit kalkulierbarem Risiko. Mittelfristig in eine Rolle mit Entscheidungsspielraum (Tech Lead, Architect, Engineering Manager).'
  )
  Object.assign(node_190.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_190.dataset.component = 'BodyTxt'
  node_188.appendChild(node_190)

  node_184.appendChild(node_188)

  // Dim
  const node_191 = document.createElement('div')
  _elements['node-191'] = node_191
  node_191.dataset.mirrorId = 'node-191'
  node_191.dataset.mirrorName = 'Dim'
  Object.assign(node_191.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_191.dataset.component = 'Dim'
  // H4
  const node_192 = document.createElement('span')
  _elements['node-192'] = node_192
  node_192.dataset.mirrorId = 'node-192'
  node_192.dataset.mirrorName = 'H4'
  node_192.innerHTML = formatInlineMarkdown('Herausforderung')
  Object.assign(node_192.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_192.dataset.component = 'H4'
  node_191.appendChild(node_192)

  // BodyTxtMuted
  const node_193 = document.createElement('span')
  _elements['node-193'] = node_193
  node_193.dataset.mirrorId = 'node-193'
  node_193.dataset.mirrorName = 'BodyTxtMuted'
  node_193.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_193.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_193.dataset.component = 'BodyTxtMuted'
  node_191.appendChild(node_193)

  node_184.appendChild(node_191)

  // Dim
  const node_194 = document.createElement('div')
  _elements['node-194'] = node_194
  node_194.dataset.mirrorId = 'node-194'
  node_194.dataset.mirrorName = 'Dim'
  Object.assign(node_194.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_194.dataset.component = 'Dim'
  // H4
  const node_195 = document.createElement('span')
  _elements['node-195'] = node_195
  node_195.dataset.mirrorId = 'node-195'
  node_195.dataset.mirrorName = 'H4'
  node_195.innerHTML = formatInlineMarkdown('Needs')
  Object.assign(node_195.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_195.dataset.component = 'H4'
  node_194.appendChild(node_195)

  // BodyTxtMuted
  const node_196 = document.createElement('span')
  _elements['node-196'] = node_196
  node_196.dataset.mirrorId = 'node-196'
  node_196.dataset.mirrorName = 'BodyTxtMuted'
  node_196.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_196.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_196.dataset.component = 'BodyTxtMuted'
  node_194.appendChild(node_196)

  node_184.appendChild(node_194)

  node_180.appendChild(node_184)

  // SoftBox
  const node_197 = document.createElement('div')
  _elements['node-197'] = node_197
  node_197.dataset.mirrorId = 'node-197'
  node_197.dataset.mirrorName = 'SoftBox'
  Object.assign(node_197.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--soft-bg)',
    padding: '22px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
  })
  node_197.dataset.component = 'SoftBox'
  // Unknown
  const node_198 = document.createElement('div')
  _elements['node-198'] = node_198
  node_198.dataset.mirrorId = 'node-198'
  node_198.dataset.mirrorName = 'Unknown'
  node_198.dataset.component = 'Unknown'
  node_197.appendChild(node_198)

  node_180.appendChild(node_197)

  // ImplikationenBlock
  const node_199 = document.createElement('div')
  _elements['node-199'] = node_199
  node_199.dataset.mirrorId = 'node-199'
  node_199.dataset.mirrorName = 'ImplikationenBlock'
  Object.assign(node_199.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '32px',
    'margin-top': '40px',
  })
  node_199.dataset.component = 'ImplikationenBlock'
  // H3
  const node_200 = document.createElement('span')
  _elements['node-200'] = node_200
  node_200.dataset.mirrorId = 'node-200'
  node_200.dataset.mirrorName = 'H3'
  node_200.innerHTML = formatInlineMarkdown('**Implikationen für FHNW-Kommunikation**')
  Object.assign(node_200.style, {
    'font-size': '28px',
    'font-weight': '700',
    'letter-spacing': '-0.015em',
    'line-height': '1.2',
  })
  node_200.dataset.component = 'H3'
  node_199.appendChild(node_200)

  // DimGridTwo
  const node_201 = document.createElement('div')
  _elements['node-201'] = node_201
  node_201.dataset.mirrorId = 'node-201'
  node_201.dataset.mirrorName = 'DimGridTwo'
  Object.assign(node_201.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(2, 1fr)',
    gap: '32px 64px',
  })
  node_201.dataset.layout = 'grid'
  node_201.dataset.component = 'DimGridTwo'
  // Dim
  const node_202 = document.createElement('div')
  _elements['node-202'] = node_202
  node_202.dataset.mirrorId = 'node-202'
  node_202.dataset.mirrorName = 'Dim'
  Object.assign(node_202.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_202.dataset.component = 'Dim'
  // H4
  const node_203 = document.createElement('span')
  _elements['node-203'] = node_203
  node_203.dataset.mirrorId = 'node-203'
  node_203.dataset.mirrorName = 'H4'
  node_203.innerHTML = formatInlineMarkdown('Webseite')
  Object.assign(node_203.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_203.dataset.component = 'H4'
  node_202.appendChild(node_203)

  // BodyTxtMuted
  const node_204 = document.createElement('span')
  _elements['node-204'] = node_204
  node_204.dataset.mirrorId = 'node-204'
  node_204.dataset.mirrorName = 'BodyTxtMuted'
  node_204.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_204.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_204.dataset.component = 'BodyTxtMuted'
  node_202.appendChild(node_204)

  node_201.appendChild(node_202)

  // Dim
  const node_205 = document.createElement('div')
  _elements['node-205'] = node_205
  node_205.dataset.mirrorId = 'node-205'
  node_205.dataset.mirrorName = 'Dim'
  Object.assign(node_205.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_205.dataset.component = 'Dim'
  // H4
  const node_206 = document.createElement('span')
  _elements['node-206'] = node_206
  node_206.dataset.mirrorId = 'node-206'
  node_206.dataset.mirrorName = 'H4'
  node_206.innerHTML = formatInlineMarkdown('Infoveranstaltung')
  Object.assign(node_206.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_206.dataset.component = 'H4'
  node_205.appendChild(node_206)

  // BodyTxtMuted
  const node_207 = document.createElement('span')
  _elements['node-207'] = node_207
  node_207.dataset.mirrorId = 'node-207'
  node_207.dataset.mirrorName = 'BodyTxtMuted'
  node_207.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_207.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_207.dataset.component = 'BodyTxtMuted'
  node_205.appendChild(node_207)

  node_201.appendChild(node_205)

  node_199.appendChild(node_201)

  node_180.appendChild(node_199)

  node_179.appendChild(node_180)

  node_170.appendChild(node_179)

  _root.appendChild(node_170)

  // PersonaBlock
  const node_208 = document.createElement('div')
  _elements['node-208'] = node_208
  node_208.dataset.mirrorId = 'node-208'
  node_208.dataset.mirrorName = 'PersonaBlock'
  Object.assign(node_208.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    padding: '0px 24px 80px 24px',
  })
  node_208.dataset.component = 'PersonaBlock'
  // PersonaHeaderBand
  const node_209 = document.createElement('div')
  _elements['node-209'] = node_209
  node_209.dataset.mirrorId = 'node-209'
  node_209.dataset.mirrorName = 'PersonaHeaderBand'
  Object.assign(node_209.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--yellow-bg)',
    padding: '72px 24px 64px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    margin: '0px 0px 80px 0px',
  })
  node_209.dataset.component = 'PersonaHeaderBand'
  // Container
  const node_210 = document.createElement('div')
  _elements['node-210'] = node_210
  node_210.dataset.mirrorId = 'node-210'
  node_210.dataset.mirrorName = 'Container'
  Object.assign(node_210.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_210.dataset.component = 'Container'
  // PersonaHeaderRow
  const node_211 = document.createElement('div')
  _elements['node-211'] = node_211
  node_211.dataset.mirrorId = 'node-211'
  node_211.dataset.mirrorName = 'PersonaHeaderRow'
  Object.assign(node_211.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-end',
    gap: '56px',
  })
  node_211.dataset.layout = 'flex'
  node_211.dataset.component = 'PersonaHeaderRow'
  // PersonaNumeral
  const node_212 = document.createElement('span')
  _elements['node-212'] = node_212
  node_212.dataset.mirrorId = 'node-212'
  node_212.dataset.mirrorName = 'PersonaNumeral'
  node_212.innerHTML = formatInlineMarkdown('04')
  Object.assign(node_212.style, {
    'font-size': '200px',
    'font-weight': '700',
    'letter-spacing': '-0.04em',
    'line-height': '0.82',
  })
  node_212.dataset.component = 'PersonaNumeral'
  node_211.appendChild(node_212)

  // PersonaTitleStack
  const node_213 = document.createElement('div')
  _elements['node-213'] = node_213
  node_213.dataset.mirrorId = 'node-213'
  node_213.dataset.mirrorName = 'PersonaTitleStack'
  Object.assign(node_213.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '6px',
    'flex-grow': '1',
  })
  node_213.dataset.component = 'PersonaTitleStack'
  // PersonaName
  const node_214 = document.createElement('span')
  _elements['node-214'] = node_214
  node_214.dataset.mirrorId = 'node-214'
  node_214.dataset.mirrorName = 'PersonaName'
  node_214.innerHTML = formatInlineMarkdown('Nadia, *die orientierungslose Maturandin*')
  Object.assign(node_214.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_214.dataset.component = 'PersonaName'
  node_213.appendChild(node_214)

  node_211.appendChild(node_213)

  node_210.appendChild(node_211)

  node_209.appendChild(node_210)

  // Container
  const node_215 = document.createElement('div')
  _elements['node-215'] = node_215
  node_215.dataset.mirrorId = 'node-215'
  node_215.dataset.mirrorName = 'Container'
  Object.assign(node_215.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_215.dataset.component = 'Container'
  // PersonaSteckbrief
  const node_216 = document.createElement('span')
  _elements['node-216'] = node_216
  node_216.dataset.mirrorId = 'node-216'
  node_216.dataset.mirrorName = 'PersonaSteckbrief'
  node_216.innerHTML = formatInlineMarkdown(
    '18, Gymi (Schwerpunkt Wirtschaft & Recht), Notenschnitt 4.8. Hat Informatik-Anwendungen als Ergänzungsfach belegt — eher zufällig. Liest viel, interessiert sich für Politik und Klima. Keine Programmiererfahrung ausser etwas HTML in der Schule.'
  )
  Object.assign(node_216.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'max-width': '80 ch',
  })
  node_216.dataset.component = 'PersonaSteckbrief'
  node_215.appendChild(node_216)

  node_209.appendChild(node_215)

  node_208.appendChild(node_209)

  // Container
  const node_217 = document.createElement('div')
  _elements['node-217'] = node_217
  node_217.dataset.mirrorId = 'node-217'
  node_217.dataset.mirrorName = 'Container'
  Object.assign(node_217.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_217.dataset.component = 'Container'
  // PersonaBody
  const node_218 = document.createElement('div')
  _elements['node-218'] = node_218
  node_218.dataset.mirrorId = 'node-218'
  node_218.dataset.mirrorName = 'PersonaBody'
  Object.assign(node_218.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '64px',
  })
  node_218.dataset.component = 'PersonaBody'
  // WarumBlock
  const node_219 = document.createElement('div')
  _elements['node-219'] = node_219
  node_219.dataset.mirrorId = 'node-219'
  node_219.dataset.mirrorName = 'WarumBlock'
  Object.assign(node_219.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
    'max-width': '920px',
  })
  node_219.dataset.component = 'WarumBlock'
  // H4
  const node_220 = document.createElement('span')
  _elements['node-220'] = node_220
  node_220.dataset.mirrorId = 'node-220'
  node_220.dataset.mirrorName = 'H4'
  node_220.innerHTML = formatInlineMarkdown('Warum diese Persona?')
  Object.assign(node_220.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_220.dataset.component = 'H4'
  node_219.appendChild(node_220)

  // BodyTxt
  const node_221 = document.createElement('span')
  _elements['node-221'] = node_221
  node_221.dataset.mirrorId = 'node-221'
  node_221.dataset.mirrorName = 'BodyTxt'
  node_221.innerHTML = formatInlineMarkdown(
    'Nadia vertritt ein **Wachstumssegment**: Maturand:innen ohne Tech-Background. Dieses Segment ist entscheidend für den **Frauenanteil** und für das mittelfristige Wachstum. Sie zwingt uns, **Eltern und Klischee-Wirkungen** ernst zu nehmen.'
  )
  Object.assign(node_221.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_221.dataset.component = 'BodyTxt'
  node_219.appendChild(node_221)

  node_218.appendChild(node_219)

  // DimGrid
  const node_222 = document.createElement('div')
  _elements['node-222'] = node_222
  node_222.dataset.mirrorId = 'node-222'
  node_222.dataset.mirrorName = 'DimGrid'
  Object.assign(node_222.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(4, 1fr)',
    gap: '32px 64px',
  })
  node_222.dataset.layout = 'grid'
  node_222.dataset.component = 'DimGrid'
  // Dim
  const node_223 = document.createElement('div')
  _elements['node-223'] = node_223
  node_223.dataset.mirrorId = 'node-223'
  node_223.dataset.mirrorName = 'Dim'
  Object.assign(node_223.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_223.dataset.component = 'Dim'
  // H4
  const node_224 = document.createElement('span')
  _elements['node-224'] = node_224
  node_224.dataset.mirrorId = 'node-224'
  node_224.dataset.mirrorName = 'H4'
  node_224.innerHTML = formatInlineMarkdown('Situation')
  Object.assign(node_224.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_224.dataset.component = 'H4'
  node_223.appendChild(node_224)

  // BodyTxt
  const node_225 = document.createElement('span')
  _elements['node-225'] = node_225
  node_225.dataset.mirrorId = 'node-225'
  node_225.dataset.mirrorName = 'BodyTxt'
  node_225.innerHTML = formatInlineMarkdown(
    'Sechs Monate vor der Matura, weiss noch nicht, was sie studieren soll. Hat bei einer Berufsmesse am FHNW-Stand gestoppt, weil «Data Science» cool klang. Eltern wollen «etwas Solides»: Mutter würde Medizin bevorzugen, Vater BWL.'
  )
  Object.assign(node_225.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_225.dataset.component = 'BodyTxt'
  node_223.appendChild(node_225)

  node_222.appendChild(node_223)

  // Dim
  const node_226 = document.createElement('div')
  _elements['node-226'] = node_226
  node_226.dataset.mirrorId = 'node-226'
  node_226.dataset.mirrorName = 'Dim'
  Object.assign(node_226.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_226.dataset.component = 'Dim'
  // H4
  const node_227 = document.createElement('span')
  _elements['node-227'] = node_227
  node_227.dataset.mirrorId = 'node-227'
  node_227.dataset.mirrorName = 'H4'
  node_227.innerHTML = formatInlineMarkdown('Ziel')
  Object.assign(node_227.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_227.dataset.component = 'H4'
  node_226.appendChild(node_227)

  // BodyTxt
  const node_228 = document.createElement('span')
  _elements['node-228'] = node_228
  node_228.dataset.mirrorId = 'node-228'
  node_228.dataset.mirrorName = 'BodyTxt'
  node_228.innerHTML = formatInlineMarkdown(
    'Eine begründete Studienwahl treffen, die zu ihr passt. Ein Beruf, der sie auch in zehn Jahren noch interessiert und Wirkung hat.'
  )
  Object.assign(node_228.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_228.dataset.component = 'BodyTxt'
  node_226.appendChild(node_228)

  node_222.appendChild(node_226)

  // Dim
  const node_229 = document.createElement('div')
  _elements['node-229'] = node_229
  node_229.dataset.mirrorId = 'node-229'
  node_229.dataset.mirrorName = 'Dim'
  Object.assign(node_229.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_229.dataset.component = 'Dim'
  // H4
  const node_230 = document.createElement('span')
  _elements['node-230'] = node_230
  node_230.dataset.mirrorId = 'node-230'
  node_230.dataset.mirrorName = 'H4'
  node_230.innerHTML = formatInlineMarkdown('Herausforderung')
  Object.assign(node_230.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_230.dataset.component = 'H4'
  node_229.appendChild(node_230)

  // BodyTxtMuted
  const node_231 = document.createElement('span')
  _elements['node-231'] = node_231
  node_231.dataset.mirrorId = 'node-231'
  node_231.dataset.mirrorName = 'BodyTxtMuted'
  node_231.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_231.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_231.dataset.component = 'BodyTxtMuted'
  node_229.appendChild(node_231)

  node_222.appendChild(node_229)

  // Dim
  const node_232 = document.createElement('div')
  _elements['node-232'] = node_232
  node_232.dataset.mirrorId = 'node-232'
  node_232.dataset.mirrorName = 'Dim'
  Object.assign(node_232.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_232.dataset.component = 'Dim'
  // H4
  const node_233 = document.createElement('span')
  _elements['node-233'] = node_233
  node_233.dataset.mirrorId = 'node-233'
  node_233.dataset.mirrorName = 'H4'
  node_233.innerHTML = formatInlineMarkdown('Needs')
  Object.assign(node_233.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_233.dataset.component = 'H4'
  node_232.appendChild(node_233)

  // BodyTxtMuted
  const node_234 = document.createElement('span')
  _elements['node-234'] = node_234
  node_234.dataset.mirrorId = 'node-234'
  node_234.dataset.mirrorName = 'BodyTxtMuted'
  node_234.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_234.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_234.dataset.component = 'BodyTxtMuted'
  node_232.appendChild(node_234)

  node_222.appendChild(node_232)

  node_218.appendChild(node_222)

  // SoftBox
  const node_235 = document.createElement('div')
  _elements['node-235'] = node_235
  node_235.dataset.mirrorId = 'node-235'
  node_235.dataset.mirrorName = 'SoftBox'
  Object.assign(node_235.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--soft-bg)',
    padding: '22px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
  })
  node_235.dataset.component = 'SoftBox'
  // Unknown
  const node_236 = document.createElement('div')
  _elements['node-236'] = node_236
  node_236.dataset.mirrorId = 'node-236'
  node_236.dataset.mirrorName = 'Unknown'
  node_236.dataset.component = 'Unknown'
  node_235.appendChild(node_236)

  node_218.appendChild(node_235)

  // ImplikationenBlock
  const node_237 = document.createElement('div')
  _elements['node-237'] = node_237
  node_237.dataset.mirrorId = 'node-237'
  node_237.dataset.mirrorName = 'ImplikationenBlock'
  Object.assign(node_237.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '32px',
    'margin-top': '40px',
  })
  node_237.dataset.component = 'ImplikationenBlock'
  // H3
  const node_238 = document.createElement('span')
  _elements['node-238'] = node_238
  node_238.dataset.mirrorId = 'node-238'
  node_238.dataset.mirrorName = 'H3'
  node_238.innerHTML = formatInlineMarkdown('**Implikationen für FHNW-Kommunikation**')
  Object.assign(node_238.style, {
    'font-size': '28px',
    'font-weight': '700',
    'letter-spacing': '-0.015em',
    'line-height': '1.2',
  })
  node_238.dataset.component = 'H3'
  node_237.appendChild(node_238)

  // DimGridTwo
  const node_239 = document.createElement('div')
  _elements['node-239'] = node_239
  node_239.dataset.mirrorId = 'node-239'
  node_239.dataset.mirrorName = 'DimGridTwo'
  Object.assign(node_239.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(2, 1fr)',
    gap: '32px 64px',
  })
  node_239.dataset.layout = 'grid'
  node_239.dataset.component = 'DimGridTwo'
  // Dim
  const node_240 = document.createElement('div')
  _elements['node-240'] = node_240
  node_240.dataset.mirrorId = 'node-240'
  node_240.dataset.mirrorName = 'Dim'
  Object.assign(node_240.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_240.dataset.component = 'Dim'
  // H4
  const node_241 = document.createElement('span')
  _elements['node-241'] = node_241
  node_241.dataset.mirrorId = 'node-241'
  node_241.dataset.mirrorName = 'H4'
  node_241.innerHTML = formatInlineMarkdown('Webseite')
  Object.assign(node_241.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_241.dataset.component = 'H4'
  node_240.appendChild(node_241)

  // BodyTxtMuted
  const node_242 = document.createElement('span')
  _elements['node-242'] = node_242
  node_242.dataset.mirrorId = 'node-242'
  node_242.dataset.mirrorName = 'BodyTxtMuted'
  node_242.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_242.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_242.dataset.component = 'BodyTxtMuted'
  node_240.appendChild(node_242)

  node_239.appendChild(node_240)

  // Dim
  const node_243 = document.createElement('div')
  _elements['node-243'] = node_243
  node_243.dataset.mirrorId = 'node-243'
  node_243.dataset.mirrorName = 'Dim'
  Object.assign(node_243.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_243.dataset.component = 'Dim'
  // H4
  const node_244 = document.createElement('span')
  _elements['node-244'] = node_244
  node_244.dataset.mirrorId = 'node-244'
  node_244.dataset.mirrorName = 'H4'
  node_244.innerHTML = formatInlineMarkdown('Infoveranstaltung')
  Object.assign(node_244.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_244.dataset.component = 'H4'
  node_243.appendChild(node_244)

  // BodyTxtMuted
  const node_245 = document.createElement('span')
  _elements['node-245'] = node_245
  node_245.dataset.mirrorId = 'node-245'
  node_245.dataset.mirrorName = 'BodyTxtMuted'
  node_245.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_245.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_245.dataset.component = 'BodyTxtMuted'
  node_243.appendChild(node_245)

  node_239.appendChild(node_243)

  node_237.appendChild(node_239)

  node_218.appendChild(node_237)

  node_217.appendChild(node_218)

  node_208.appendChild(node_217)

  _root.appendChild(node_208)

  // PersonaBlock
  const node_246 = document.createElement('div')
  _elements['node-246'] = node_246
  node_246.dataset.mirrorId = 'node-246'
  node_246.dataset.mirrorName = 'PersonaBlock'
  Object.assign(node_246.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    padding: '0px 24px 80px 24px',
  })
  node_246.dataset.component = 'PersonaBlock'
  // PersonaHeaderBand
  const node_247 = document.createElement('div')
  _elements['node-247'] = node_247
  node_247.dataset.mirrorId = 'node-247'
  node_247.dataset.mirrorName = 'PersonaHeaderBand'
  Object.assign(node_247.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--yellow-bg)',
    padding: '72px 24px 64px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    margin: '0px 0px 80px 0px',
  })
  node_247.dataset.component = 'PersonaHeaderBand'
  // Container
  const node_248 = document.createElement('div')
  _elements['node-248'] = node_248
  node_248.dataset.mirrorId = 'node-248'
  node_248.dataset.mirrorName = 'Container'
  Object.assign(node_248.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_248.dataset.component = 'Container'
  // PersonaHeaderRow
  const node_249 = document.createElement('div')
  _elements['node-249'] = node_249
  node_249.dataset.mirrorId = 'node-249'
  node_249.dataset.mirrorName = 'PersonaHeaderRow'
  Object.assign(node_249.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-end',
    gap: '56px',
  })
  node_249.dataset.layout = 'flex'
  node_249.dataset.component = 'PersonaHeaderRow'
  // PersonaNumeral
  const node_250 = document.createElement('span')
  _elements['node-250'] = node_250
  node_250.dataset.mirrorId = 'node-250'
  node_250.dataset.mirrorName = 'PersonaNumeral'
  node_250.innerHTML = formatInlineMarkdown('05')
  Object.assign(node_250.style, {
    'font-size': '200px',
    'font-weight': '700',
    'letter-spacing': '-0.04em',
    'line-height': '0.82',
  })
  node_250.dataset.component = 'PersonaNumeral'
  node_249.appendChild(node_250)

  // PersonaTitleStack
  const node_251 = document.createElement('div')
  _elements['node-251'] = node_251
  node_251.dataset.mirrorId = 'node-251'
  node_251.dataset.mirrorName = 'PersonaTitleStack'
  Object.assign(node_251.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '6px',
    'flex-grow': '1',
  })
  node_251.dataset.component = 'PersonaTitleStack'
  // PersonaName
  const node_252 = document.createElement('span')
  _elements['node-252'] = node_252
  node_252.dataset.mirrorId = 'node-252'
  node_252.dataset.mirrorName = 'PersonaName'
  node_252.innerHTML = formatInlineMarkdown('Tim, *der Game-begeisterte Spezialist*')
  Object.assign(node_252.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_252.dataset.component = 'PersonaName'
  node_251.appendChild(node_252)

  node_249.appendChild(node_251)

  node_248.appendChild(node_249)

  node_247.appendChild(node_248)

  // Container
  const node_253 = document.createElement('div')
  _elements['node-253'] = node_253
  node_253.dataset.mirrorId = 'node-253'
  node_253.dataset.mirrorName = 'Container'
  Object.assign(node_253.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_253.dataset.component = 'Container'
  // PersonaSteckbrief
  const node_254 = document.createElement('span')
  _elements['node-254'] = node_254
  node_254.dataset.mirrorId = 'node-254'
  node_254.dataset.mirrorName = 'PersonaSteckbrief'
  node_254.innerHTML = formatInlineMarkdown(
    '17, Informatikmittelschule (IMS) in Bern, im letzten Lehrjahr. Macht in der Freizeit Unity-Tutorials, hat zwei kleine Prototypen gebaut, ist auf Discord in Game-Dev-Communities aktiv, war bereits an zwei Game Jams.'
  )
  Object.assign(node_254.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'max-width': '80 ch',
  })
  node_254.dataset.component = 'PersonaSteckbrief'
  node_253.appendChild(node_254)

  node_247.appendChild(node_253)

  node_246.appendChild(node_247)

  // Container
  const node_255 = document.createElement('div')
  _elements['node-255'] = node_255
  node_255.dataset.mirrorId = 'node-255'
  node_255.dataset.mirrorName = 'Container'
  Object.assign(node_255.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_255.dataset.component = 'Container'
  // PersonaBody
  const node_256 = document.createElement('div')
  _elements['node-256'] = node_256
  node_256.dataset.mirrorId = 'node-256'
  node_256.dataset.mirrorName = 'PersonaBody'
  Object.assign(node_256.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '64px',
  })
  node_256.dataset.component = 'PersonaBody'
  // WarumBlock
  const node_257 = document.createElement('div')
  _elements['node-257'] = node_257
  node_257.dataset.mirrorId = 'node-257'
  node_257.dataset.mirrorName = 'WarumBlock'
  Object.assign(node_257.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
    'max-width': '920px',
  })
  node_257.dataset.component = 'WarumBlock'
  // H4
  const node_258 = document.createElement('span')
  _elements['node-258'] = node_258
  node_258.dataset.mirrorId = 'node-258'
  node_258.dataset.mirrorName = 'H4'
  node_258.innerHTML = formatInlineMarkdown('Warum diese Persona?')
  Object.assign(node_258.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_258.dataset.component = 'H4'
  node_257.appendChild(node_258)

  // BodyTxt
  const node_259 = document.createElement('span')
  _elements['node-259'] = node_259
  node_259.dataset.mirrorId = 'node-259'
  node_259.dataset.mirrorName = 'BodyTxt'
  node_259.innerHTML = formatInlineMarkdown(
    'Tim steht für eine ganz andere Logik: Studieninteressierte, die nicht primär eine Hochschule, sondern **eine Studienrichtung** suchen. Er ist der härteste Test dafür, ob die Spezialisierungen **wirklich tragen oder nur Etikett** sind.'
  )
  Object.assign(node_259.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_259.dataset.component = 'BodyTxt'
  node_257.appendChild(node_259)

  node_256.appendChild(node_257)

  // DimGrid
  const node_260 = document.createElement('div')
  _elements['node-260'] = node_260
  node_260.dataset.mirrorId = 'node-260'
  node_260.dataset.mirrorName = 'DimGrid'
  Object.assign(node_260.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(4, 1fr)',
    gap: '32px 64px',
  })
  node_260.dataset.layout = 'grid'
  node_260.dataset.component = 'DimGrid'
  // Dim
  const node_261 = document.createElement('div')
  _elements['node-261'] = node_261
  node_261.dataset.mirrorId = 'node-261'
  node_261.dataset.mirrorName = 'Dim'
  Object.assign(node_261.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_261.dataset.component = 'Dim'
  // H4
  const node_262 = document.createElement('span')
  _elements['node-262'] = node_262
  node_262.dataset.mirrorId = 'node-262'
  node_262.dataset.mirrorName = 'H4'
  node_262.innerHTML = formatInlineMarkdown('Situation')
  Object.assign(node_262.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_262.dataset.component = 'H4'
  node_261.appendChild(node_262)

  // BodyTxt
  const node_263 = document.createElement('span')
  _elements['node-263'] = node_263
  node_263.dataset.mirrorId = 'node-263'
  node_263.dataset.mirrorName = 'BodyTxt'
  node_263.innerHTML = formatInlineMarkdown(
    'Weiss seit der 7. Klasse: Games. Hat Schweizer Studios recherchiert, weiss, dass die Branche klein ist. ZHdK Game Design ist auf dem Radar, FHNW Game Entwicklung daher ernsthafte Option — er fragt sich aber, wie ernst die Hochschule diese Studienrichtung wirklich meint.'
  )
  Object.assign(node_263.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_263.dataset.component = 'BodyTxt'
  node_261.appendChild(node_263)

  node_260.appendChild(node_261)

  // Dim
  const node_264 = document.createElement('div')
  _elements['node-264'] = node_264
  node_264.dataset.mirrorId = 'node-264'
  node_264.dataset.mirrorName = 'Dim'
  Object.assign(node_264.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_264.dataset.component = 'Dim'
  // H4
  const node_265 = document.createElement('span')
  _elements['node-265'] = node_265
  node_265.dataset.mirrorId = 'node-265'
  node_265.dataset.mirrorName = 'H4'
  node_265.innerHTML = formatInlineMarkdown('Ziel')
  Object.assign(node_265.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_265.dataset.component = 'H4'
  node_264.appendChild(node_265)

  // BodyTxt
  const node_266 = document.createElement('span')
  _elements['node-266'] = node_266
  node_266.dataset.mirrorId = 'node-266'
  node_266.dataset.mirrorName = 'BodyTxt'
  node_266.innerHTML = formatInlineMarkdown(
    'Eine echte Game-Engineer-Ausbildung mit substanzieller Industrie-Anbindung. Mittelfristig: Gameplay-Programmer oder Engine-Entwickler in einem ernsthaften Studio.'
  )
  Object.assign(node_266.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.65',
  })
  node_266.dataset.component = 'BodyTxt'
  node_264.appendChild(node_266)

  node_260.appendChild(node_264)

  // Dim
  const node_267 = document.createElement('div')
  _elements['node-267'] = node_267
  node_267.dataset.mirrorId = 'node-267'
  node_267.dataset.mirrorName = 'Dim'
  Object.assign(node_267.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_267.dataset.component = 'Dim'
  // H4
  const node_268 = document.createElement('span')
  _elements['node-268'] = node_268
  node_268.dataset.mirrorId = 'node-268'
  node_268.dataset.mirrorName = 'H4'
  node_268.innerHTML = formatInlineMarkdown('Herausforderung')
  Object.assign(node_268.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_268.dataset.component = 'H4'
  node_267.appendChild(node_268)

  // BodyTxtMuted
  const node_269 = document.createElement('span')
  _elements['node-269'] = node_269
  node_269.dataset.mirrorId = 'node-269'
  node_269.dataset.mirrorName = 'BodyTxtMuted'
  node_269.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_269.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_269.dataset.component = 'BodyTxtMuted'
  node_267.appendChild(node_269)

  node_260.appendChild(node_267)

  // Dim
  const node_270 = document.createElement('div')
  _elements['node-270'] = node_270
  node_270.dataset.mirrorId = 'node-270'
  node_270.dataset.mirrorName = 'Dim'
  Object.assign(node_270.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_270.dataset.component = 'Dim'
  // H4
  const node_271 = document.createElement('span')
  _elements['node-271'] = node_271
  node_271.dataset.mirrorId = 'node-271'
  node_271.dataset.mirrorName = 'H4'
  node_271.innerHTML = formatInlineMarkdown('Needs')
  Object.assign(node_271.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_271.dataset.component = 'H4'
  node_270.appendChild(node_271)

  // BodyTxtMuted
  const node_272 = document.createElement('span')
  _elements['node-272'] = node_272
  node_272.dataset.mirrorId = 'node-272'
  node_272.dataset.mirrorName = 'BodyTxtMuted'
  node_272.innerHTML = formatInlineMarkdown('(siehe Detailbericht)')
  Object.assign(node_272.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_272.dataset.component = 'BodyTxtMuted'
  node_270.appendChild(node_272)

  node_260.appendChild(node_270)

  node_256.appendChild(node_260)

  // SoftBox
  const node_273 = document.createElement('div')
  _elements['node-273'] = node_273
  node_273.dataset.mirrorId = 'node-273'
  node_273.dataset.mirrorName = 'SoftBox'
  Object.assign(node_273.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: 'var(--soft-bg)',
    padding: '22px 24px',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
  })
  node_273.dataset.component = 'SoftBox'
  // Unknown
  const node_274 = document.createElement('div')
  _elements['node-274'] = node_274
  node_274.dataset.mirrorId = 'node-274'
  node_274.dataset.mirrorName = 'Unknown'
  node_274.dataset.component = 'Unknown'
  node_273.appendChild(node_274)

  node_256.appendChild(node_273)

  // ImplikationenBlock
  const node_275 = document.createElement('div')
  _elements['node-275'] = node_275
  node_275.dataset.mirrorId = 'node-275'
  node_275.dataset.mirrorName = 'ImplikationenBlock'
  Object.assign(node_275.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '32px',
    'margin-top': '40px',
  })
  node_275.dataset.component = 'ImplikationenBlock'
  // H3
  const node_276 = document.createElement('span')
  _elements['node-276'] = node_276
  node_276.dataset.mirrorId = 'node-276'
  node_276.dataset.mirrorName = 'H3'
  node_276.innerHTML = formatInlineMarkdown('**Implikationen für FHNW-Kommunikation**')
  Object.assign(node_276.style, {
    'font-size': '28px',
    'font-weight': '700',
    'letter-spacing': '-0.015em',
    'line-height': '1.2',
  })
  node_276.dataset.component = 'H3'
  node_275.appendChild(node_276)

  // DimGridTwo
  const node_277 = document.createElement('div')
  _elements['node-277'] = node_277
  node_277.dataset.mirrorId = 'node-277'
  node_277.dataset.mirrorName = 'DimGridTwo'
  Object.assign(node_277.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(2, 1fr)',
    gap: '32px 64px',
  })
  node_277.dataset.layout = 'grid'
  node_277.dataset.component = 'DimGridTwo'
  // Dim
  const node_278 = document.createElement('div')
  _elements['node-278'] = node_278
  node_278.dataset.mirrorId = 'node-278'
  node_278.dataset.mirrorName = 'Dim'
  Object.assign(node_278.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_278.dataset.component = 'Dim'
  // H4
  const node_279 = document.createElement('span')
  _elements['node-279'] = node_279
  node_279.dataset.mirrorId = 'node-279'
  node_279.dataset.mirrorName = 'H4'
  node_279.innerHTML = formatInlineMarkdown('Webseite')
  Object.assign(node_279.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_279.dataset.component = 'H4'
  node_278.appendChild(node_279)

  // BodyTxtMuted
  const node_280 = document.createElement('span')
  _elements['node-280'] = node_280
  node_280.dataset.mirrorId = 'node-280'
  node_280.dataset.mirrorName = 'BodyTxtMuted'
  node_280.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_280.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_280.dataset.component = 'BodyTxtMuted'
  node_278.appendChild(node_280)

  node_277.appendChild(node_278)

  // Dim
  const node_281 = document.createElement('div')
  _elements['node-281'] = node_281
  node_281.dataset.mirrorId = 'node-281'
  node_281.dataset.mirrorName = 'Dim'
  Object.assign(node_281.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '16px',
  })
  node_281.dataset.component = 'Dim'
  // H4
  const node_282 = document.createElement('span')
  _elements['node-282'] = node_282
  node_282.dataset.mirrorId = 'node-282'
  node_282.dataset.mirrorName = 'H4'
  node_282.innerHTML = formatInlineMarkdown('Infoveranstaltung')
  Object.assign(node_282.style, {
    'font-size': '19px',
    'font-weight': '700',
  })
  node_282.dataset.component = 'H4'
  node_281.appendChild(node_282)

  // BodyTxtMuted
  const node_283 = document.createElement('span')
  _elements['node-283'] = node_283
  node_283.dataset.mirrorId = 'node-283'
  node_283.dataset.mirrorName = 'BodyTxtMuted'
  node_283.innerHTML = formatInlineMarkdown('(persona-spezifisch — siehe Detailbericht)')
  Object.assign(node_283.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
    'font-style': 'italic',
    color: 'var(--muted-col)',
  })
  node_283.dataset.component = 'BodyTxtMuted'
  node_281.appendChild(node_283)

  node_277.appendChild(node_281)

  node_275.appendChild(node_277)

  node_256.appendChild(node_275)

  node_255.appendChild(node_256)

  node_246.appendChild(node_255)

  _root.appendChild(node_246)

  // SectionWide
  const node_284 = document.createElement('div')
  _elements['node-284'] = node_284
  node_284.dataset.mirrorId = 'node-284'
  node_284.dataset.mirrorName = 'SectionWide'
  Object.assign(node_284.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    padding: '112px 24px 112px 24px',
    width: '100%',
  })
  node_284.dataset.component = 'SectionWide'
  // Container
  const node_285 = document.createElement('div')
  _elements['node-285'] = node_285
  node_285.dataset.mirrorId = 'node-285'
  node_285.dataset.mirrorName = 'Container'
  Object.assign(node_285.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_285.dataset.component = 'Container'
  // TwoColumn
  const node_286 = document.createElement('div')
  _elements['node-286'] = node_286
  node_286.dataset.mirrorId = 'node-286'
  node_286.dataset.mirrorName = 'TwoColumn'
  Object.assign(node_286.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '64px',
  })
  node_286.dataset.layout = 'flex'
  node_286.dataset.component = 'TwoColumn'
  // ProseColumn
  const node_287 = document.createElement('div')
  _elements['node-287'] = node_287
  node_287.dataset.mirrorId = 'node-287'
  node_287.dataset.mirrorName = 'ProseColumn'
  Object.assign(node_287.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '280px',
    'flex-shrink': '0',
  })
  node_287.dataset.component = 'ProseColumn'
  // H2
  const node_288 = document.createElement('span')
  _elements['node-288'] = node_288
  node_288.dataset.mirrorId = 'node-288'
  node_288.dataset.mirrorName = 'H2'
  node_288.innerHTML = formatInlineMarkdown('Offene Punkte / nächste Schritte')
  Object.assign(node_288.style, {
    'font-size': '44px',
    'font-weight': '700',
    'letter-spacing': '-0.02em',
    'line-height': '1.05',
  })
  node_288.dataset.component = 'H2'
  node_287.appendChild(node_288)

  node_286.appendChild(node_287)

  // OffeneList
  const node_289 = document.createElement('div')
  _elements['node-289'] = node_289
  node_289.dataset.mirrorId = 'node-289'
  node_289.dataset.mirrorName = 'OffeneList'
  Object.assign(node_289.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '22px',
  })
  node_289.dataset.component = 'OffeneList'
  // OffenePunkt
  const node_290 = document.createElement('div')
  _elements['node-290'] = node_290
  node_290.dataset.mirrorId = 'node-290'
  node_290.dataset.mirrorName = 'OffenePunkt'
  Object.assign(node_290.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_290.dataset.layout = 'flex'
  node_290.dataset.component = 'OffenePunkt'
  // OffeneNum
  const node_291 = document.createElement('span')
  _elements['node-291'] = node_291
  node_291.dataset.mirrorId = 'node-291'
  node_291.dataset.mirrorName = 'OffeneNum'
  node_291.innerHTML = formatInlineMarkdown('01')
  Object.assign(node_291.style, {
    'font-size': '14px',
    'font-weight': '300',
    width: '32px',
    'flex-shrink': '0',
  })
  node_291.dataset.component = 'OffeneNum'
  node_290.appendChild(node_291)

  // BodyTxtCompact
  const node_292 = document.createElement('span')
  _elements['node-292'] = node_292
  node_292.dataset.mirrorId = 'node-292'
  node_292.dataset.mirrorName = 'BodyTxtCompact'
  node_292.innerHTML = formatInlineMarkdown(
    '**Eltern als Co-Persona** — vor allem für Lukas und Nadia entscheidungsrelevant. Soll separat ausgearbeitet werden.'
  )
  Object.assign(node_292.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_292.dataset.component = 'BodyTxtCompact'
  node_290.appendChild(node_292)

  node_289.appendChild(node_290)

  // OffenePunkt
  const node_293 = document.createElement('div')
  _elements['node-293'] = node_293
  node_293.dataset.mirrorId = 'node-293'
  node_293.dataset.mirrorName = 'OffenePunkt'
  Object.assign(node_293.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_293.dataset.layout = 'flex'
  node_293.dataset.component = 'OffenePunkt'
  // OffeneNum
  const node_294 = document.createElement('span')
  _elements['node-294'] = node_294
  node_294.dataset.mirrorId = 'node-294'
  node_294.dataset.mirrorName = 'OffeneNum'
  node_294.innerHTML = formatInlineMarkdown('02')
  Object.assign(node_294.style, {
    'font-size': '14px',
    'font-weight': '300',
    width: '32px',
    'flex-shrink': '0',
  })
  node_294.dataset.component = 'OffeneNum'
  node_293.appendChild(node_294)

  // BodyTxtCompact
  const node_295 = document.createElement('span')
  _elements['node-295'] = node_295
  node_295.dataset.mirrorId = 'node-295'
  node_295.dataset.mirrorName = 'BodyTxtCompact'
  node_295.innerHTML = formatInlineMarkdown(
    '**Diversität explizit verankern** — Frauenanteil, Migrationsbiografie, Erstakademiker:in. Querschnitt durch alle Personas, nicht eigene Persona.'
  )
  Object.assign(node_295.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_295.dataset.component = 'BodyTxtCompact'
  node_293.appendChild(node_295)

  node_289.appendChild(node_293)

  // OffenePunkt
  const node_296 = document.createElement('div')
  _elements['node-296'] = node_296
  node_296.dataset.mirrorId = 'node-296'
  node_296.dataset.mirrorName = 'OffenePunkt'
  Object.assign(node_296.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_296.dataset.layout = 'flex'
  node_296.dataset.component = 'OffenePunkt'
  // OffeneNum
  const node_297 = document.createElement('span')
  _elements['node-297'] = node_297
  node_297.dataset.mirrorId = 'node-297'
  node_297.dataset.mirrorName = 'OffeneNum'
  node_297.innerHTML = formatInlineMarkdown('03')
  Object.assign(node_297.style, {
    'font-size': '14px',
    'font-weight': '300',
    width: '32px',
    'flex-shrink': '0',
  })
  node_297.dataset.component = 'OffeneNum'
  node_296.appendChild(node_297)

  // BodyTxtCompact
  const node_298 = document.createElement('span')
  _elements['node-298'] = node_298
  node_298.dataset.mirrorId = 'node-298'
  node_298.dataset.mirrorName = 'BodyTxtCompact'
  node_298.innerHTML = formatInlineMarkdown(
    '**Data Science** — analoge Persona-Arbeit, sobald Informatik validiert ist.'
  )
  Object.assign(node_298.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_298.dataset.component = 'BodyTxtCompact'
  node_296.appendChild(node_298)

  node_289.appendChild(node_296)

  // OffenePunkt
  const node_299 = document.createElement('div')
  _elements['node-299'] = node_299
  node_299.dataset.mirrorId = 'node-299'
  node_299.dataset.mirrorName = 'OffenePunkt'
  Object.assign(node_299.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_299.dataset.layout = 'flex'
  node_299.dataset.component = 'OffenePunkt'
  // OffeneNum
  const node_300 = document.createElement('span')
  _elements['node-300'] = node_300
  node_300.dataset.mirrorId = 'node-300'
  node_300.dataset.mirrorName = 'OffeneNum'
  node_300.innerHTML = formatInlineMarkdown('04')
  Object.assign(node_300.style, {
    'font-size': '14px',
    'font-weight': '300',
    width: '32px',
    'flex-shrink': '0',
  })
  node_300.dataset.component = 'OffeneNum'
  node_299.appendChild(node_300)

  // BodyTxtCompact
  const node_301 = document.createElement('span')
  _elements['node-301'] = node_301
  node_301.dataset.mirrorId = 'node-301'
  node_301.dataset.mirrorName = 'BodyTxtCompact'
  node_301.innerHTML = formatInlineMarkdown(
    '**User Research** — diese Proto-Personas mit echten Studieninteressierten testen. Insbesondere prüfen: Stimmen die «inneren Stimmen»? Welche Fragen fehlen? Welche Personas existieren so gar nicht?'
  )
  Object.assign(node_301.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_301.dataset.component = 'BodyTxtCompact'
  node_299.appendChild(node_301)

  node_289.appendChild(node_299)

  // OffenePunkt
  const node_302 = document.createElement('div')
  _elements['node-302'] = node_302
  node_302.dataset.mirrorId = 'node-302'
  node_302.dataset.mirrorName = 'OffenePunkt'
  Object.assign(node_302.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_302.dataset.layout = 'flex'
  node_302.dataset.component = 'OffenePunkt'
  // OffeneNum
  const node_303 = document.createElement('span')
  _elements['node-303'] = node_303
  node_303.dataset.mirrorId = 'node-303'
  node_303.dataset.mirrorName = 'OffeneNum'
  node_303.innerHTML = formatInlineMarkdown('05')
  Object.assign(node_303.style, {
    'font-size': '14px',
    'font-weight': '300',
    width: '32px',
    'flex-shrink': '0',
  })
  node_303.dataset.component = 'OffeneNum'
  node_302.appendChild(node_303)

  // BodyTxtCompact
  const node_304 = document.createElement('span')
  _elements['node-304'] = node_304
  node_304.dataset.mirrorId = 'node-304'
  node_304.dataset.mirrorName = 'BodyTxtCompact'
  node_304.innerHTML = formatInlineMarkdown(
    '**Inhaltliche Architektur** — pro Persona ableiten, was das für Webseite und Infoveranstaltung konkret heisst.'
  )
  Object.assign(node_304.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_304.dataset.component = 'BodyTxtCompact'
  node_302.appendChild(node_304)

  node_289.appendChild(node_302)

  // OffenePunkt
  const node_305 = document.createElement('div')
  _elements['node-305'] = node_305
  node_305.dataset.mirrorId = 'node-305'
  node_305.dataset.mirrorName = 'OffenePunkt'
  Object.assign(node_305.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '24px',
  })
  node_305.dataset.layout = 'flex'
  node_305.dataset.component = 'OffenePunkt'
  // OffeneNum
  const node_306 = document.createElement('span')
  _elements['node-306'] = node_306
  node_306.dataset.mirrorId = 'node-306'
  node_306.dataset.mirrorName = 'OffeneNum'
  node_306.innerHTML = formatInlineMarkdown('06')
  Object.assign(node_306.style, {
    'font-size': '14px',
    'font-weight': '300',
    width: '32px',
    'flex-shrink': '0',
  })
  node_306.dataset.component = 'OffeneNum'
  node_305.appendChild(node_306)

  // BodyTxtCompact
  const node_307 = document.createElement('span')
  _elements['node-307'] = node_307
  node_307.dataset.mirrorId = 'node-307'
  node_307.dataset.mirrorName = 'BodyTxtCompact'
  node_307.innerHTML = formatInlineMarkdown(
    '**Persona-Lücken prüfen** — z. B. Studieninteressierte mit Migrationshintergrund, internationale Bewerber:innen, ältere Quereinsteiger:innen (30+).'
  )
  Object.assign(node_307.style, {
    'font-size': '17px',
    'font-weight': '300',
    'line-height': '1.6',
  })
  node_307.dataset.component = 'BodyTxtCompact'
  node_305.appendChild(node_307)

  node_289.appendChild(node_305)

  node_286.appendChild(node_289)

  node_285.appendChild(node_286)

  node_284.appendChild(node_285)

  _root.appendChild(node_284)

  // FooterFrame
  const node_308 = document.createElement('div')
  _elements['node-308'] = node_308
  node_308.dataset.mirrorId = 'node-308'
  node_308.dataset.mirrorName = 'FooterFrame'
  Object.assign(node_308.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    background: '$ink',
    padding: '88px 24px 40px 24px',
    width: '100%',
  })
  node_308.dataset.component = 'FooterFrame'
  // Container
  const node_309 = document.createElement('div')
  _elements['node-309'] = node_309
  node_309.dataset.mirrorId = 'node-309'
  node_309.dataset.mirrorName = 'Container'
  Object.assign(node_309.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '100%',
    'align-self': 'stretch',
    'min-width': '0',
    'max-width': '1280px',
    margin: '0px auto',
  })
  node_309.dataset.component = 'Container'
  // LogoLight
  const node_310 = document.createElement('div')
  _elements['node-310'] = node_310
  node_310.dataset.mirrorId = 'node-310'
  node_310.dataset.mirrorName = 'LogoLight'
  Object.assign(node_310.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
    gap: '14px',
  })
  node_310.dataset.layout = 'flex'
  node_310.dataset.component = 'LogoLight'
  // Frame
  const node_311 = document.createElement('div')
  _elements['node-311'] = node_311
  node_311.dataset.mirrorId = 'node-311'
  node_311.dataset.mirrorName = 'Frame'
  Object.assign(node_311.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'center',
  })
  node_311.dataset.layout = 'flex'
  node_311.dataset.component = 'Frame'
  node_311.dataset.slot = 'Frame'
  // LogoMarkNLight
  const node_312 = document.createElement('span')
  _elements['node-312'] = node_312
  node_312.dataset.mirrorId = 'node-312'
  node_312.dataset.mirrorName = 'LogoMarkNLight'
  node_312.innerHTML = formatInlineMarkdown('n')
  Object.assign(node_312.style, {
    'font-size': '28px',
    'font-weight': '700',
    'line-height': '1',
    color: 'var(--white-col)',
  })
  node_312.dataset.component = 'LogoMarkNLight'
  node_311.appendChild(node_312)

  // LogoBarLight
  const node_313 = document.createElement('div')
  _elements['node-313'] = node_313
  node_313.dataset.mirrorId = 'node-313'
  node_313.dataset.mirrorName = 'LogoBarLight'
  Object.assign(node_313.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    width: '2px',
    'flex-shrink': '0',
    height: '24px',
    'flex-shrink': '0',
    background: '$white',
    margin: '0px 4px 0px 4px',
  })
  node_313.dataset.component = 'LogoBarLight'
  node_311.appendChild(node_313)

  // LogoMarkWLight
  const node_314 = document.createElement('span')
  _elements['node-314'] = node_314
  node_314.dataset.mirrorId = 'node-314'
  node_314.dataset.mirrorName = 'LogoMarkWLight'
  node_314.innerHTML = formatInlineMarkdown('w')
  Object.assign(node_314.style, {
    'font-size': '30px',
    'font-weight': '700',
    'font-style': 'italic',
    'font-family': 'Georgia, serif',
    'line-height': '1',
    color: 'var(--white-col)',
  })
  node_314.dataset.component = 'LogoMarkWLight'
  node_311.appendChild(node_314)

  node_310.appendChild(node_311)

  // Frame
  const node_315 = document.createElement('div')
  _elements['node-315'] = node_315
  node_315.dataset.mirrorId = 'node-315'
  node_315.dataset.mirrorName = 'Frame'
  Object.assign(node_315.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '2px',
  })
  node_315.dataset.component = 'Frame'
  node_315.dataset.slot = 'Frame'
  // LogoTextSmall
  const node_316 = document.createElement('span')
  _elements['node-316'] = node_316
  node_316.dataset.mirrorId = 'node-316'
  node_316.dataset.mirrorName = 'LogoTextSmall'
  node_316.innerHTML = formatInlineMarkdown('Fachhochschule Nordwestschweiz')
  Object.assign(node_316.style, {
    'font-size': '11px',
    'font-weight': '400',
    color: 'var(--white-col)',
  })
  node_316.dataset.component = 'LogoTextSmall'
  node_315.appendChild(node_316)

  // LogoTextBold
  const node_317 = document.createElement('span')
  _elements['node-317'] = node_317
  node_317.dataset.mirrorId = 'node-317'
  node_317.dataset.mirrorName = 'LogoTextBold'
  node_317.innerHTML = formatInlineMarkdown('Hochschule für Informatik')
  Object.assign(node_317.style, {
    'font-size': '11px',
    'font-weight': '700',
    color: 'var(--white-col)',
  })
  node_317.dataset.component = 'LogoTextBold'
  node_315.appendChild(node_317)

  node_310.appendChild(node_315)

  node_309.appendChild(node_310)

  // FooterGrid
  const node_318 = document.createElement('div')
  _elements['node-318'] = node_318
  node_318.dataset.mirrorId = 'node-318'
  node_318.dataset.mirrorName = 'FooterGrid'
  Object.assign(node_318.style, {
    display: 'grid',
    'grid-template-columns': 'repeat(3, 1fr)',
    gap: '32px',
  })
  node_318.dataset.layout = 'grid'
  node_318.dataset.component = 'FooterGrid'
  // FooterColMain
  const node_319 = document.createElement('div')
  _elements['node-319'] = node_319
  node_319.dataset.mirrorId = 'node-319'
  node_319.dataset.mirrorName = 'FooterColMain'
  Object.assign(node_319.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    gap: '14px',
    'grid-column': 'span 6',
    width: '100%',
  })
  node_319.dataset.component = 'FooterColMain'
  // FooterHeading
  const node_320 = document.createElement('span')
  _elements['node-320'] = node_320
  node_320.dataset.mirrorId = 'node-320'
  node_320.dataset.mirrorName = 'FooterHeading'
  node_320.innerHTML = formatInlineMarkdown('Über dieses Dokument')
  Object.assign(node_320.style, {
    'font-size': '14px',
    'font-weight': '700',
    color: 'var(--white-col)',
  })
  node_320.dataset.component = 'FooterHeading'
  node_319.appendChild(node_320)

  // FooterText
  const node_321 = document.createElement('span')
  _elements['node-321'] = node_321
  node_321.dataset.mirrorId = 'node-321'
  node_321.dataset.mirrorName = 'FooterText'
  node_321.innerHTML = formatInlineMarkdown(
    'Internes Arbeitsdokument zur Persona-Arbeit für Studieninteressierte des Bachelor Informatik. Proto-Personas, hypothesenbasiert; bewusst dafür gemacht, durch User Research validiert oder verworfen zu werden.'
  )
  Object.assign(node_321.style, {
    'font-size': '14px',
    'font-weight': '300',
    'line-height': '1.55',
    color: 'var(--white-col)',
  })
  node_321.dataset.component = 'FooterText'
  node_319.appendChild(node_321)

  node_318.appendChild(node_319)

  // FooterColMeta
  const node_322 = document.createElement('div')
  _elements['node-322'] = node_322
  node_322.dataset.mirrorId = 'node-322'
  node_322.dataset.mirrorName = 'FooterColMeta'
  Object.assign(node_322.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-items': 'flex-start',
    gap: '14px',
    'grid-column': 'span 3',
    width: '100%',
  })
  node_322.dataset.component = 'FooterColMeta'
  // FooterHeading
  const node_323 = document.createElement('span')
  _elements['node-323'] = node_323
  node_323.dataset.mirrorId = 'node-323'
  node_323.dataset.mirrorName = 'FooterHeading'
  node_323.innerHTML = formatInlineMarkdown('Status')
  Object.assign(node_323.style, {
    'font-size': '14px',
    'font-weight': '700',
    color: 'var(--white-col)',
  })
  node_323.dataset.component = 'FooterHeading'
  node_322.appendChild(node_323)

  // FooterStatusList
  const node_324 = document.createElement('div')
  _elements['node-324'] = node_324
  node_324.dataset.mirrorId = 'node-324'
  node_324.dataset.mirrorName = 'FooterStatusList'
  Object.assign(node_324.style, {
    display: 'flex',
    'flex-direction': 'column',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    gap: '6px',
  })
  node_324.dataset.component = 'FooterStatusList'
  // FooterText
  const node_325 = document.createElement('span')
  _elements['node-325'] = node_325
  node_325.dataset.mirrorId = 'node-325'
  node_325.dataset.mirrorName = 'FooterText'
  node_325.innerHTML = formatInlineMarkdown('Stand: 6. Mai 2026')
  Object.assign(node_325.style, {
    'font-size': '14px',
    'font-weight': '300',
    'line-height': '1.55',
    color: 'var(--white-col)',
  })
  node_325.dataset.component = 'FooterText'
  node_324.appendChild(node_325)

  // FooterText
  const node_326 = document.createElement('span')
  _elements['node-326'] = node_326
  node_326.dataset.mirrorId = 'node-326'
  node_326.dataset.mirrorName = 'FooterText'
  node_326.innerHTML = formatInlineMarkdown('Version: v1')
  Object.assign(node_326.style, {
    'font-size': '14px',
    'font-weight': '300',
    'line-height': '1.55',
    color: 'var(--white-col)',
  })
  node_326.dataset.component = 'FooterText'
  node_324.appendChild(node_326)

  // FooterText
  const node_327 = document.createElement('span')
  _elements['node-327'] = node_327
  node_327.dataset.mirrorId = 'node-327'
  node_327.dataset.mirrorName = 'FooterText'
  node_327.innerHTML = formatInlineMarkdown('Scope: Studiengang Informatik')
  Object.assign(node_327.style, {
    'font-size': '14px',
    'font-weight': '300',
    'line-height': '1.55',
    color: 'var(--white-col)',
  })
  node_327.dataset.component = 'FooterText'
  node_324.appendChild(node_327)

  node_322.appendChild(node_324)

  node_318.appendChild(node_322)

  node_309.appendChild(node_318)

  // FooterBaseRow
  const node_328 = document.createElement('div')
  _elements['node-328'] = node_328
  node_328.dataset.mirrorId = 'node-328'
  node_328.dataset.mirrorName = 'FooterBaseRow'
  Object.assign(node_328.style, {
    display: 'flex',
    'flex-direction': 'row',
    'align-self': 'stretch',
    'align-items': 'flex-start',
    'flex-wrap': 'wrap',
    gap: '12px 24px',
    'margin-top': '40px',
  })
  node_328.dataset.layout = 'flex'
  node_328.dataset.component = 'FooterBaseRow'
  // FooterBase
  const node_329 = document.createElement('span')
  _elements['node-329'] = node_329
  node_329.dataset.mirrorId = 'node-329'
  node_329.dataset.mirrorName = 'FooterBase'
  node_329.innerHTML = formatInlineMarkdown(
    '© Fachhochschule Nordwestschweiz · Hochschule für Informatik'
  )
  Object.assign(node_329.style, {
    'font-size': '12px',
    color: 'var(--white-col)',
  })
  node_329.dataset.component = 'FooterBase'
  node_328.appendChild(node_329)

  // FooterBase
  const node_330 = document.createElement('span')
  _elements['node-330'] = node_330
  node_330.dataset.mirrorId = 'node-330'
  node_330.dataset.mirrorName = 'FooterBase'
  node_330.innerHTML = formatInlineMarkdown(
    'Internes Arbeitsdokument · nicht zur externen Verbreitung'
  )
  Object.assign(node_330.style, {
    'font-size': '12px',
    color: 'var(--white-col)',
  })
  node_330.dataset.component = 'FooterBase'
  node_328.appendChild(node_330)

  node_309.appendChild(node_328)

  node_308.appendChild(node_309)

  _root.appendChild(node_308)

  // Attach API methods directly to _root for intuitive usage
  // createUI() returns the DOM node directly, with API methods attached

  // State management
  _root.setState = function (key, value) {
    _state[key] = value
    if (typeof __mirrorData !== 'undefined') __mirrorData[key] = value
    this.update()
  }

  _root.getState = function (key) {
    return typeof __mirrorData !== 'undefined' && key in __mirrorData
      ? __mirrorData[key]
      : _state[key]
  }

  _root.update = function () {
    // Re-render each loops based on state changes
    for (const el of _root.querySelectorAll('[data-each-container]')) {
      if (el._eachConfig) {
        const { collection, renderItem, filterFn } = el._eachConfig
        const items = _state[collection] || []
        const filtered = filterFn ? items.filter(filterFn) : items
        el.innerHTML = ''
        filtered.forEach((item, index) => el.appendChild(renderItem(item, index)))
      }
    }

    // Re-render conditionals based on state changes
    for (const el of _root.querySelectorAll('[data-conditional-id]')) {
      if (el._conditionalConfig) {
        const { condition, renderThen, renderElse } = el._conditionalConfig
        el.innerHTML = ''
        if (condition()) {
          el.appendChild(renderThen())
        } else if (renderElse) {
          el.appendChild(renderElse())
        }
      }
    }
  }

  return _root
}

// Mirror DOM Runtime

// Stamping shim: when the typed runtime modules are .toString()'d at
// compile time, esbuild/tsx may insert __name(fn, "label") calls to
// preserve Function.name across minification. The compiled output
// references __name at runtime; declare a no-op so the calls are
// harmless. (Real function names come from the function declarations
// themselves; this only masks the build-tool helper.)
function __name(fn, _name) {
  return fn
}

// Security helpers (stamped from compiler/runtime/icons.ts via .toString())
function sanitizeIconName(name) {
  if (!name || typeof name !== 'string') return null
  if (name.startsWith('__loopVar:') || name.startsWith('__conditional:')) {
    return null
  }
  if (!/^[a-z0-9\-]+$/.test(name)) {
    console.warn('[Security] Invalid icon name rejected:', name)
    return null
  }
  if (name.length > 50) {
    console.warn('[Security] Icon name too long:', name)
    return null
  }
  return name
}
function sanitizeSVG(svgText) {
  function parseSvgDocument(text) {
    const parser = new DOMParser()
    const doc = parser.parseFromString(text, 'image/svg+xml')
    if (doc.querySelector('parsererror')) {
      console.warn('[Security] Invalid SVG content')
      return null
    }
    return doc
  }
  __name(parseSvgDocument, 'parseSvgDocument')
  function isValidSvgRoot(element) {
    if (element.tagName.toLowerCase() !== 'svg') {
      console.warn('[Security] Not an SVG element')
      return false
    }
    return true
  }
  __name(isValidSvgRoot, 'isValidSvgRoot')
  function removeDangerousElements(svg) {
    const dangerous = [
      'script',
      'foreignObject',
      'use',
      'image',
      'a',
      'style',
      'defs',
      'metadata',
      'animate',
      'set',
    ]
    for (const tag of dangerous) svg.querySelectorAll(tag).forEach(el => el.remove())
  }
  __name(removeDangerousElements, 'removeDangerousElements')
  function removeDangerousAttributes(svg) {
    const dangerousPattern = /^(on|href|xlink:href|src|data|formaction)/i
    function isDangerous(attr) {
      return dangerousPattern.test(attr.name) || attr.value.includes('javascript:')
    }
    __name(isDangerous, 'isDangerous')
    for (const el of svg.querySelectorAll('*')) {
      for (const attr of Array.from(el.attributes)) {
        if (isDangerous(attr)) el.removeAttribute(attr.name)
      }
    }
  }
  __name(removeDangerousAttributes, 'removeDangerousAttributes')
  try {
    const doc = parseSvgDocument(svgText)
    if (!doc) return null
    const svg = doc.documentElement
    if (!isValidSvgRoot(svg)) return null
    removeDangerousElements(svg)
    removeDangerousAttributes(svg)
    return svg.outerHTML
  } catch (err) {
    console.warn('[Security] SVG sanitization failed:', err)
    return null
  }
}

// Inline-markdown formatter (stamped from compiler/runtime/inline-markdown.ts).
// Converts `**bold**` and `*italic*` inside Text content to <strong>/<em>.
// HTML-escapes input first, so user data can never inject tags.
function formatInlineMarkdown(text) {
  if (text === null || text === void 0) return ''
  const str = typeof text === 'string' ? text : String(text)
  let html = str
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;')
  html = html.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
  html = html.replace(/\*([^*]+?)\*/g, '<em>$1</em>')
  return html
}

// Scroll helpers (stamped from compiler/runtime/scroll.ts via .toString())
function resolveElement(element) {
  if (!element) return null
  if (typeof element !== 'string') return element
  return document.querySelector(`[data-mirror-name="${element}"]`)
}
function scrollTo(element, options) {
  const el = resolveElement(element)
  if (!el) return
  const { behavior = 'smooth', block = 'start', inline = 'nearest' } = options || {}
  el.scrollIntoView({ behavior, block, inline })
}
function scrollBy(container, x = 0, y = 0, behavior = 'smooth') {
  const el = resolveElement(container)
  if (!el) return
  el.scrollBy({ left: x, top: y, behavior })
}
function scrollToTop(element, behavior = 'smooth') {
  if (!element) {
    window.scrollTo({ top: 0, behavior })
    return
  }
  const el = resolveElement(element)
  if (el) el.scrollTo({ top: 0, behavior })
}
function scrollToBottom(element, behavior = 'smooth') {
  if (!element) {
    window.scrollTo({ top: document.body.scrollHeight, behavior })
    return
  }
  const el = resolveElement(element)
  if (el) el.scrollTo({ top: el.scrollHeight, behavior })
}

// Toast module (stamped factory from compiler/runtime/toast.ts).
// Invoke once to get a fresh counter + active-toast map; expose the
// public API as top-level functions so _runtime can shorthand them.
function createToastModule() {
  const POSITION_STYLES = {
    top: { top: '20px', left: '50%', transform: 'translateX(-50%) translateY(-10px)' },
    bottom: { bottom: '20px', left: '50%', transform: 'translateX(-50%) translateY(10px)' },
    'top-left': { top: '20px', left: '20px', transform: 'translateY(-10px)' },
    'top-right': { top: '20px', right: '20px', transform: 'translateY(-10px)' },
    'bottom-left': { bottom: '20px', left: '20px', transform: 'translateY(10px)' },
    'bottom-right': { bottom: '20px', right: '20px', transform: 'translateY(10px)' },
  }
  const TYPE_COLORS = {
    info: { bg: '#1a1a1a', color: '#fff' },
    success: { bg: '#10b981', color: '#fff' },
    error: { bg: '#ef4444', color: '#fff' },
    warning: { bg: '#f59e0b', color: '#000' },
  }
  let toastCounter = 0
  const activeToasts = new Map()
  function createToastElement(message, type, position, toastId) {
    const el = document.createElement('div')
    el.className = 'mirror-toast'
    el.dataset.type = type
    el.dataset.position = position
    el.dataset.toastId = String(toastId)
    el.textContent = message
    Object.assign(el.style, {
      position: 'fixed',
      zIndex: '10000',
      padding: '12px 20px',
      borderRadius: '8px',
      fontSize: '14px',
      fontFamily: 'system-ui, -apple-system, sans-serif',
      boxShadow: '0 4px 12px rgba(0,0,0,0.3)',
      transition: 'opacity 0.2s, transform 0.2s',
      opacity: '0',
      transform: 'translateY(10px)',
    })
    Object.assign(el.style, POSITION_STYLES[position])
    const colors = TYPE_COLORS[type] || TYPE_COLORS.info
    el.style.background = colors.bg
    el.style.color = colors.color
    return el
  }
  __name(createToastElement, 'createToastElement')
  function animateIn(el, position) {
    requestAnimationFrame(() => {
      el.style.opacity = '1'
      if (position === 'top' || position === 'bottom') {
        el.style.transform = 'translateX(-50%) translateY(0)'
      } else {
        el.style.transform = 'translateY(0)'
      }
    })
  }
  __name(animateIn, 'animateIn')
  function dismissToastsAtPosition(position) {
    for (const [id, t] of activeToasts) {
      if (t.element.dataset.position === position) {
        dismissSingleToast(id)
      }
    }
  }
  __name(dismissToastsAtPosition, 'dismissToastsAtPosition')
  function dismissSingleToast(toastId) {
    const t = activeToasts.get(toastId)
    if (!t) return
    clearTimeout(t.dismissTimeout)
    clearTimeout(t.fadeTimeout)
    t.element.remove()
    activeToasts.delete(toastId)
  }
  __name(dismissSingleToast, 'dismissSingleToast')
  function scheduleAutoDismiss(toastId, el, duration) {
    const dismissTimeout = window.setTimeout(() => startFadeOut(toastId, el), duration)
    activeToasts.set(toastId, { element: el, dismissTimeout, fadeTimeout: 0 })
  }
  __name(scheduleAutoDismiss, 'scheduleAutoDismiss')
  function startFadeOut(toastId, el) {
    const data = activeToasts.get(toastId)
    if (!data) return
    el.style.opacity = '0'
    const fadeTimeout = window.setTimeout(() => {
      if (el.parentNode) el.remove()
      activeToasts.delete(toastId)
    }, 200)
    data.fadeTimeout = fadeTimeout
  }
  __name(startFadeOut, 'startFadeOut')
  function toast2(message, options) {
    const opts = options || {}
    const duration = opts.duration ?? 3e3
    const type = opts.type ?? 'info'
    const position = opts.position ?? 'bottom'
    const toastId = ++toastCounter
    dismissToastsAtPosition(position)
    const el = createToastElement(message, type, position, toastId)
    document.body.appendChild(el)
    animateIn(el, position)
    scheduleAutoDismiss(toastId, el, duration)
    return toastId
  }
  __name(toast2, 'toast')
  function dismissToast2(toastId) {
    if (toastId !== void 0) {
      dismissSingleToast(toastId)
    } else {
      for (const [id] of activeToasts) dismissSingleToast(id)
    }
  }
  __name(dismissToast2, 'dismissToast')
  return { toast: toast2, dismissToast: dismissToast2 }
}
const { toast, dismissToast } = createToastModule()

// Batching module (stamped factory from compiler/runtime/batching.ts).
// Available as batchInFrame(fn) for any future stamps that need
// requestAnimationFrame coalescing.
function createBatchingModule() {
  let insideFrame = false
  function isInsideFrame2() {
    return insideFrame
  }
  __name(isInsideFrame2, 'isInsideFrame')
  function setFrameState2(value) {
    insideFrame = value
  }
  __name(setFrameState2, 'setFrameState')
  function batchInFrame2(fn) {
    if (insideFrame) {
      fn()
      return
    }
    requestAnimationFrame(() => {
      insideFrame = true
      try {
        fn()
      } finally {
        insideFrame = false
      }
    })
  }
  __name(batchInFrame2, 'batchInFrame')
  return {
    isInsideFrame: isInsideFrame2,
    setFrameState: setFrameState2,
    batchInFrame: batchInFrame2,
  }
}
const { isInsideFrame, setFrameState, batchInFrame } = createBatchingModule()

// Input-control helpers (stamped from compiler/runtime/input-control.ts)
function focus(el) {
  const target = resolveElement(el)
  if (!target) return
  const isFormElement =
    target instanceof HTMLInputElement ||
    target instanceof HTMLTextAreaElement ||
    target instanceof HTMLSelectElement ||
    target instanceof HTMLButtonElement
  if (isFormElement) {
    target.focus()
  } else if (target.tabIndex >= 0 || target.hasAttribute('tabindex')) {
    target.focus()
  }
}
function blur(el) {
  const target = resolveElement(el)
  if (!target) return
  target.blur()
}
function clear(el) {
  const target = resolveElement(el)
  if (!target) return
  const isTextInput = target instanceof HTMLInputElement || target instanceof HTMLTextAreaElement
  if (isTextInput) {
    target.value = ''
    target.dispatchEvent(new Event('input', { bubbles: true }))
  }
}
function selectText(el) {
  const target = resolveElement(el)
  if (!target) return
  const isTextInput = target instanceof HTMLInputElement || target instanceof HTMLTextAreaElement
  if (isTextInput) {
    target.select()
  }
}
function setError(el, message) {
  const target = resolveElement(el)
  if (!target) return
  target.dataset.invalid = 'true'
  target.dataset.state = 'invalid'
  target.setAttribute('aria-invalid', 'true')
  const isTextInput = target instanceof HTMLInputElement || target instanceof HTMLTextAreaElement
  if (isTextInput) {
    target.setCustomValidity(message || 'Invalid')
  }
  if (message) {
    const errorId = target.dataset.errorId || `${target.id || target.dataset.name || 'field'}-error`
    target.dataset.errorId = errorId
    let errorEl = document.getElementById(errorId)
    if (!errorEl) {
      errorEl = document.createElement('span')
      errorEl.id = errorId
      errorEl.className = 'mirror-error-message'
      errorEl.style.cssText = 'color: #ef4444; font-size: 12px; margin-top: 4px; display: block;'
      target.parentNode?.insertBefore(errorEl, target.nextSibling)
    }
    errorEl.textContent = message
    errorEl.style.display = 'block'
    target.setAttribute('aria-describedby', errorId)
  }
}
function clearError(el) {
  const target = resolveElement(el)
  if (!target) return
  delete target.dataset.invalid
  if (target.dataset.state === 'invalid') {
    delete target.dataset.state
  }
  target.removeAttribute('aria-describedby')
  target.removeAttribute('aria-invalid')
  const isTextInput = target instanceof HTMLInputElement || target instanceof HTMLTextAreaElement
  if (isTextInput) {
    target.setCustomValidity('')
  }
  const errorId = target.dataset.errorId
  if (errorId) {
    const errorEl = document.getElementById(errorId)
    if (errorEl) {
      errorEl.style.display = 'none'
    }
  }
}

// Page-navigation security (stamped from compiler/runtime/component-navigation.ts)
function sanitizePageName(name) {
  if (!name || typeof name !== 'string') return null
  if (name.includes('..')) {
    console.warn('[Security] Path traversal attempt blocked:', name)
    return null
  }
  if (name.startsWith('/') || name.startsWith('\\')) {
    console.warn('[Security] Absolute path blocked:', name)
    return null
  }
  if (!/^[a-zA-Z0-9_\-/]+(\.[a-zA-Z0-9]+)?$/.test(name)) {
    console.warn('[Security] Invalid page name characters:', name)
    return null
  }
  return name
}

// State-machine cluster (stamped from compiler/runtime/state-machine.ts).
// Helpers in dep order: collectStateProperties → storeBaseStyles →
// ensureBaseStyles, buildStateContext → evaluateToken → evaluateCondition,
// hasLogicalOperators → evaluateChildVisibility → updateVisibility, and
// finally setState / toggleState which use the lot.
function applyState(el, state) {
  if (!el?._stateStyles?.[state]) return
  Object.assign(el.style, el._stateStyles[state])
}
function removeState(el, _state) {
  if (!el?._baseStyles) return
  Object.assign(el.style, el._baseStyles)
}
function collectStateProperties(stateStyles) {
  const props = new Set()
  for (const state of Object.values(stateStyles)) {
    for (const prop of Object.keys(state)) {
      props.add(prop)
    }
  }
  return props
}
function storeBaseStyles(el, props) {
  el._baseStyles = {}
  for (const prop of props) {
    el._baseStyles[prop] = el.style[prop] || ''
  }
}
function ensureBaseStyles(el) {
  if (el._baseStyles || !el._stateStyles) return
  const props = collectStateProperties(el._stateStyles)
  storeBaseStyles(el, props)
}
function buildStateContext(currentState) {
  const states = {}
  for (const name of KNOWN_STATES) {
    states[name] = currentState === name
  }
  if (currentState) {
    states[currentState] = true
  }
  return states
}
function evaluateToken(token, states, currentState) {
  let negate = false
  let stateName = token
  if (stateName.startsWith('!')) {
    negate = true
    stateName = stateName.slice(1).trim()
  }
  const value = states[stateName] !== void 0 ? states[stateName] : currentState === stateName
  return negate ? !value : value
}
function hasLogicalOperators(condition) {
  return condition.includes('&&') || condition.includes('||') || condition.includes('!')
}
function evaluateCondition(condition, currentState) {
  const states = buildStateContext(currentState)
  const tokens = condition.split(/(\s*&&\s*|\s*\|\|\s*)/).filter(t => t.trim())
  let result = null
  let pendingOp = null
  for (const token of tokens) {
    const trimmed = token.trim()
    if (trimmed === '&&' || trimmed === '||') {
      pendingOp = trimmed
      continue
    }
    const value = evaluateToken(trimmed, states, currentState)
    if (result === null) {
      result = value
    } else if (pendingOp === '&&') {
      result = result && value
    } else if (pendingOp === '||') {
      result = result || value
    }
    pendingOp = null
  }
  return result === true
}
function evaluateChildVisibility(condition, state) {
  if (hasLogicalOperators(condition)) {
    try {
      return evaluateCondition(condition, state)
    } catch (err) {
      console.warn(`[Mirror] Invalid visibility condition "${condition}":`, err)
      return false
    }
  }
  return state === condition
}
function updateVisibility(el) {
  if (!el) return
  const state = el.dataset.state
  const children = el.querySelectorAll('[data-mirror-id]')
  for (const child of children) {
    if (child._visibleWhen) {
      const visible = evaluateChildVisibility(child._visibleWhen, state)
      child.style.display = visible ? '' : 'none'
    }
  }
}
function setState(el, stateName) {
  if (!el) return
  ensureBaseStyles(el)
  if (el._baseStyles) Object.assign(el.style, el._baseStyles)
  el.dataset.state = stateName
  if (stateName !== 'default' && el._stateStyles?.[stateName])
    Object.assign(el.style, el._stateStyles[stateName])
  updateVisibility(el)
}
function toggleState(el, state1, state2) {
  if (!el) return
  const fallback = state2 || 'default'
  const current = el.dataset.state || fallback
  const next = current === state1 ? fallback : state1
  setState(el, next)
}

// Visibility helpers (stamped from compiler/runtime/visibility.ts).
// show/hide/toggle reference top-level applyState/setState (above).
// close() stays as inline _runtime method (state-machine fast-path).
function show(el) {
  const target = resolveElement(el)
  if (!target) return
  target.style.display = target._savedDisplay || ''
  target.hidden = false
}
function hide(el) {
  const target = resolveElement(el)
  if (!target) return
  if (target.style.display !== 'none') {
    target._savedDisplay = target.style.display
  }
  target.style.display = 'none'
  target.hidden = true
}
function toggle(el) {
  const target = resolveElement(el)
  if (!target) return
  const STATE_PAIRS = {
    closed: 'open',
    open: 'closed',
    collapsed: 'expanded',
    expanded: 'collapsed',
  }
  const currentState = target.dataset.state || target._initialState
  const newState = STATE_PAIRS[currentState]
  if (newState) {
    setState(target, newState)
    return
  }
  target.hidden = !target.hidden
  applyState(target, target.hidden ? 'off' : 'on')
}

// Selection / highlight cluster (stamped from compiler/runtime/selection.ts).
// Helpers come first so the public API can reference them by name.
function updateBoundElements(varName, value) {
  const elements = document.querySelectorAll('[data-mirror-id]')
  for (const el of elements) {
    if (el._textBinding === varName) {
      el.textContent = value || el._textPlaceholder || ''
    }
  }
}
function updateTriggerText(container, selectedText) {
  const trigger =
    container.querySelector('[data-trigger]') ||
    container.querySelector('[data-mirror-name="Trigger"]')
  if (trigger) {
    const textEl = trigger.querySelector('span, [data-text]')
    if (textEl) {
      textEl.textContent = selectedText
    } else {
      const textNode = Array.from(trigger.childNodes).find(
        node => node.nodeType === Node.TEXT_NODE && node.textContent?.trim()
      )
      if (textNode) {
        textNode.textContent = selectedText
      }
    }
  }
}
function updateSelectionBinding(el) {
  if (!el) return
  let parent = el.parentElement
  const value = el.textContent?.trim() || ''
  while (parent) {
    if (parent._selectionBinding) {
      const varName = parent._selectionBinding
      const mirrorState = (window._mirrorState ||= {})
      mirrorState[varName] = value
      updateBoundElements(varName, value)
    }
    if (parent._triggerBinding) {
      updateTriggerText(parent, value)
    }
    parent = parent.parentElement
  }
}
function bindTriggerText(container) {
  if (!container) return
  container._triggerBinding = 'true'
}
function deselectSiblings(el) {
  if (!el.parentElement) return
  for (const sibling of el.parentElement.children) {
    if (sibling !== el && sibling.dataset.selected) {
      deselect(sibling)
    }
  }
}
function unhighlightSiblings(el) {
  if (!el.parentElement) return
  for (const sibling of el.parentElement.children) {
    if (sibling !== el && sibling.dataset.highlighted) {
      unhighlight(sibling)
    }
  }
}
function isHighlightable(el, requireHighlightState) {
  if (el._stateStyles?.highlighted) return true
  if (!requireHighlightState && el.style.cursor === 'pointer') return true
  return false
}
function findHighlightableItems(el, requireHighlightState) {
  const items = []
  for (const child of el.children) {
    const mirrorChild = child
    if (isHighlightable(mirrorChild, requireHighlightState)) {
      items.push(mirrorChild)
    } else {
      items.push(...findHighlightableItems(mirrorChild, requireHighlightState))
    }
  }
  return items
}
function getHighlightableItems(container) {
  let items = findHighlightableItems(container, true)
  if (!items.length) {
    items = findHighlightableItems(container, false)
  }
  return items
}
function findHighlightIndex(items) {
  return items.findIndex(el => el.dataset.highlighted === 'true')
}
function hasLoopFocus(container) {
  return container._loopFocus === true || container.dataset.loopFocus === 'true'
}
function select(el) {
  if (!el) return
  deselectSiblings(el)
  el.dataset.selected = 'true'
  applyState(el, 'selected')
  updateSelectionBinding(el)
}
function deselect(el) {
  if (!el) return
  delete el.dataset.selected
  removeState(el, 'selected')
}
function selectHighlighted(container) {
  if (!container) return
  const items = getHighlightableItems(container)
  const highlighted = items.find(el => el.dataset.highlighted === 'true')
  if (highlighted) select(highlighted)
}
function highlight(el) {
  if (!el) return
  unhighlightSiblings(el)
  el.dataset.highlighted = 'true'
  applyState(el, 'highlighted')
}
function unhighlight(el) {
  if (!el) return
  delete el.dataset.highlighted
  removeState(el, 'highlighted')
}
function highlightNext(container) {
  if (!container) return
  const items = getHighlightableItems(container)
  if (!items.length) return
  const current = findHighlightIndex(items)
  const loop = hasLoopFocus(container)
  let next
  if (current === -1) {
    next = 0
  } else if (loop) {
    next = (current + 1) % items.length
  } else {
    next = Math.min(current + 1, items.length - 1)
  }
  highlight(items[next])
}
function highlightPrev(container) {
  if (!container) return
  const items = getHighlightableItems(container)
  if (!items.length) return
  const current = findHighlightIndex(items)
  const loop = hasLoopFocus(container)
  let prev
  if (current === -1) {
    prev = items.length - 1
  } else if (loop) {
    prev = (current - 1 + items.length) % items.length
  } else {
    prev = Math.max(current - 1, 0)
  }
  highlight(items[prev])
}
function highlightFirst(container) {
  if (!container) return
  const items = getHighlightableItems(container)
  if (items.length) highlight(items[0])
}
function highlightLast(container) {
  if (!container) return
  const items = getHighlightableItems(container)
  if (items.length) highlight(items[items.length - 1])
}
function activate(el) {
  if (!el) return
  el.dataset.active = 'true'
  applyState(el, 'active')
}
function deactivate(el) {
  if (!el) return
  delete el.dataset.active
  removeState(el, 'active')
}
function setupTypeahead(container) {
  if (container._typeaheadEnabled) return
  container._typeaheadEnabled = true
  const state = { text: '', timeout: 0 }
  container.addEventListener('keydown', e => {
    if (e.key.length !== 1 || e.ctrlKey || e.metaKey || e.altKey) return
    const items = getHighlightableItems(container)
    if (!items.length) return
    if (state.timeout) clearTimeout(state.timeout)
    state.text += e.key.toLowerCase()
    const searchText = state.text
    const match = items.find(item => {
      const text = item.textContent?.trim().toLowerCase() || ''
      return text.startsWith(searchText)
    })
    if (match) {
      highlight(match)
      match.scrollIntoView({ block: 'nearest' })
    }
    state.timeout = window.setTimeout(() => {
      state.text = ''
    }, 500)
  })
}
function typeahead(container) {
  if (!container) return
  setupTypeahead(container)
}
function setupHoverHighlight(container) {
  if (container._hoverHighlightEnabled) return
  container._hoverHighlightEnabled = true
  container.addEventListener('mouseover', e => {
    const target = e.target
    if (!target) return
    const items = getHighlightableItems(container)
    const item = items.find(it => it === target || it.contains(target))
    if (item) highlight(item)
  })
}

const _runtime = {
  // Debug mode check
  _isDebug() {
    return typeof window !== 'undefined' && window.__MIRROR_DEBUG__ === true
  },

  // Property mapping (stamped from compiler/runtime/dom-runtime.ts → PROP_MAP)
  _propMap: {
    bg: 'background',
    col: 'color',
    pad: 'padding',
    rad: 'borderRadius',
    gap: 'gap',
    w: 'width',
    h: 'height',
    opacity: 'opacity',
  },

  // SVG Icon Constants (extracted to avoid duplication)
  _icons: {
    chevronDown: (size = 14) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"></polyline></svg>`,
    chevronUp: (size = 14) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 15 12 9 18 15"></polyline></svg>`,
    chevronLeft: (size = 16) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>`,
    chevronRight: (size = 16) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>`,
    chevronDouble: (size = 12) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 15l5 5 5-5M7 9l5-5 5 5"/></svg>`,
    check: (size = 14, strokeWidth = 2) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="${strokeWidth}" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>`,
    x: (size = 14) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>`,
    minus: (size = 12) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><line x1="5" y1="12" x2="19" y2="12"/></svg>`,
    calendar: (size = 16) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>`,
    user: (size = 24) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>`,
    upload: (size = 40) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="17 8 12 3 7 8"></polyline><line x1="12" y1="3" x2="12" y2="15"></line></svg>`,
    file: (size = 20) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path><polyline points="14 2 14 8 20 8"></polyline></svg>`,
    eyeOff: (size = 20) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"></path><line x1="1" y1="1" x2="23" y2="23"></line></svg>`,
    eye: (size = 20) =>
      `<svg width="${size}" height="${size}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path><circle cx="12" cy="12" r="3"></circle></svg>`,
  },

  // Alignment helpers
  _alignToCSS(el, prop, value) {
    const dir = el.style.flexDirection || 'column'
    const isRow = dir === 'row'
    const alignMap = {
      left: 'flex-start',
      right: 'flex-end',
      center: 'center',
      top: 'flex-start',
      bottom: 'flex-end',
    }
    const cssVal = alignMap[value] || value

    if (prop === 'align' || prop === 'hor-align') {
      if (isRow) {
        el.style.justifyContent = cssVal
      } else {
        el.style.alignItems = cssVal
      }
    } else if (prop === 'ver-align') {
      if (isRow) {
        el.style.alignItems = cssVal
      } else {
        el.style.justifyContent = cssVal
      }
    }
  },

  _getAlign(el, prop) {
    const dir = el.style.flexDirection || 'column'
    const isRow = dir === 'row'
    const reverseMap = { 'flex-start': 'left', 'flex-end': 'right', center: 'center' }

    if (prop === 'align' || prop === 'hor-align') {
      const val = isRow ? el.style.justifyContent : el.style.alignItems
      return reverseMap[val] || val
    } else if (prop === 'ver-align') {
      const val = isRow ? el.style.alignItems : el.style.justifyContent
      const vertMap = { 'flex-start': 'top', 'flex-end': 'bottom', center: 'center' }
      return vertMap[val] || val
    }
  },

  // Element wrapper
  wrap(el) {
    if (!el) return null
    const self = this
    return {
      _el: el,
      get text() {
        return el.textContent
      },
      set text(v) {
        el.textContent = v
      },
      get value() {
        return el.value
      },
      set value(v) {
        el.value = v
      },
      get visible() {
        return el.style.display !== 'none'
      },
      set visible(v) {
        el.style.display = v ? '' : 'none'
      },
      get hidden() {
        return el.hidden
      },
      set hidden(v) {
        el.hidden = v
        el.style.display = v ? 'none' : ''
      },
      get align() {
        return self._getAlign(el, 'align')
      },
      set align(v) {
        self._alignToCSS(el, 'align', v)
      },
      get verAlign() {
        return self._getAlign(el, 'ver-align')
      },
      set verAlign(v) {
        self._alignToCSS(el, 'ver-align', v)
      },
      get bg() {
        return el.style.background
      },
      set bg(v) {
        el.style.background = v
      },
      get col() {
        return el.style.color
      },
      set col(v) {
        el.style.color = v
      },
      get pad() {
        return el.style.padding
      },
      set pad(v) {
        el.style.padding = typeof v === 'number' ? v + 'px' : v
      },
      get gap() {
        return el.style.gap
      },
      set gap(v) {
        el.style.gap = typeof v === 'number' ? v + 'px' : v
      },
      get rad() {
        return el.style.borderRadius
      },
      set rad(v) {
        el.style.borderRadius = typeof v === 'number' ? v + 'px' : v
      },
      get w() {
        return el.style.width
      },
      set w(v) {
        el.style.width = typeof v === 'number' ? v + 'px' : v
      },
      get h() {
        return el.style.height
      },
      set h(v) {
        el.style.height = typeof v === 'number' ? v + 'px' : v
      },
      get opacity() {
        return el.style.opacity
      },
      set opacity(v) {
        el.style.opacity = v
      },
      get state() {
        return el.dataset.state || 'default'
      },
      set state(v) {
        self.setState(el, v)
      },
      set onclick(fn) {
        el.addEventListener('click', fn)
      },
      set onchange(fn) {
        el.addEventListener('change', fn)
      },
      addClass(c) {
        el.classList.add(c)
      },
      removeClass(c) {
        el.classList.remove(c)
      },
      toggleClass(c) {
        el.classList.toggle(c)
      },
      setStyle(prop, val) {
        el.style[prop] = val
      },
      getStyle(prop) {
        return el.style[prop]
      },
    }
  },

  // show/hide/toggle stamped from compiler/runtime/visibility.ts (top-level)
  show,
  hide,
  toggle,

  close(el) {
    if (!el) return
    const initialState = el._initialState
    const currentState = el.dataset.state
    const hasStateMachine = el._stateMachine

    // Check if element has open/closed states in its state machine
    if (hasStateMachine && (hasStateMachine.states?.open || hasStateMachine.states?.closed)) {
      this.transitionTo(el, 'default')
    } else if (
      initialState === 'closed' ||
      initialState === 'open' ||
      currentState === 'open' ||
      currentState === 'closed'
    ) {
      this.setState(el, 'closed')
    } else if (
      initialState === 'expanded' ||
      initialState === 'collapsed' ||
      currentState === 'expanded' ||
      currentState === 'collapsed'
    ) {
      this.setState(el, 'collapsed')
    } else {
      // Falls through to the top-level stamped hide() function
      // (lexical scope picks it up, no method-on-_runtime lookup).
      hide(el)
    }
  },

  // ============================================
  // POSITIONING FUNCTIONS (Built-in)
  // ============================================

  showBelow(el, trigger, offset = 4) {
    if (typeof el === 'string') {
      el = document.querySelector('[data-mirror-name="' + el + '"]')
    }
    if (!el || !trigger) return

    const rect = trigger.getBoundingClientRect()
    el.style.position = 'fixed'
    el.style.top = rect.bottom + offset + 'px'
    el.style.left = rect.left + 'px'
    el.style.zIndex = '1000'
    el.hidden = false
    el.style.display = el._savedDisplay || ''
  },

  showAbove(el, trigger, offset = 4) {
    if (typeof el === 'string') {
      el = document.querySelector('[data-mirror-name="' + el + '"]')
    }
    if (!el || !trigger) return

    const rect = trigger.getBoundingClientRect()
    const elRect = el.getBoundingClientRect()
    el.style.position = 'fixed'
    el.style.top = rect.top - elRect.height - offset + 'px'
    el.style.left = rect.left + 'px'
    el.style.zIndex = '1000'
    el.hidden = false
    el.style.display = el._savedDisplay || ''
  },

  showLeft(el, trigger, offset = 4) {
    if (typeof el === 'string') {
      el = document.querySelector('[data-mirror-name="' + el + '"]')
    }
    if (!el || !trigger) return

    const rect = trigger.getBoundingClientRect()
    const elRect = el.getBoundingClientRect()
    el.style.position = 'fixed'
    el.style.top = rect.top + 'px'
    el.style.left = rect.left - elRect.width - offset + 'px'
    el.style.zIndex = '1000'
    el.hidden = false
    el.style.display = el._savedDisplay || ''
  },

  showRight(el, trigger, offset = 4) {
    if (typeof el === 'string') {
      el = document.querySelector('[data-mirror-name="' + el + '"]')
    }
    if (!el || !trigger) return

    const rect = trigger.getBoundingClientRect()
    el.style.position = 'fixed'
    el.style.top = rect.top + 'px'
    el.style.left = rect.right + offset + 'px'
    el.style.zIndex = '1000'
    el.hidden = false
    el.style.display = el._savedDisplay || ''
  },

  showAt(el, trigger, position = 'below', offset = 4) {
    switch (position) {
      case 'below':
        return this.showBelow(el, trigger, offset)
      case 'above':
        return this.showAbove(el, trigger, offset)
      case 'left':
        return this.showLeft(el, trigger, offset)
      case 'right':
        return this.showRight(el, trigger, offset)
      default:
        return this.showBelow(el, trigger, offset)
    }
  },

  showModal(el, backdrop = true) {
    if (typeof el === 'string') {
      el = document.querySelector('[data-mirror-name="' + el + '"]')
    }
    if (!el) return

    // Create backdrop if requested
    if (backdrop) {
      const bd = document.createElement('div')
      bd.dataset.mirrorBackdrop = 'true'
      bd.style.cssText =
        'position:fixed;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,0.5);z-index:999'
      document.body.appendChild(bd)
    }

    el.style.position = 'fixed'
    el.style.top = '50%'
    el.style.left = '50%'
    el.style.transform = 'translate(-50%, -50%)'
    el.style.zIndex = '1000'
    el.hidden = false
    el.style.display = el._savedDisplay || ''
  },

  dismiss(el) {
    if (typeof el === 'string') {
      el = document.querySelector('[data-mirror-name="' + el + '"]')
    }
    if (!el) return

    el.hidden = true
    el.style.display = 'none'
    el.style.position = ''
    el.style.top = ''
    el.style.left = ''
    el.style.transform = ''
    el.style.zIndex = ''

    // Remove backdrop if exists
    const backdrop = document.querySelector('[data-mirror-backdrop]')
    if (backdrop) backdrop.remove()
  },

  // ============================================
  // SCROLL FUNCTIONS (Built-in)
  // ============================================

  // scrollTo / scrollBy / scrollToTop / scrollToBottom are stamped from
  // compiler/runtime/scroll.ts as top-level functions; expose them on
  // _runtime via shorthand property assignment so the emitter's
  // _runtime.scrollTo(...) call sites keep working — the typed unit
  // tests in tests/integration/builtin-scroll.test.ts now exercise the
  // exact same code that ships in production.
  scrollTo,
  scrollBy,
  scrollToTop,
  scrollToBottom,

  // ============================================
  // FEEDBACK: Toast Notifications
  // ============================================

  // toast / dismissToast are stamped from compiler/runtime/toast.ts
  // (see createToastModule call above the _runtime declaration).
  // Compiler emits toast(message, { type, position }) — the typed
  // function honours that signature; the previous inline impl took
  // positional args and silently dropped the options object.
  toast,
  dismissToast,

  // ============================================
  // INPUT CONTROL: Focus, Clear, Error
  // ============================================
  // Stamped from compiler/runtime/input-control.ts. Each accepts
  // string|element via resolveElement. setError/clearError now use
  // dataset.state directly (no state-machine roundtrip), matching
  // what production has always actually shipped.
  focus,
  blur,
  clear,
  selectText,
  setError,
  clearError,

  // ============================================
  // NAVIGATION: Back, Forward, OpenUrl
  // ============================================

  back() {
    window.history.back()
  },

  forward() {
    window.history.forward()
  },

  openUrl(url, options = {}) {
    if (!url) return
    const { newTab = true } = options
    // Ensure URL has a protocol
    const fullUrl =
      url.startsWith('http://') || url.startsWith('https://') || url.startsWith('mailto:')
        ? url
        : 'https://' + url
    const target = newTab ? '_blank' : '_self'
    window.open(fullUrl, target, newTab ? 'noopener,noreferrer' : undefined)
  },

  // ============================================
  // KEYBOARD NAVIGATION (Accessibility)
  // ============================================

  /**
   * Setup keyboard navigation for a form container
   * - Enter moves to next field (or submits if last)
   * - Escape blurs current field
   * - Tab cycles through fields normally
   */
  setupKeyboardNav(container) {
    if (!container) return
    if (container._keyboardNavSetup) return // Already setup
    container._keyboardNavSetup = true

    // Selector for form inputs
    const inputSelector =
      'input:not([type="hidden"]):not([disabled]), select:not([disabled]), textarea:not([disabled])'

    // Get ordered list of inputs
    const getInputs = () => {
      const inputs = Array.from(container.querySelectorAll(inputSelector))
      // Sort by tabindex if present
      return inputs.sort((a, b) => {
        const aTab = parseInt(a.getAttribute('tabindex') || '0', 10)
        const bTab = parseInt(b.getAttribute('tabindex') || '0', 10)
        return aTab - bTab
      })
    }

    container.addEventListener('keydown', e => {
      const target = e.target
      if (!target.matches || !target.matches(inputSelector)) return

      if (e.key === 'Enter' && target.tagName !== 'TEXTAREA') {
        e.preventDefault()
        const inputs = getInputs()
        const currentIndex = inputs.indexOf(target)

        if (currentIndex < inputs.length - 1) {
          // Move to next input
          inputs[currentIndex + 1].focus()
        } else {
          // Last input - look for submit button
          const submitBtn = container.querySelector('button[type="submit"], button:not([type])')
          if (submitBtn) {
            submitBtn.click()
          }
        }
      } else if (e.key === 'Escape') {
        target.blur()
      }
    })
  },

  // ============================================
  // TYPEAHEAD + HOVER (Accessibility / mouse UX)
  // ============================================
  // setupTypeahead / typeahead / setupHoverHighlight stamped from
  // compiler/runtime/selection.ts (top-level above). All three now
  // share the canonical getHighlightableItems / highlight / removeState
  // implementations stamped above instead of duplicating findItems
  // inline (the inline copies became dead code).
  setupTypeahead,
  typeahead,
  setupHoverHighlight,

  // Bind trigger text to show selected value (stamped from selection.ts).
  bindTriggerText,

  // ============================================
  // TABLE FUNCTIONS (Built-in)
  // ============================================

  /**
   * Sort table by field
   * Toggles between ascending and descending on repeated clicks
   */
  tableSort(table, field) {
    if (!table) return

    // Initialize or get sort state
    const state = table._sortState || { field: null, desc: false }

    // Toggle direction if same field, otherwise reset to ascending
    if (state.field === field) {
      state.desc = !state.desc
    } else {
      state.field = field
      state.desc = false
    }
    table._sortState = state

    // Update sort icons visual state
    table.querySelectorAll('.mirror-sort-icon').forEach(icon => {
      const isActive = icon.dataset.field === field
      icon.style.opacity = isActive ? '1' : '0.5'

      // Check for custom icons
      if (icon.dataset.hasCustomIcons === 'true') {
        const ascIcon = icon.querySelector('.mirror-sort-asc')
        const descIcon = icon.querySelector('.mirror-sort-desc')
        const defaultIcon = icon.querySelector('.mirror-sort-default')

        if (isActive) {
          if (defaultIcon) defaultIcon.style.display = 'none'
          if (state.desc) {
            if (ascIcon) ascIcon.style.display = 'none'
            if (descIcon) descIcon.style.display = ''
          } else {
            if (ascIcon) ascIcon.style.display = ''
            if (descIcon) descIcon.style.display = 'none'
          }
        } else {
          // Reset to default
          if (ascIcon) ascIcon.style.display = 'none'
          if (descIcon) descIcon.style.display = 'none'
          if (defaultIcon) defaultIcon.style.display = ''
        }
      } else {
        // Update icon direction with default SVGs
        if (isActive) {
          if (state.desc) {
            icon.innerHTML =
              '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 10l5 5 5-5"/></svg>'
          } else {
            icon.innerHTML =
              '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 14l5-5 5 5"/></svg>'
          }
        } else {
          // Reset to default double chevron
          icon.innerHTML =
            '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M7 15l5 5 5-5M7 9l5-5 5 5"/></svg>'
        }
      }
    })

    // Re-sort the table body
    const body = table.querySelector('.mirror-table-body')
    if (!body) return

    const rows = Array.from(body.querySelectorAll('.mirror-table-row'))
    rows.sort((a, b) => {
      const aVal =
        a.dataset[field] || a.querySelector('[data-field="' + field + '"]')?.textContent || ''
      const bVal =
        b.dataset[field] || b.querySelector('[data-field="' + field + '"]')?.textContent || ''

      // Try numeric comparison first
      const aNum = parseFloat(aVal)
      const bNum = parseFloat(bVal)

      let cmp
      if (!isNaN(aNum) && !isNaN(bNum)) {
        cmp = aNum - bNum
      } else {
        cmp = aVal.localeCompare(bVal)
      }

      return state.desc ? -cmp : cmp
    })

    // Re-append sorted rows
    rows.forEach(row => body.appendChild(row))
  },

  /**
   * Go to previous page in paginated table
   */
  tablePrev(table) {
    if (!table || !table._pageState) return

    const state = table._pageState
    if (state.current > 1) {
      state.current--
      this._updateTablePage(table)
    }
  },

  /**
   * Go to next page in paginated table
   */
  tableNext(table) {
    if (!table || !table._pageState) return

    const state = table._pageState
    const totalPages = Math.ceil(state.total / state.size)
    if (state.current < totalPages) {
      state.current++
      this._updateTablePage(table)
    }
  },

  /**
   * Update table to show current page
   */
  _updateTablePage(table) {
    if (!table || !table._pageState) return

    const state = table._pageState
    const body = table.querySelector('.mirror-table-body')
    if (!body) return

    const rows = Array.from(body.querySelectorAll('.mirror-table-row'))
    const start = (state.current - 1) * state.size
    const end = start + state.size

    rows.forEach((row, index) => {
      row.style.display = index >= start && index < end ? '' : 'none'
    })

    // Update page info
    const totalPages = Math.ceil(state.total / state.size)
    const pageInfo = table.querySelector('.mirror-paginator-info')
    if (pageInfo) {
      pageInfo.textContent = 'Page ' + state.current + ' of ' + totalPages
    }
  },

  // ============================================
  // VALUE FUNCTIONS (Built-in)
  // ============================================

  // Token storage
  _tokens: {},
  _initialTokens: {},
  _tokenWatchers: [],

  registerToken(name, value) {
    this._tokens[name] = value
    this._initialTokens[name] = value
  },

  watchToken(name, el, prop) {
    this._tokenWatchers.push({ name, el, prop })
  },

  _notifyTokenWatchers(name) {
    this._tokenWatchers.forEach(w => {
      if (w.name === name) {
        if (w.prop === 'textContent') {
          w.el.textContent = this._tokens[name]
        } else {
          w.el[w.prop] = this._tokens[name]
        }
      }
    })
  },

  get(name) {
    // Read from __mirrorData for consistency with $get()
    if (window.__mirrorData && name in window.__mirrorData) {
      return window.__mirrorData[name]
    }
    return this._tokens[name]
  },

  set(name, value) {
    this._tokens[name] = value
    // Also update __mirrorData so $get() returns new value
    if (window.__mirrorData) {
      window.__mirrorData[name] = value
    }
    this._notifyTokenWatchers(name)
    // Notify text bindings to re-render
    this.notifyDataChange && this.notifyDataChange(name, value)
  },

  increment(name, options = {}) {
    // Read from __mirrorData for consistency
    const current =
      window.__mirrorData && name in window.__mirrorData
        ? typeof window.__mirrorData[name] === 'number'
          ? window.__mirrorData[name]
          : 0
        : this._tokens[name] || 0
    const step = options.step || 1
    let newVal = current + step
    if (options.max !== undefined && newVal > options.max) {
      newVal = options.max
    }
    this._tokens[name] = newVal
    // Also update __mirrorData so $get() returns new value
    if (window.__mirrorData) {
      window.__mirrorData[name] = newVal
    }
    this._notifyTokenWatchers(name)
    // Notify text bindings to re-render
    this.notifyDataChange && this.notifyDataChange(name, newVal)
  },

  decrement(name, options = {}) {
    // Read from __mirrorData for consistency
    const current =
      window.__mirrorData && name in window.__mirrorData
        ? typeof window.__mirrorData[name] === 'number'
          ? window.__mirrorData[name]
          : 0
        : this._tokens[name] || 0
    const step = options.step || 1
    let newVal = current - step
    if (options.min !== undefined && newVal < options.min) {
      newVal = options.min
    }
    this._tokens[name] = newVal
    // Also update __mirrorData so $get() returns new value
    if (window.__mirrorData) {
      window.__mirrorData[name] = newVal
    }
    this._notifyTokenWatchers(name)
    // Notify text bindings to re-render
    this.notifyDataChange && this.notifyDataChange(name, newVal)
  },

  reset(name, initialValue) {
    let newVal
    if (initialValue !== undefined) {
      newVal = initialValue
    } else {
      newVal = this._initialTokens[name]
    }
    this._tokens[name] = newVal
    // Also update __mirrorData so $get() returns new value
    if (window.__mirrorData) {
      window.__mirrorData[name] = newVal
    }
    this._notifyTokenWatchers(name)
    // Notify text bindings to re-render
    this.notifyDataChange && this.notifyDataChange(name, newVal)
  },

  // ============================================
  // CRUD OPERATIONS
  // ============================================

  // Generate unique ID for new entries
  _generateId(prefix = 'item') {
    return prefix + '_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9)
  },

  // Add new entry to collection
  add(collectionName, values) {
    const name = collectionName.startsWith('$') ? collectionName.slice(1) : collectionName
    const data = window.__mirrorData || {}

    if (!data[name] || typeof data[name] !== 'object') {
      data[name] = {}
    }

    const collection = data[name]
    const key = this._generateId(name.slice(0, 3))

    // Use provided values or create empty entry with _key
    collection[key] = values || { _key: key }
    if (!collection[key]._key) {
      collection[key]._key = key
    }

    this._refreshEachLoops(name)
    return key
  },

  // Remove entry from collection
  remove(itemOrCollection, key) {
    let collectionName, entryKey

    if (typeof itemOrCollection === 'string') {
      // Called as remove('collectionName', 'key')
      collectionName = itemOrCollection.startsWith('$')
        ? itemOrCollection.slice(1)
        : itemOrCollection
      entryKey = key
    } else if (itemOrCollection && typeof itemOrCollection === 'object') {
      // Called as remove(item) - find collection by _key
      if (typeof itemOrCollection._key !== 'string') {
        console.warn('[Mirror] remove() requires item with _key property')
        return
      }
      entryKey = itemOrCollection._key

      // Find which collection contains this key
      const data = window.__mirrorData || {}
      const found = Object.entries(data).find(([, val]) => {
        if (val && typeof val === 'object' && !Array.isArray(val)) {
          return val[entryKey] !== undefined
        }
        return false
      })

      if (!found) {
        console.warn('[Mirror] remove() could not find collection for item')
        return
      }
      collectionName = found[0]
    } else {
      return
    }

    const data = window.__mirrorData || {}
    const collection = data[collectionName]

    if (!collection || typeof collection !== 'object') {
      return
    }

    delete collection[entryKey]
    this._refreshEachLoops(collectionName)
  },

  // Create new entry in collection
  create(collectionName, initialValues = {}) {
    const name = collectionName.startsWith('$') ? collectionName.slice(1) : collectionName
    const data = window.__mirrorData || {}
    const collection = data[name]

    if (!collection || typeof collection !== 'object' || Array.isArray(collection)) {
      console.warn('[Mirror] create() - collection not found:', name)
      return null
    }

    // Generate unique key
    const newKey = 'new_' + Date.now() + '_' + Math.random().toString(36).slice(2, 9)

    // Get template from existing entry
    const entries = Object.values(collection)
    const template = entries[0]
    const newEntry = { _key: newKey }

    // Copy structure from template
    if (template && typeof template === 'object') {
      for (const key of Object.keys(template)) {
        if (key !== '_key') {
          const val = template[key]
          if (typeof val === 'string') newEntry[key] = ''
          else if (typeof val === 'number') newEntry[key] = 0
          else if (typeof val === 'boolean') newEntry[key] = false
          else newEntry[key] = null
        }
      }
    }

    // Apply initial values
    Object.assign(newEntry, initialValues)

    // Add to collection
    collection[newKey] = newEntry
    this._refreshEachLoops(name)

    return newEntry
  },

  // Save (dispatch event for persistence)
  save(target) {
    const data = window.__mirrorData || {}
    if (this._isDebug()) console.log('[Mirror] save()', target || '', data)
    window.dispatchEvent(new CustomEvent('mirror:save', { detail: { target, data } }))
  },

  // Delete item (alias for remove, avoids reserved word)
  deleteItem(itemOrKey) {
    this.remove(itemOrKey)
  },

  // Revert changes (dispatch event)
  revert(target) {
    if (this._isDebug()) console.log('[Mirror] revert()', target || '')
    window.dispatchEvent(new CustomEvent('mirror:revert', { detail: { target } }))
  },

  // Update field in item
  updateField(item, field, value) {
    if (!item || typeof item._key !== 'string') {
      return
    }

    item[field] = value

    // Also update in __mirrorData
    const data = window.__mirrorData || {}
    const entryKey = item._key

    for (const [collName, colData] of Object.entries(data)) {
      if (colData && typeof colData === 'object' && !Array.isArray(colData)) {
        if (colData[entryKey]) {
          colData[entryKey][field] = value
          return
        }
      }
    }
  },

  // Re-render each loops for a collection after data changes
  _refreshEachLoops(collectionName) {
    const containers = document.querySelectorAll('[data-each-container]')

    containers.forEach(container => {
      const config = container._eachConfig
      if (!config) return
      if (config.collection !== collectionName) return

      // Clear and re-render
      container.innerHTML = ''

      const data = window.__mirrorData || {}
      let items = data[collectionName]

      if (!items) return

      // Convert object to array with _key
      if (!Array.isArray(items)) {
        items = Object.entries(items).map(([k, v]) => {
          if (typeof v === 'object' && v !== null) {
            return { _key: k, ...v }
          }
          return v
        })
      }

      // Apply filter if configured
      if (config.filterFn) {
        items = items.filter(config.filterFn)
      }

      // Apply sort if configured
      if (config.orderBy) {
        const dir = config.orderDesc ? -1 : 1
        items = [...items].sort((a, b) => {
          const aVal = a[config.orderBy]
          const bVal = b[config.orderBy]
          if (aVal < bVal) return -dir
          if (aVal > bVal) return dir
          return 0
        })
      }

      // Render each item
      items.forEach((item, index) => {
        const itemEl = config.renderItem(item, index)
        container.appendChild(itemEl)
      })
    })
  },

  // ============================================
  // TWO-WAY DATA BINDING
  // ============================================

  // Bindings Registry: path → Set of bound elements
  _inputBindings: new Map(),
  _textBindings: new Map(),
  _visibilityBindings: new Map(), // path → Set of elements with _visibleWhen depending on this path

  // Register element for two-way value binding (Input, Textarea)
  bindValue(el, path) {
    if (!el || !path) return
    el._valueBinding = path
    if (!this._inputBindings.has(path)) {
      this._inputBindings.set(path, new Set())
    }
    this._inputBindings.get(path).add(el)
  },

  // ============================================
  // INPUT MASK
  // ============================================

  // Parse mask pattern into char types
  _parseMask(pattern) {
    return pattern.split('').map(c => {
      if (c === '#') return { type: 'digit', char: c }
      if (c === 'A') return { type: 'letter', char: c }
      if (c === '*') return { type: 'alphanum', char: c }
      return { type: 'literal', char: c }
    })
  },

  // Check if char is valid for mask type
  _isMaskCharValid(char, type) {
    if (type === 'digit') return /\d/.test(char)
    if (type === 'letter') return /[a-zA-Z]/.test(char)
    if (type === 'alphanum') return /[a-zA-Z0-9]/.test(char)
    return false
  },

  // Format value with mask pattern
  formatWithMask(value, pattern) {
    const mask = this._parseMask(pattern)
    const raw = String(value || '')
      .replace(/[^a-zA-Z0-9]/g, '')
      .split('')
    let result = '',
      ri = 0

    for (const m of mask) {
      if (ri >= raw.length) break
      if (m.type === 'literal') {
        result += m.char
      } else {
        while (ri < raw.length && !this._isMaskCharValid(raw[ri], m.type)) ri++
        if (ri < raw.length) result += raw[ri++]
      }
    }
    return result
  },

  // Get raw value from masked input (extracts all alphanumeric chars)
  getMaskRawValue(input) {
    const pattern = input._maskPattern
    if (!pattern) return input.value
    // Extract all alphanumeric characters, regardless of current formatting
    return (input.value || '').replace(/[^a-zA-Z0-9]/g, '')
  },

  // Adjust cursor position after formatting
  _adjustMaskCursor(oldVal, newVal, oldPos, pattern) {
    const mask = this._parseMask(pattern)
    let inputChars = 0
    for (let i = 0; i < oldPos && i < mask.length; i++) {
      if (mask[i].type !== 'literal') inputChars++
    }
    let newPos = 0,
      counted = 0
    for (let i = 0; i < newVal.length && i < mask.length; i++) {
      newPos = i + 1
      if (mask[i].type !== 'literal') {
        counted++
        if (counted >= inputChars) break
      }
    }
    while (newPos < mask.length && mask[newPos] && mask[newPos].type === 'literal') newPos++
    return Math.min(newPos, newVal.length)
  },

  // Apply mask to input element
  applyMask(input, pattern) {
    const self = this
    input._maskPattern = pattern

    // Format initial value
    if (input.value) {
      input.value = self.formatWithMask(input.value, pattern)
    }

    // Handle input events
    input.addEventListener('input', () => {
      const pos = input.selectionStart || 0
      const old = input.value
      const raw = self.getMaskRawValue(input)
      const formatted = self.formatWithMask(raw, pattern)

      if (formatted !== old) {
        input.value = formatted
        const newPos = self._adjustMaskCursor(old, formatted, pos, pattern)
        input.setSelectionRange(newPos, newPos)
      }
    })

    // Validate keypress
    input.addEventListener('keypress', e => {
      if (e.ctrlKey || e.metaKey || e.key.length > 1) return
      const pos = input.selectionStart || 0
      const mask = self._parseMask(pattern)

      let maskIdx = 0
      for (let i = 0; i < pos && maskIdx < mask.length; i++) {
        maskIdx++
      }
      while (maskIdx < mask.length && mask[maskIdx].type === 'literal') maskIdx++

      if (maskIdx >= mask.length || !self._isMaskCharValid(e.key, mask[maskIdx].type)) {
        e.preventDefault()
      }
    })

    // Handle paste
    input.addEventListener('paste', e => {
      e.preventDefault()
      const data = (e.clipboardData && e.clipboardData.getData('text')) || ''
      input.value = self.formatWithMask(data, pattern)
      input.dispatchEvent(new Event('input', { bubbles: true }))
    })
  },

  // Register element for text binding (Text elements that display data)
  bindText(el, path, expression) {
    if (!el || !path) return
    el._textBinding = path
    el._textExpression = expression // For complex expressions like "Hello, " + $name
    if (!this._textBindings.has(path)) {
      this._textBindings.set(path, new Set())
    }
    this._textBindings.get(path).add(el)
  },

  // Register element for visibility binding (elements with _visibleWhen that depends on data)
  bindVisibility(el, path) {
    if (!el || !path) return
    // Store all paths this element depends on
    if (!el._visibilityPaths) {
      el._visibilityPaths = []
    }
    el._visibilityPaths.push(path)
    // Register in the bindings map
    if (!this._visibilityBindings.has(path)) {
      this._visibilityBindings.set(path, new Set())
    }
    this._visibilityBindings.get(path).add(el)
  },

  // Evaluate a visibility condition using $get
  // Supports both $varName and bare varName formats
  _evaluateVisibilityCondition(condition) {
    try {
      const reserved = new Set([
        'true',
        'false',
        'null',
        'undefined',
        'NaN',
        'Infinity',
        'typeof',
        'instanceof',
        'new',
        'delete',
        'void',
      ])

      // First handle $-prefixed variables: $varName → $get("varName")
      let evalCondition = condition.replace(/\$([a-zA-Z_][a-zA-Z0-9_.]*)/g, '$get("$1")')

      // Then handle bare identifiers (not already wrapped, not reserved)
      evalCondition = evalCondition.replace(
        /(?<!["\w$.])(\b[a-zA-Z_][a-zA-Z0-9_]*(?:\.[a-zA-Z_][a-zA-Z0-9_]*)*\b)(?!["\w(])/g,
        (match, identifier) => {
          const firstPart = identifier.split('.')[0]
          if (reserved.has(firstPart)) return match
          return '$get("' + identifier + '")'
        }
      )

      // Use Function constructor to evaluate in the context with $get
      const result = new Function('$get', 'return ' + evalCondition)($get)
      return !!result
    } catch (e) {
      return false
    }
  },

  // Set data value and notify all bound elements
  notifyDataChange(path, value) {
    // Collect all paths to update: the exact path AND all sub-paths (e.g., selectedAddress.firstName)
    const pathsToUpdate = [path]
    const pathPrefix = path + '.'

    // Find all registered paths that are sub-paths of the changed path
    for (const registeredPath of this._textBindings.keys()) {
      if (registeredPath.startsWith(pathPrefix)) {
        pathsToUpdate.push(registeredPath)
      }
    }
    for (const registeredPath of this._inputBindings.keys()) {
      if (registeredPath.startsWith(pathPrefix) && !pathsToUpdate.includes(registeredPath)) {
        pathsToUpdate.push(registeredPath)
      }
    }
    for (const registeredPath of this._visibilityBindings.keys()) {
      if (registeredPath.startsWith(pathPrefix) && !pathsToUpdate.includes(registeredPath)) {
        pathsToUpdate.push(registeredPath)
      }
    }

    // Update all affected paths
    for (const updatePath of pathsToUpdate) {
      // Update other inputs (skip active element to avoid cursor jumping)
      const inputElements = this._inputBindings.get(updatePath)
      if (inputElements) {
        for (const el of inputElements) {
          if (el !== document.activeElement) {
            el.value = value ?? ''
          }
        }
      }

      // Update text elements
      const textElements = this._textBindings.get(updatePath)
      if (textElements) {
        for (const el of textElements) {
          if (el._textTemplate) {
            // Re-evaluate expression template with new data
            try {
              const result = el._textTemplate()
              el.innerHTML = formatInlineMarkdown(result ?? '')
            } catch (e) {
              el.innerHTML = formatInlineMarkdown(value ?? '')
            }
          } else {
            el.innerHTML = formatInlineMarkdown(value ?? '')
          }
        }
      }

      // Update visibility of elements with _visibleWhen that depend on this path
      const visElements = this._visibilityBindings.get(updatePath)
      if (visElements) {
        for (const el of visElements) {
          if (el._visibleWhen) {
            const visible = this._evaluateVisibilityCondition(el._visibleWhen)
            el.style.display = visible ? '' : 'none'
          }
        }
      }
    }

    // Refresh all each loops as their filter expressions may depend on the changed value
    this._refreshAllEachLoops()
  },

  // Refresh all each loops (for filter variable changes)
  _refreshAllEachLoops() {
    const containers = document.querySelectorAll('[data-each-container]')
    const data = window.__mirrorData || {}

    containers.forEach(container => {
      const config = container._eachConfig
      if (!config) return

      const collectionName = typeof config.collection === 'string' ? config.collection : null
      if (!collectionName) return

      // Clear existing items
      container.innerHTML = ''

      // Get fresh data
      const items = data[collectionName]
      if (!items || typeof items !== 'object') return

      // Convert to array
      let itemsArray
      if (Array.isArray(items)) {
        itemsArray = items
      } else {
        itemsArray = Object.entries(items).map(([k, v]) =>
          typeof v === 'object' && v !== null ? { _key: k, ...v } : { _key: k, value: v }
        )
      }

      // Apply filter
      if (config.filterFn) {
        itemsArray = itemsArray.filter(config.filterFn)
      }

      // Apply sorting
      if (config.orderBy) {
        const sortDir = config.orderDesc ? -1 : 1
        itemsArray = [...itemsArray].sort((a, b) => {
          const aVal = a[config.orderBy]
          const bVal = b[config.orderBy]
          if (aVal < bVal) return -sortDir
          if (aVal > bVal) return sortDir
          return 0
        })
      }

      // Render
      if (config.renderItem) {
        itemsArray.forEach((item, index) => {
          container.appendChild(config.renderItem(item, index))
        })
      }
    })
  },

  // Remove element from bindings (cleanup)
  unbindValue(el) {
    if (!el || !el._valueBinding) return
    const path = el._valueBinding
    const elements = this._inputBindings.get(path)
    if (elements) {
      elements.delete(el)
      if (elements.size === 0) {
        this._inputBindings.delete(path)
      }
    }
    delete el._valueBinding
  },

  // ============================================
  // CLIPBOARD FUNCTIONS (Built-in)
  // ============================================

  async copy(text, triggerEl) {
    const textToCopy = String(text)
    try {
      if (navigator.clipboard) {
        await navigator.clipboard.writeText(textToCopy)
      } else {
        // Fallback for older browsers
        const textarea = document.createElement('textarea')
        textarea.value = textToCopy
        textarea.style.position = 'fixed'
        textarea.style.opacity = '0'
        document.body.appendChild(textarea)
        textarea.select()
        document.execCommand('copy')
        document.body.removeChild(textarea)
      }

      // Apply feedback state to trigger element
      if (triggerEl) {
        const prevState = triggerEl.dataset.state
        triggerEl.dataset.state = 'copied'
        setTimeout(() => {
          if (prevState) {
            triggerEl.dataset.state = prevState
          } else {
            delete triggerEl.dataset.state
          }
        }, 2000)
      }
    } catch (err) {
      console.error('Copy failed:', err)
    }
  },

  // Selection + highlight cluster — stamped from compiler/runtime/selection.ts
  // (see top-level declarations above _runtime). Helpers use the
  // top-level applyState/removeState by lexical name lookup, not
  // this.applyState method dispatch.
  select,
  deselect,
  highlight,
  unhighlight,
  highlightNext,
  highlightPrev,
  highlightFirst,
  highlightLast,
  getHighlightableItems,

  selectHighlighted,

  // activate / deactivate stamped from compiler/runtime/selection.ts (top-level above).
  activate,
  deactivate,

  // State management
  // applyState / removeState stamped from compiler/runtime/state-machine.ts
  applyState,
  removeState,

  // setState / toggleState stamped from state-machine.ts (top-level above).
  setState,
  toggleState,

  // State Machine Functions (Interaction Model)
  transitionTo(el, stateName, animation) {
    if (!el?._stateMachine) return
    const sm = el._stateMachine
    const prevStateName = sm.current
    const prevState = sm.states[prevStateName]
    const newState = sm.states[stateName]
    if (!newState) return
    if (prevStateName === stateName) return
    // Store base styles on first transition
    if (!el._baseStyles) {
      el._baseStyles = {}
      const stateProps = new Set()
      for (const state of Object.values(sm.states)) {
        // styles is an object like { 'background': '#2271C1' }
        for (const prop of Object.keys(state.styles || {})) {
          stateProps.add(prop)
        }
      }
      for (const prop of stateProps) {
        el._baseStyles[prop] = el.style[prop] || ''
      }
    }
    // Store base children on first transition (if any state has children)
    // This enables states with completely different children (like Figma Variants)
    if (!el._baseChildren) {
      const hasStateWithChildren = Object.values(sm.states).some(s => s.children)
      if (hasStateWithChildren) {
        el._baseChildren = Array.from(el.children)
      }
    }
    sm.current = stateName
    el.dataset.state = stateName
    // Restore base styles before applying new state
    if (el._baseStyles) {
      Object.assign(el.style, el._baseStyles)
    }
    // Swap children if state has children defined (like Figma Variants)
    if (newState.children) {
      // Remove current children
      while (el.firstChild) {
        el.removeChild(el.firstChild)
      }
      // Add state children (created fresh via factory function)
      const stateChildren = newState.children()
      for (const child of stateChildren) {
        el.appendChild(child)
      }
    } else if (el._baseChildren && prevState?.children) {
      // Previous state had children, restore base children
      while (el.firstChild) {
        el.removeChild(el.firstChild)
      }
      // Re-create base children (clone them since originals may be reused)
      for (const child of el._baseChildren) {
        el.appendChild(child.cloneNode(true))
      }
    }
    // Temporarily disable transition to ensure immediate style application
    const originalTransition = el.style.transition
    el.style.transition = 'none'
    Object.assign(el.style, newState.styles)
    // Force synchronous style recalc
    void el.offsetHeight
    // Restore transition
    el.style.transition = originalTransition
    // Handle visibility states specially
    if (stateName === 'visible') {
      el.style.display = el._baseDisplay || 'flex'
      el.hidden = false
    } else if (prevStateName === 'visible' && sm.states['visible']) {
      el.style.display = 'none'
      el.hidden = true
    }
    this.updateVisibility(el)
    // Note: animation parameter accepted for API compatibility but not executed in string runtime
  },

  // Toggle: 1 state = binary (default ↔ state), 2+ states = cycle
  stateMachineToggle(el, stateOrder) {
    if (!el?._stateMachine) return
    const sm = el._stateMachine
    // Get custom states (exclude 'default' and CSS pseudo-states)
    // CSS pseudo-states are browser-triggered, not toggle targets
    const cssStates = ['default', 'hover', 'focus', 'active', 'disabled']
    const order = stateOrder || Object.keys(sm.states).filter(s => !cssStates.includes(s))
    if (order.length === 0) return

    if (order.length === 1) {
      // Binary toggle: default ↔ single state
      const targetState = order[0]
      if (sm.current === targetState) {
        this.transitionTo(el, 'default')
      } else {
        this.transitionTo(el, targetState)
      }
    } else {
      // Cycle through multiple states
      const currentIndex = order.indexOf(sm.current)
      const nextIndex = currentIndex === -1 ? 0 : (currentIndex + 1) % order.length
      this.transitionTo(el, order[nextIndex])
    }
  },

  exclusiveTransition(el, stateName, animation) {
    if (!el?._stateMachine) return
    // Find siblings - handle display:contents wrapper elements (from each loops)
    // Walk up until we find a real parent (not display:contents or eachItem)
    let parent = el.parentElement
    try {
      while (
        parent &&
        (parent.dataset.eachItem !== undefined ||
          (typeof getComputedStyle === 'function' &&
            getComputedStyle(parent).display === 'contents'))
      ) {
        parent = parent.parentElement
      }
    } catch (e) {
      // getComputedStyle might fail in some environments, fall back to direct parent
      parent = el.parentElement
    }
    if (parent) {
      // Find all elements with same component name (for exclusive within component type)
      // Use data-component (not data-mirror-name which gets overwritten by instance name)
      const componentName = el.dataset.component
      const siblings = componentName
        ? parent.querySelectorAll(`[data-component="${componentName}"]`)
        : parent.querySelectorAll('[data-mirror-id]')
      siblings.forEach(sibling => {
        if (sibling !== el && sibling._stateMachine) {
          const sibSm = sibling._stateMachine
          // Deselect siblings: transition to 'default' state (not initial!)
          // A sibling might have started in 'active' state, but we want to
          // deselect it by going to 'default', not back to 'active'
          if (sibSm.current !== 'default') {
            this.transitionTo(sibling, 'default')
          }
        }
      })
    }
    this.transitionTo(el, stateName)

    // Update bind variable if parent has one
    // Walk up to find container with data-bind attribute
    let container = el.parentElement
    while (container) {
      const bindVar = container.dataset?.bind
      if (bindVar) {
        // Get the loop item if available (from each loops), otherwise fall back to textContent
        // Walk up from el to find the _loopItem, checking each parent including itemContainer
        let loopItem = el._loopItem
        if (loopItem === undefined) {
          // Check parent elements (template elements might store the item on a wrapper)
          let walker = el.parentElement
          while (walker && loopItem === undefined) {
            if (walker._loopItem !== undefined) {
              loopItem = walker._loopItem
            } else if (walker.dataset?.eachItem !== undefined) {
              // Stop at eachItem container boundary
              break
            }
            walker = walker.parentElement
          }
        }
        const activeValue = loopItem !== undefined ? loopItem : el.textContent?.trim() || ''
        // Update the global state and notify bound elements
        if (typeof window !== 'undefined') {
          window._mirrorState = window._mirrorState || {}
          window._mirrorState[bindVar] = activeValue
        }
        this.notifyDataChange && this.notifyDataChange(bindVar, activeValue)
        break
      }
      container = container.parentElement
    }
  },

  // Wrapper for exclusive selection (used by template actions)
  exclusive(el, state) {
    if (!el) return
    const targetState =
      state ||
      (el._stateMachine
        ? Object.keys(el._stateMachine.states).find(s => s !== 'default')
        : 'active') ||
      'active'
    this.exclusiveTransition(el, targetState)
  },

  watchStates(el, targetState, initialState, condition, dependencies) {
    if (!el) return
    const root = el.closest('[data-mirror-root]') || document.body
    const targetElements = new Map()
    for (const dep of dependencies) {
      const targetEl = root.querySelector(`[data-mirror-name="${dep.target}"]`)
      if (targetEl) targetElements.set(dep.target, targetEl)
    }
    const checkCondition = () => {
      if (condition === 'and') {
        return dependencies.every(dep => {
          const targetEl = targetElements.get(dep.target)
          return targetEl?.dataset.state === dep.state
        })
      } else {
        return dependencies.some(dep => {
          const targetEl = targetElements.get(dep.target)
          return targetEl?.dataset.state === dep.state
        })
      }
    }
    const updateState = () => {
      if (checkCondition()) {
        this.transitionTo(el, targetState)
      } else {
        this.transitionTo(el, initialState)
      }
    }
    updateState()
    const observer = new MutationObserver(mutations => {
      for (const mutation of mutations) {
        if (mutation.attributeName === 'data-state') {
          updateState()
          break
        }
      }
    })
    for (const targetEl of targetElements.values()) {
      observer.observe(targetEl, { attributes: true, attributeFilter: ['data-state'] })
    }
  },

  // updateVisibility stamped from state-machine.ts (top-level above).
  // The inline _evaluateCondition was orphaned after this consolidation
  // (the typed updateVisibility uses evaluateChildVisibility which is
  // also stamped at top-level).
  updateVisibility,

  // Navigation
  navigate(targetName, clickedElement) {
    if (!targetName) return
    // Get the root node (Shadow DOM root or document) to support both contexts
    const root = clickedElement ? clickedElement.getRootNode() : document
    // Try data-mirror-name first (from name property), then fall back to data-component
    const target =
      root.querySelector(`[data-mirror-name="${targetName}"]`) ||
      root.querySelector(`[data-component="${targetName}"]`)
    if (!target) return
    if (target.parentElement) {
      Array.from(target.parentElement.children).forEach(sibling => {
        // Check for either data-mirror-name or data-component to identify view elements
        if (sibling.dataset && (sibling.dataset.mirrorName || sibling.dataset.component)) {
          sibling.style.display = sibling === target ? '' : 'none'
        }
      })
    }
    this.updateNavSelection(clickedElement)
  },

  updateNavSelection(clickedElement) {
    if (!clickedElement) return
    const nav = clickedElement.closest('nav')
    if (!nav) return
    const navItems = nav.querySelectorAll('[data-route]')
    navItems.forEach(item => {
      if (item === clickedElement) {
        item.dataset.selected = 'true'
        this.applyState(item, 'selected')
      } else {
        delete item.dataset.selected
        this.removeState(item, 'selected')
      }
    })
  },

  navigateToPage(pageName, clickedElement) {
    if (!pageName) return
    // Reject path-traversal / absolute-path / weird-character page names
    // BEFORE constructing a filename. sanitizePageName is stamped from
    // compiler/runtime/component-navigation.ts.
    const safePage = sanitizePageName(pageName)
    if (!safePage) return
    const filename = safePage.endsWith('.mirror') ? safePage : safePage + '.mirror'
    const readFile = this._readFile || window._mirrorReadFile
    if (!readFile) {
      console.warn('No readFile callback available for page navigation')
      return
    }
    const content = readFile(filename)
    if (!content) {
      console.warn(`Page not found: ${filename}`)
      return
    }
    if (typeof Mirror === 'undefined' || !Mirror.compile) {
      console.warn('Mirror compiler not available for dynamic page loading')
      return
    }
    try {
      const pageCode = Mirror.compile(content, { readFile })
      const container = this.getPageContainer()
      if (!container) {
        console.warn('No page container found for rendering')
        return
      }
      container.innerHTML = ''
      const execCode = pageCode.replace('export function createUI', 'function createUI')
      const fn = new Function(execCode + '\nreturn createUI();')
      const ui = fn()
      if (ui && ui.root) {
        while (ui.root.firstChild) {
          container.appendChild(ui.root.firstChild)
        }
      }
    } catch (err) {
      console.error(`Failed to load page ${filename}:`, err)
    }
    this.updateNavSelection(clickedElement)
  },

  getPageContainer() {
    let container = document.querySelector('[data-page-container]')
    if (container) return container
    container = document.querySelector('[data-instance-name="PageContent"]')
    if (container) return container
    container = document.querySelector('[data-instance-name="Content"]')
    if (container) return container
    const nav = document.querySelector('nav')
    if (nav && nav.parentElement) {
      for (const sibling of nav.parentElement.children) {
        if (sibling !== nav && sibling.tagName !== 'NAV') {
          return sibling
        }
      }
    }
    return null
  },

  // Selection binding + trigger-text + bound-elements update —
  // stamped from compiler/runtime/selection.ts.
  updateSelectionBinding,
  updateTriggerText,
  updateBoundElements,

  // Cleanup
  destroy(el) {
    if (!el) return
    if (el._clickOutsideHandler) {
      document.removeEventListener('click', el._clickOutsideHandler)
      delete el._clickOutsideHandler
    }
    if (el.children) {
      Array.from(el.children).forEach(child => this.destroy(child))
    }
  },

  // ========================================
  // DatePicker Component
  // ========================================
  initDatePickerComponent(el) {
    if (!el || !el._zagConfig) return

    const config = el._zagConfig

    // Create missing elements if needed (robust initialization)
    let control = el.querySelector('[data-slot="Control"]')
    let input = el.querySelector('[data-slot="Input"]')
    let trigger = el.querySelector('[data-slot="Trigger"]')
    let content = el.querySelector('[data-slot="Content"]')

    // Auto-create structure if missing
    if (!control) {
      control = document.createElement('div')
      control.dataset.slot = 'Control'
      el.appendChild(control)
    }
    if (!input) {
      input = document.createElement('input')
      input.type = 'text'
      input.dataset.slot = 'Input'
      input.placeholder = config.machineConfig?.placeholder || 'Select date...'
      control.appendChild(input)
    }
    if (!trigger) {
      trigger = document.createElement('button')
      trigger.type = 'button'
      trigger.dataset.slot = 'Trigger'
      trigger.setAttribute('aria-label', 'Open calendar')
      trigger.innerHTML = _runtime._icons.calendar(16)
      control.appendChild(trigger)
    }
    if (!content) {
      content = document.createElement('div')
      content.dataset.slot = 'Content'
      el.appendChild(content)
    }

    // Get config values
    const locale = config.machineConfig?.locale || 'en-US'
    const selectionMode = config.machineConfig?.selectionMode || 'single'
    const startOfWeek = config.machineConfig?.startOfWeek ?? 0
    const isDisabled = config.machineConfig?.disabled || false
    const isReadOnly = config.machineConfig?.readOnly || false
    const isInline = config.machineConfig?.inline || false
    const closeOnSelect = config.machineConfig?.closeOnSelect !== false
    const minDate = config.machineConfig?.min ? new Date(config.machineConfig.min) : null
    const maxDate = config.machineConfig?.max ? new Date(config.machineConfig.max) : null

    // Initialize state
    const today = new Date()
    el._datePickerState = {
      open: isInline,
      focusedDate: today,
      viewDate: new Date(today.getFullYear(), today.getMonth(), 1),
      selectedDates: [],
      view: 'day', // day, month, year
    }

    // Parse initial value
    if (config.machineConfig?.value || config.machineConfig?.defaultValue) {
      const dateStr = config.machineConfig?.value || config.machineConfig?.defaultValue
      const parsed = new Date(dateStr)
      if (!isNaN(parsed.getTime())) {
        el._datePickerState.selectedDates = [parsed]
        el._datePickerState.viewDate = new Date(parsed.getFullYear(), parsed.getMonth(), 1)
      }
    }

    // ========================================
    // HELPER FUNCTIONS
    // ========================================

    const formatDate = (date, fmt = 'short') => {
      if (!date) return ''
      try {
        return new Intl.DateTimeFormat(locale, { dateStyle: fmt }).format(date)
      } catch {
        // Intl.DateTimeFormat failed (unsupported format/locale) - use basic fallback
        return date.toLocaleDateString()
      }
    }

    const isSameDay = (a, b) => {
      if (!a || !b) return false
      return (
        a.getFullYear() === b.getFullYear() &&
        a.getMonth() === b.getMonth() &&
        a.getDate() === b.getDate()
      )
    }

    const isDateDisabled = date => {
      if (minDate && date < minDate) return true
      if (maxDate && date > maxDate) return true
      return false
    }

    const getDaysInMonth = (year, month) => {
      return new Date(year, month + 1, 0).getDate()
    }

    const getWeekdayNames = () => {
      const names = []
      const date = new Date(2024, 0, startOfWeek) // Start from a known Sunday (Jan 2024)
      // Adjust to get first day of week
      while (date.getDay() !== startOfWeek) {
        date.setDate(date.getDate() + 1)
      }
      for (let i = 0; i < 7; i++) {
        names.push(new Intl.DateTimeFormat(locale, { weekday: 'short' }).format(date))
        date.setDate(date.getDate() + 1)
      }
      return names
    }

    const getMonthName = (month, year) => {
      const date = new Date(year, month, 1)
      return new Intl.DateTimeFormat(locale, { month: 'long', year: 'numeric' }).format(date)
    }

    // ========================================
    // DEFAULT STYLING
    // ========================================

    const setDefault = (element, prop, value) => {
      if (!element.style[prop]) element.style[prop] = value
    }

    // Root styling
    setDefault(el, 'position', 'relative')
    setDefault(el, 'display', 'inline-block')

    // Control styling
    setDefault(control, 'display', 'flex')
    setDefault(control, 'alignItems', 'center')
    setDefault(control, 'gap', '0')
    setDefault(control, 'background', '#1a1a1a')
    setDefault(control, 'border', '1px solid #333')
    setDefault(control, 'borderRadius', '6px')
    setDefault(control, 'overflow', 'hidden')

    // Input styling
    setDefault(input, 'flex', '1')
    setDefault(input, 'padding', '8px 12px')
    setDefault(input, 'background', 'transparent')
    setDefault(input, 'border', 'none')
    setDefault(input, 'outline', 'none')
    setDefault(input, 'color', '#e0e0e0')
    setDefault(input, 'fontSize', '13px')
    setDefault(input, 'minWidth', '140px')
    input.readOnly = true

    // Trigger styling
    setDefault(trigger, 'padding', '8px 10px')
    setDefault(trigger, 'background', 'transparent')
    setDefault(trigger, 'border', 'none')
    setDefault(trigger, 'borderLeft', '1px solid #333')
    setDefault(trigger, 'cursor', isDisabled ? 'not-allowed' : 'pointer')
    setDefault(trigger, 'color', '#888')
    setDefault(trigger, 'display', 'flex')
    setDefault(trigger, 'alignItems', 'center')
    setDefault(trigger, 'justifyContent', 'center')

    // Content (calendar popup) styling
    content.style.position = isInline ? 'relative' : 'absolute'
    content.style.top = isInline ? '0' : '100%'
    content.style.left = '0'
    content.style.marginTop = isInline ? '0' : '4px'
    content.style.zIndex = '1000'
    setDefault(content, 'background', '#1a1a1a')
    setDefault(content, 'border', '1px solid #333')
    setDefault(content, 'borderRadius', '8px')
    setDefault(content, 'padding', '12px')
    setDefault(content, 'boxShadow', '0 4px 20px rgba(0,0,0,0.4)')
    setDefault(content, 'minWidth', '280px')

    if (!isInline) {
      content.style.display = 'none'
    }

    // ========================================
    // CALENDAR RENDERING
    // ========================================

    const renderCalendar = () => {
      const state = el._datePickerState
      const { viewDate, selectedDates, view } = state

      if (view === 'day') {
        renderDayView(viewDate, selectedDates)
      } else if (view === 'month') {
        renderMonthView(viewDate.getFullYear())
      } else if (view === 'year') {
        renderYearView(viewDate.getFullYear())
      }
    }

    const renderDayView = (viewDate, selectedDates) => {
      const year = viewDate.getFullYear()
      const month = viewDate.getMonth()
      const daysInMonth = getDaysInMonth(year, month)
      const firstDayOfMonth = new Date(year, month, 1).getDay()
      const offset = (firstDayOfMonth - startOfWeek + 7) % 7
      const weekdays = getWeekdayNames()

      let html = `
        <div data-part="view-control" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px;">
          <button data-part="prev-trigger" type="button" style="background: transparent; border: none; color: #888; cursor: pointer; padding: 4px 8px; border-radius: 4px;">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6"/></svg>
          </button>
          <button data-part="view-trigger" type="button" style="background: transparent; border: none; color: #e0e0e0; cursor: pointer; font-size: 14px; font-weight: 500; padding: 4px 8px; border-radius: 4px;">
            ${getMonthName(month, year)}
          </button>
          <button data-part="next-trigger" type="button" style="background: transparent; border: none; color: #888; cursor: pointer; padding: 4px 8px; border-radius: 4px;">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
          </button>
        </div>
        <table data-part="table" style="width: 100%; border-collapse: collapse; text-align: center;">
          <thead data-part="table-head">
            <tr>${weekdays.map(d => `<th style="color: #666; font-size: 11px; font-weight: 500; padding: 4px 0;">${d}</th>`).join('')}</tr>
          </thead>
          <tbody data-part="table-body">
      `

      let day = 1
      for (let week = 0; week < 6; week++) {
        html += '<tr>'
        for (let dow = 0; dow < 7; dow++) {
          const cellIndex = week * 7 + dow
          if (cellIndex < offset || day > daysInMonth) {
            html += '<td style="padding: 2px;"></td>'
          } else {
            const date = new Date(year, month, day)
            const isSelected = selectedDates.some(d => isSameDay(d, date))
            const isToday = isSameDay(date, new Date())
            const disabled = isDateDisabled(date)

            const cellStyle = `
              padding: 2px;
            `
            const btnStyle = `
              width: 32px;
              height: 32px;
              border: none;
              border-radius: 6px;
              cursor: ${disabled ? 'not-allowed' : 'pointer'};
              font-size: 13px;
              background: ${isSelected ? '#4f46e5' : 'transparent'};
              color: ${disabled ? '#444' : isSelected ? '#fff' : isToday ? '#4f46e5' : '#e0e0e0'};
              font-weight: ${isToday ? '600' : '400'};
              opacity: ${disabled ? '0.5' : '1'};
            `

            html += `
              <td data-part="day-table-cell" style="${cellStyle}">
                <button
                  data-part="day-table-cell-trigger"
                  data-day="${day}"
                  data-date="${date.toISOString()}"
                  type="button"
                  style="${btnStyle}"
                  ${disabled ? 'disabled' : ''}
                  ${isSelected ? 'data-selected="true"' : ''}
                  ${isToday ? 'data-today="true"' : ''}
                >${day}</button>
              </td>
            `
            day++
          }
        }
        html += '</tr>'
        if (day > daysInMonth) break
      }

      html += '</tbody></table>'
      content.innerHTML = html

      // Bind events
      bindCalendarEvents()
    }

    const renderMonthView = year => {
      const months = []
      for (let i = 0; i < 12; i++) {
        const date = new Date(year, i, 1)
        months.push(new Intl.DateTimeFormat(locale, { month: 'short' }).format(date))
      }

      let html = `
        <div data-part="view-control" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px;">
          <button data-part="prev-trigger" type="button" style="background: transparent; border: none; color: #888; cursor: pointer; padding: 4px 8px;">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6"/></svg>
          </button>
          <button data-part="view-trigger" type="button" style="background: transparent; border: none; color: #e0e0e0; cursor: pointer; font-size: 14px; font-weight: 500; padding: 4px 8px;">
            ${year}
          </button>
          <button data-part="next-trigger" type="button" style="background: transparent; border: none; color: #888; cursor: pointer; padding: 4px 8px;">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
          </button>
        </div>
        <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">
      `

      months.forEach((name, i) => {
        const isCurrentMonth =
          el._datePickerState.viewDate.getMonth() === i &&
          el._datePickerState.viewDate.getFullYear() === year
        html += `
          <button
            data-part="month-table-cell-trigger"
            data-month="${i}"
            type="button"
            style="padding: 12px 8px; background: ${isCurrentMonth ? '#4f46e5' : 'transparent'}; color: ${isCurrentMonth ? '#fff' : '#e0e0e0'}; border: none; border-radius: 6px; cursor: pointer; font-size: 13px;"
          >${name}</button>
        `
      })

      html += '</div>'
      content.innerHTML = html
      bindCalendarEvents()
    }

    const renderYearView = centerYear => {
      const startYear = centerYear - 5
      const years = []
      for (let i = 0; i < 12; i++) {
        years.push(startYear + i)
      }

      let html = `
        <div data-part="view-control" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 12px;">
          <button data-part="prev-trigger" type="button" style="background: transparent; border: none; color: #888; cursor: pointer; padding: 4px 8px;">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M15 18l-6-6 6-6"/></svg>
          </button>
          <span style="color: #e0e0e0; font-size: 14px; font-weight: 500;">${startYear} - ${startYear + 11}</span>
          <button data-part="next-trigger" type="button" style="background: transparent; border: none; color: #888; cursor: pointer; padding: 4px 8px;">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg>
          </button>
        </div>
        <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px;">
      `

      years.forEach(y => {
        const isCurrentYear = el._datePickerState.viewDate.getFullYear() === y
        html += `
          <button
            data-part="year-table-cell-trigger"
            data-year="${y}"
            type="button"
            style="padding: 12px 8px; background: ${isCurrentYear ? '#4f46e5' : 'transparent'}; color: ${isCurrentYear ? '#fff' : '#e0e0e0'}; border: none; border-radius: 6px; cursor: pointer; font-size: 13px;"
          >${y}</button>
        `
      })

      html += '</div>'
      content.innerHTML = html
      bindCalendarEvents()
    }

    const bindCalendarEvents = () => {
      const state = el._datePickerState

      // Day selection
      content.querySelectorAll('[data-part="day-table-cell-trigger"]').forEach(btn => {
        btn.addEventListener('click', () => {
          if (btn.disabled) return
          const dateStr = btn.getAttribute('data-date')
          const date = new Date(dateStr)
          selectDate(date)
        })
        btn.addEventListener('mouseenter', () => {
          if (!btn.disabled)
            btn.style.background = btn.hasAttribute('data-selected') ? '#4f46e5' : '#252525'
        })
        btn.addEventListener('mouseleave', () => {
          if (!btn.disabled)
            btn.style.background = btn.hasAttribute('data-selected') ? '#4f46e5' : 'transparent'
        })
      })

      // Month selection
      content.querySelectorAll('[data-part="month-table-cell-trigger"]').forEach(btn => {
        btn.addEventListener('click', () => {
          const month = parseInt(btn.getAttribute('data-month'))
          state.viewDate = new Date(state.viewDate.getFullYear(), month, 1)
          state.view = 'day'
          renderCalendar()
        })
      })

      // Year selection
      content.querySelectorAll('[data-part="year-table-cell-trigger"]').forEach(btn => {
        btn.addEventListener('click', () => {
          const year = parseInt(btn.getAttribute('data-year'))
          state.viewDate = new Date(year, state.viewDate.getMonth(), 1)
          state.view = 'month'
          renderCalendar()
        })
      })

      // View trigger (month/year header)
      const viewTrigger = content.querySelector('[data-part="view-trigger"]')
      if (viewTrigger) {
        viewTrigger.addEventListener('click', () => {
          if (state.view === 'day') {
            state.view = 'month'
          } else if (state.view === 'month') {
            state.view = 'year'
          }
          renderCalendar()
        })
      }

      // Prev/Next navigation
      const prevTrigger = content.querySelector('[data-part="prev-trigger"]')
      const nextTrigger = content.querySelector('[data-part="next-trigger"]')

      if (prevTrigger) {
        prevTrigger.addEventListener('click', () => {
          if (state.view === 'day') {
            state.viewDate = new Date(
              state.viewDate.getFullYear(),
              state.viewDate.getMonth() - 1,
              1
            )
          } else if (state.view === 'month') {
            state.viewDate = new Date(
              state.viewDate.getFullYear() - 1,
              state.viewDate.getMonth(),
              1
            )
          } else if (state.view === 'year') {
            state.viewDate = new Date(
              state.viewDate.getFullYear() - 12,
              state.viewDate.getMonth(),
              1
            )
          }
          renderCalendar()
        })
      }

      if (nextTrigger) {
        nextTrigger.addEventListener('click', () => {
          if (state.view === 'day') {
            state.viewDate = new Date(
              state.viewDate.getFullYear(),
              state.viewDate.getMonth() + 1,
              1
            )
          } else if (state.view === 'month') {
            state.viewDate = new Date(
              state.viewDate.getFullYear() + 1,
              state.viewDate.getMonth(),
              1
            )
          } else if (state.view === 'year') {
            state.viewDate = new Date(
              state.viewDate.getFullYear() + 12,
              state.viewDate.getMonth(),
              1
            )
          }
          renderCalendar()
        })
      }
    }

    // ========================================
    // DATE SELECTION
    // ========================================

    const selectDate = date => {
      const state = el._datePickerState

      if (selectionMode === 'single') {
        state.selectedDates = [date]
      } else if (selectionMode === 'multiple') {
        const idx = state.selectedDates.findIndex(d => isSameDay(d, date))
        if (idx >= 0) {
          state.selectedDates.splice(idx, 1)
        } else {
          state.selectedDates.push(date)
        }
      } else if (selectionMode === 'range') {
        if (state.selectedDates.length === 0 || state.selectedDates.length === 2) {
          state.selectedDates = [date]
        } else {
          const start = state.selectedDates[0]
          if (date < start) {
            state.selectedDates = [date, start]
          } else {
            state.selectedDates = [start, date]
          }
        }
      }

      updateInputValue()
      renderCalendar()

      // Fire change event
      el.dispatchEvent(
        new CustomEvent('change', {
          detail: {
            value: state.selectedDates.map(d => d.toISOString().split('T')[0]),
            dates: state.selectedDates,
          },
          bubbles: true,
        })
      )

      // Close popup if configured
      if (closeOnSelect && !isInline && selectionMode === 'single') {
        closeCalendar()
      }
      if (
        closeOnSelect &&
        !isInline &&
        selectionMode === 'range' &&
        state.selectedDates.length === 2
      ) {
        closeCalendar()
      }
    }

    const updateInputValue = () => {
      const state = el._datePickerState
      if (state.selectedDates.length === 0) {
        input.value = ''
      } else if (selectionMode === 'range' && state.selectedDates.length === 2) {
        input.value = `${formatDate(state.selectedDates[0])} - ${formatDate(state.selectedDates[1])}`
      } else {
        input.value = state.selectedDates.map(d => formatDate(d)).join(', ')
      }
    }

    // ========================================
    // OPEN/CLOSE
    // ========================================

    const openCalendar = () => {
      if (isDisabled || isInline) return
      el._datePickerState.open = true
      el._datePickerState.view = 'day'
      content.style.display = 'block'
      el.setAttribute('data-state', 'open')
      renderCalendar()

      el.dispatchEvent(new CustomEvent('open', { bubbles: true }))
    }

    const closeCalendar = () => {
      if (isInline) return
      el._datePickerState.open = false
      content.style.display = 'none'
      el.setAttribute('data-state', 'closed')

      el.dispatchEvent(new CustomEvent('close', { bubbles: true }))
    }

    // ========================================
    // EVENT HANDLERS
    // ========================================

    // Debounce flag to prevent double-firing from pointerdown + click
    let lastToggleTime = 0

    // Toggle calendar helper with debounce
    const toggleCalendar = e => {
      const now = Date.now()
      if (now - lastToggleTime < 100) return // Debounce 100ms
      lastToggleTime = now

      if (e) {
        e.preventDefault()
        e.stopPropagation()
      }
      if (el._datePickerState.open) {
        closeCalendar()
      } else {
        openCalendar()
      }
    }

    // Trigger click - multiple event types for robustness
    // Using both click and pointerdown ensures compatibility with
    // different browsers, Shadow DOM, and automation tools
    trigger.addEventListener('click', toggleCalendar)
    trigger.addEventListener('pointerdown', e => {
      // Only handle primary button (left click)
      if (e.button !== 0) return
      toggleCalendar(e)
    })

    // Input click (also opens) - same robust handling
    let lastInputOpenTime = 0
    const openFromInput = e => {
      const now = Date.now()
      if (now - lastInputOpenTime < 100) return
      lastInputOpenTime = now

      if (e) {
        e.preventDefault()
        e.stopPropagation()
      }
      if (!el._datePickerState.open) {
        openCalendar()
      }
    }
    input.addEventListener('click', openFromInput)
    input.addEventListener('pointerdown', e => {
      if (e.button !== 0) return
      openFromInput(e)
    })

    // Click outside to close (use composedPath for Shadow DOM compatibility)
    document.addEventListener('click', e => {
      if (el._datePickerState.open && !e.composedPath().includes(el)) {
        closeCalendar()
      }
    })

    // Keyboard navigation
    el.addEventListener('keydown', e => {
      if (e.key === 'Escape' && el._datePickerState.open) {
        closeCalendar()
        trigger.focus()
      }
    })

    // ========================================
    // ACCESSIBILITY
    // ========================================

    el.setAttribute('role', 'group')
    trigger.setAttribute('aria-label', 'Open calendar')
    trigger.setAttribute('aria-haspopup', 'dialog')

    // ========================================
    // DISABLED STATE
    // ========================================

    if (isDisabled) {
      el.setAttribute('data-disabled', 'true')
      el.style.opacity = '0.5'
      el.style.pointerEvents = 'none'
    }

    // ========================================
    // INITIAL RENDER
    // ========================================

    updateInputValue()
    el.setAttribute('data-state', isInline ? 'open' : 'closed')
    if (isInline) {
      renderCalendar()
    }
  },

  // Icon cache and loading
  _iconCache: new Map(),
  _customIcons: new Map(),
  _pendingIcons: new Map(),
  _fallbackIcon:
    '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="m9 9 6 6"/><path d="m15 9-6 6"/></svg>',

  // Register a user-defined icon via $icons: name: "M..." path data.
  // Stored in a separate registry that loadIcon consults first, ahead of
  // the network/Lucide cache.
  registerIcon(name, pathData, viewBox) {
    this._customIcons.set(name, { path: pathData, viewBox: viewBox || '0 0 24 24' })
  },

  async loadIcon(el, iconName) {
    if (!el || !iconName) return
    // Reject malicious icon names (path traversal, scripts, oversized).
    // sanitizeIconName is stamped from compiler/runtime/icons.ts.
    const safeName = sanitizeIconName(iconName)
    if (!safeName) return
    iconName = safeName
    const size = el.dataset.iconSize || '16'
    const color = el.dataset.iconColor || 'currentColor'
    const strokeWidth = el.dataset.iconWeight || '2'
    const isFilled = el.dataset.iconFill === 'true'

    // Custom icons (via $icons:) take precedence over Lucide
    const custom = this._customIcons.get(iconName)
    if (custom) {
      const paths = String(custom.path)
        .split(/[\n|]/)
        .map(p => p.trim())
        .filter(p => p.length > 0)
        .map(d => `<path d="${d}"/>`)
        .join('')
      el.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="${custom.viewBox}" fill="${isFilled ? 'currentColor' : 'none'}" stroke="${isFilled ? 'none' : 'currentColor'}" stroke-width="${strokeWidth}" stroke-linecap="round" stroke-linejoin="round" style="width:${size}px;height:${size}px;color:${color};display:block;">${paths}</svg>`
      return
    }

    // Check cache first
    let svgText = this._iconCache.get(iconName)

    if (!svgText) {
      // Check pending request
      let pending = this._pendingIcons.get(iconName)
      if (!pending) {
        pending = this._fetchIcon(iconName)
        this._pendingIcons.set(iconName, pending)
      }
      svgText = await pending
      this._pendingIcons.delete(iconName)
      if (!svgText) {
        console.warn(`Icon "${iconName}" not found, using fallback`)
        svgText = this._fallbackIcon
      }
    }

    el.innerHTML = svgText
    const svg = el.querySelector('svg')
    if (svg) {
      svg.style.width = size + 'px'
      svg.style.height = size + 'px'
      svg.style.color = color
      svg.style.display = 'block'
      if (isFilled) {
        svg.setAttribute('fill', 'currentColor')
        svg.setAttribute('stroke', 'none')
      } else {
        svg.setAttribute('stroke-width', strokeWidth)
      }
    }
  },

  async _fetchIcon(iconName) {
    // Defence-in-depth: reject unsafe names even if a caller bypasses
    // loadIcon's check. sanitizeIconName is stamped from icons.ts.
    const safeName = sanitizeIconName(iconName)
    if (!safeName) return null
    try {
      const url = `https://unpkg.com/lucide-static/icons/${safeName}.svg`
      const res = await fetch(url)
      if (!res.ok) return null
      const rawSvg = await res.text()
      // Strip script/foreignObject/use/event-handlers from CDN response
      // before it ever reaches innerHTML. sanitizeSVG is stamped from
      // compiler/runtime/icons.ts.
      const svgText = sanitizeSVG(rawSvg)
      if (!svgText) return null
      this._iconCache.set(safeName, svgText)
      return svgText
    } catch (err) {
      console.warn(`Failed to load icon "${safeName}":`, err)
      return null
    }
  },

  preloadIcons(names) {
    names.forEach(name => {
      if (!this._iconCache.has(name)) {
        this._fetchIcon(name)
      }
    })
  },

  // Chart.js integration
  _chartJsLoaded: false,
  _chartJsLoading: null,

  async _loadChartJs() {
    if (this._chartJsLoaded) return
    if (this._chartJsLoading) return this._chartJsLoading

    this._chartJsLoading = new Promise((resolve, reject) => {
      const script = document.createElement('script')
      script.src = 'https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js'
      script.onload = () => {
        this._chartJsLoaded = true
        resolve()
      }
      script.onerror = () => reject(new Error('Failed to load Chart.js'))
      document.head.appendChild(script)
    })

    return this._chartJsLoading
  },

  _parseChartData(data, xField, yField) {
    if (!data) return { labels: [], values: [] }

    // Handle array of objects with x/y fields
    if (Array.isArray(data)) {
      if (xField && yField) {
        return {
          labels: data.map(item => item[xField]),
          values: data.map(item => item[yField]),
        }
      }
      return { labels: data.map((_, i) => String(i)), values: data }
    }

    // Handle key-value object (most common case)
    if (typeof data === 'object') {
      const entries = Object.entries(data)
      // Check if values are objects with x/y fields
      if (entries.length > 0 && typeof entries[0][1] === 'object' && xField && yField) {
        return {
          labels: entries.map(([_, v]) => v[xField]),
          values: entries.map(([_, v]) => v[yField]),
        }
      }
      // Simple key-value pairs
      return {
        labels: entries.map(([k]) => k),
        values: entries.map(([_, v]) => v),
      }
    }

    return { labels: [], values: [] }
  },

  async createChart(element, config) {
    await this._loadChartJs()

    // Chart.js requires a container with position:relative and overflow:hidden
    // to properly constrain the canvas size when responsive:true
    element.style.position = 'relative'
    element.style.overflow = 'hidden'

    // Create a wrapper that fills the container - Chart.js needs this structure
    const wrapper = document.createElement('div')
    wrapper.style.cssText = 'position:absolute;top:0;left:0;width:100%;height:100%;'
    element.appendChild(wrapper)

    const canvas = document.createElement('canvas')
    wrapper.appendChild(canvas)

    const { labels, values } = this._parseChartData(config.data, config.xField, config.yField)

    // Default colors
    const defaultColors = [
      '#2563eb',
      '#10b981',
      '#f59e0b',
      '#ef4444',
      '#8b5cf6',
      '#ec4899',
      '#06b6d4',
      '#84cc16',
      '#f97316',
      '#6366f1',
    ]
    const colors = config.colors || defaultColors
    const chartType = config.type || 'line'
    const isPieOrDoughnut = chartType === 'pie' || chartType === 'doughnut'

    // Determine fill based on config (Area charts have fill: true by default)
    const shouldFill = config.fill === true || (chartType === 'line' && config.fill === 'origin')

    // Build Chart.js config
    const chartConfig = {
      type: chartType,
      data: {
        labels: labels,
        datasets: [
          {
            label: config.title || '', // Empty string fallback for legend
            data: values,
            backgroundColor: isPieOrDoughnut
              ? colors.slice(0, values.length)
              : shouldFill
                ? colors[0] + '40'
                : colors[0] + '80',
            borderColor: isPieOrDoughnut ? colors.slice(0, values.length) : colors[0],
            borderWidth: isPieOrDoughnut ? 1 : 2,
            fill: shouldFill ? 'origin' : false,
            tension: config.tension !== undefined ? config.tension : 0.3,
            pointBackgroundColor: colors[0],
            pointBorderColor: colors[0],
            pointRadius: chartType === 'scatter' ? 5 : 3,
          },
        ],
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: {
            display: config.legend === true || isPieOrDoughnut,
            labels: {
              color: '#ccc',
              // For pie/doughnut: use labels from data (each slice = label)
              // For others: use dataset labels (but we only have one dataset)
              generateLabels: isPieOrDoughnut
                ? undefined
                : chart => {
                    // Hide legend for single-dataset charts unless explicitly requested
                    if (!config.legend) return []
                    return Chart.defaults.plugins.legend.labels.generateLabels(chart)
                  },
            },
          },
          title: {
            display: !!config.title,
            text: config.title || '',
            color: '#fff',
          },
        },
        scales: isPieOrDoughnut
          ? {}
          : {
              x: {
                display: config.axes !== false,
                grid: { display: config.grid !== false, color: '#333' },
                ticks: { color: '#888' },
              },
              y: {
                display: config.axes !== false,
                grid: { display: config.grid !== false, color: '#333' },
                ticks: { color: '#888' },
                beginAtZero: true,
              },
            },
      },
    }

    // Special handling for radar charts
    if (chartType === 'radar') {
      chartConfig.options.scales = {
        r: {
          grid: { color: '#333' },
          pointLabels: { color: '#888' },
          ticks: { color: '#888', backdropColor: 'transparent' },
        },
      }
    }

    new window.Chart(canvas.getContext('2d'), chartConfig)
  },

  // Animation registry
  _animations: new Map(),

  registerAnimation(animation) {
    if (!animation || !animation.name) return
    this._animations.set(animation.name, animation)
  },

  getAnimation(name) {
    return this._animations.get(name)
  },

  animate(animationName, elements, options = {}) {
    const animation = this._animations.get(animationName)
    if (!animation) {
      console.warn(`Animation "${animationName}" not found`)
      return null
    }

    // Normalize elements to array
    let targets = []
    if (!elements) {
      return null
    } else if (Array.isArray(elements)) {
      targets = elements.map(e => e._el || e)
    } else {
      targets = [elements._el || elements]
    }

    const { delay = 0, stagger = 0, loop = false, reverse = false } = options
    const results = []

    // Easing curves
    const easingMap = {
      linear: 'linear',
      ease: 'ease',
      'ease-in': 'ease-in',
      'ease-out': 'ease-out',
      'ease-in-out': 'ease-in-out',
    }

    targets.forEach((el, index) => {
      if (!el) return

      const elementDelay = delay + index * stagger

      // Build keyframes from animation definition
      const keyframes = []
      const propertyTimelines = new Map()

      // Group properties by name to build proper keyframe sequence
      animation.keyframes.forEach(kf => {
        kf.properties.forEach(prop => {
          if (!propertyTimelines.has(prop.name)) {
            propertyTimelines.set(prop.name, [])
          }
          propertyTimelines.get(prop.name).push({
            time: kf.time,
            value: prop.value,
            easing: prop.easing || animation.easing || 'ease-out',
          })
        })
      })

      // Convert to Web Animations API format
      const duration = animation.duration * 1000
      const allTimes = [...new Set(animation.keyframes.map(kf => kf.time))].sort((a, b) => a - b)

      allTimes.forEach(time => {
        const kf = { offset: time / animation.duration }
        propertyTimelines.forEach((timeline, propName) => {
          const point = timeline.find(p => p.time === time)
          if (point) {
            // Map Mirror property names to CSS
            const cssName = this._animPropMap(propName)
            kf[cssName] = this._animValueMap(propName, point.value)
          }
        })
        keyframes.push(kf)
      })

      if (reverse) {
        keyframes.reverse()
        keyframes.forEach((kf, i) => {
          kf.offset = i / (keyframes.length - 1)
        })
      }

      const timing = {
        duration,
        delay: elementDelay,
        easing: easingMap[animation.easing] || 'ease-out',
        iterations: loop === true ? Infinity : typeof loop === 'number' ? loop : 1,
        fill: 'forwards',
      }

      try {
        const anim = el.animate(keyframes, timing)
        results.push(anim)
      } catch (err) {
        console.warn('Animation error:', err)
      }
    })

    return results
  },

  _animPropMap(name) {
    const map = {
      opacity: 'opacity',
      'x-offset': 'translateX',
      'y-offset': 'translateY',
      scale: 'scale',
      'scale-x': 'scaleX',
      'scale-y': 'scaleY',
      rotate: 'rotate',
      background: 'backgroundColor',
      bg: 'backgroundColor',
      color: 'color',
      col: 'color',
      width: 'width',
      height: 'height',
      padding: 'padding',
      pad: 'padding',
      radius: 'borderRadius',
      rad: 'borderRadius',
    }
    return map[name] || name
  },

  _animValueMap(propName, value) {
    if (propName === 'x-offset' || propName === 'y-offset') {
      return typeof value === 'number' ? value + 'px' : value
    }
    if (propName === 'rotate') {
      return typeof value === 'number' ? value + 'deg' : value
    }
    if (
      propName === 'width' ||
      propName === 'height' ||
      propName === 'padding' ||
      propName === 'pad' ||
      propName === 'radius' ||
      propName === 'rad'
    ) {
      return typeof value === 'number' ? value + 'px' : value
    }
    return value
  },

  setupEnterExitObserver(el, onEnter, onExit) {
    if (!el) return null
    const element = el._el || el

    const observer = new IntersectionObserver(
      entries => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            if (onEnter) onEnter()
          } else {
            if (onExit) onExit()
          }
        })
      },
      { threshold: 0.1 }
    )

    observer.observe(element)
    return observer
  },
}

// ============================================
// TEST API
// ============================================

/**
 * Mirror Test API - Always available for testing
 */
const __MIRROR_TEST__ = {
  // Element Access
  getElement(nodeId) {
    return document.querySelector(`[data-mirror-id="${nodeId}"]`)
  },

  getAllElements() {
    return Array.from(document.querySelectorAll('[data-mirror-id]'))
  },

  findByName(name) {
    return (
      document.querySelector(`[data-mirror-name="${name}"]`) ||
      document.querySelector(`[data-instance-name="${name}"]`) ||
      document.querySelector(`[data-component-name="${name}"]`) ||
      document.querySelector(`[name="${name}"]`)
    )
  },

  // State Inspection
  getState(el) {
    if (!el) return 'default'
    return el._stateMachine?.current || el.dataset?.state || 'default'
  },

  getAvailableStates(el) {
    if (!el?._stateMachine) return ['default']
    return Object.keys(el._stateMachine.states)
  },

  getStyles(el) {
    if (!el) return {}
    const styles = {}
    for (let i = 0; i < el.style.length; i++) {
      const prop = el.style[i]
      styles[prop] = el.style.getPropertyValue(prop)
    }
    return styles
  },

  getBaseStyles(el) {
    return el?._baseStyles || {}
  },

  // State Manipulation
  setState(el, state) {
    if (el) _runtime.transitionTo(el, state)
  },

  resetToBase(el) {
    if (el) _runtime.transitionTo(el, 'default')
  },

  // Event Simulation
  trigger(el, event) {
    if (!el) return
    switch (event) {
      case 'click':
        el.click()
        break
      case 'hover':
        el.dispatchEvent(new MouseEvent('mouseenter', { bubbles: true }))
        break
      case 'focus':
        el.focus()
        el.dispatchEvent(new FocusEvent('focus', { bubbles: true }))
        break
      case 'blur':
        el.blur()
        el.dispatchEvent(new FocusEvent('blur', { bubbles: true }))
        break
      case 'change':
        el.dispatchEvent(new Event('change', { bubbles: true }))
        break
      case 'input':
        el.dispatchEvent(new InputEvent('input', { bubbles: true }))
        break
    }
  },

  triggerKey(el, key, eventType = 'keydown') {
    if (!el) return
    const keyMap = {
      enter: 'Enter',
      escape: 'Escape',
      esc: 'Escape',
      space: ' ',
      tab: 'Tab',
      backspace: 'Backspace',
      delete: 'Delete',
      'arrow-up': 'ArrowUp',
      'arrow-down': 'ArrowDown',
      'arrow-left': 'ArrowLeft',
      'arrow-right': 'ArrowRight',
      up: 'ArrowUp',
      down: 'ArrowDown',
      left: 'ArrowLeft',
      right: 'ArrowRight',
    }
    const normalizedKey = keyMap[key.toLowerCase()] || key
    el.dispatchEvent(
      new KeyboardEvent(eventType, {
        key: normalizedKey,
        code: normalizedKey,
        bubbles: true,
        cancelable: true,
      })
    )
  },

  // Built-in Function Calls
  toggle(el, states) {
    if (el) _runtime.stateMachineToggle(el, states)
  },

  exclusive(el, state) {
    if (!el) return
    const targetState =
      state ||
      (el._stateMachine
        ? Object.keys(el._stateMachine.states).find(s => s !== 'default')
        : 'active') ||
      'active'
    _runtime.exclusiveTransition(el, targetState)
  },

  // Visibility Control
  show(el) {
    if (!el) return
    el.hidden = false
    if (el.style.display === 'none') {
      el.style.display = el._savedDisplay || ''
    }
    _runtime.updateVisibility && _runtime.updateVisibility(el)
  },

  hide(el) {
    if (!el) return
    const currentDisplay = window.getComputedStyle(el).display
    if (currentDisplay !== 'none') {
      el._savedDisplay = currentDisplay
    }
    el.style.display = 'none'
    el.hidden = true
  },

  // Visibility Checks
  isVisible(el) {
    if (!el) return false
    if (el.hidden) return false
    const style = window.getComputedStyle(el)
    return (
      style.display !== 'none' && style.visibility !== 'hidden' && parseFloat(style.opacity) !== 0
    )
  },

  isHidden(el) {
    return !this.isVisible(el)
  },

  isDisplayNone(el) {
    if (!el) return true
    return window.getComputedStyle(el).display === 'none'
  },

  isOpacityZero(el) {
    if (!el) return true
    return parseFloat(window.getComputedStyle(el).opacity) === 0
  },

  hasHiddenAttribute(el) {
    if (!el) return true
    return el.hidden === true
  },

  getVisibilityState(el) {
    if (!el) return { visible: false, display: 'none', opacity: 0, hidden: true, reason: 'hidden' }
    const style = window.getComputedStyle(el)
    const display = style.display
    const opacity = parseFloat(style.opacity)
    const hidden = el.hidden
    const visibility = style.visibility
    let reason = 'visible',
      visible = true
    if (display === 'none') {
      visible = false
      reason = 'display'
    } else if (opacity === 0) {
      visible = false
      reason = 'opacity'
    } else if (hidden) {
      visible = false
      reason = 'hidden'
    } else if (visibility === 'hidden') {
      visible = false
      reason = 'visibility'
    }
    return { visible, display, opacity, hidden, reason }
  },

  getComputedStyle(el, prop) {
    if (!el) return ''
    return window.getComputedStyle(el).getPropertyValue(prop)
  },

  waitForHidden(el, timeout = 1000) {
    return new Promise(resolve => {
      if (!el) {
        resolve(true)
        return
      }
      if (!this.isVisible(el)) {
        resolve(true)
        return
      }
      const startTime = Date.now()
      const check = setInterval(() => {
        if (!this.isVisible(el)) {
          clearInterval(check)
          resolve(true)
        } else if (Date.now() - startTime > timeout) {
          clearInterval(check)
          resolve(false)
        }
      }, 16)
    })
  },

  // Async Helpers
  waitForState(el, state, timeout = 1000) {
    return new Promise(resolve => {
      if (!el) {
        resolve(false)
        return
      }
      if (this.getState(el) === state) {
        resolve(true)
        return
      }
      const startTime = Date.now()
      const check = setInterval(() => {
        if (this.getState(el) === state) {
          clearInterval(check)
          resolve(true)
        } else if (Date.now() - startTime > timeout) {
          clearInterval(check)
          resolve(false)
        }
      }, 16)
    })
  },

  waitForVisible(el, visible, timeout = 1000) {
    return new Promise(resolve => {
      if (!el) {
        resolve(false)
        return
      }
      if (this.isVisible(el) === visible) {
        resolve(true)
        return
      }
      const startTime = Date.now()
      const check = setInterval(() => {
        if (this.isVisible(el) === visible) {
          clearInterval(check)
          resolve(true)
        } else if (Date.now() - startTime > timeout) {
          clearInterval(check)
          resolve(false)
        }
      }, 16)
    })
  },

  // Debug
  logStateMachine(el) {
    const info = this.getStateMachineInfo(el)
    if (!info) {
      console.log('[Mirror Test API] No state machine')
      return
    }
    console.group('[Mirror Test API] State Machine')
    console.log('Current:', info.current)
    console.log('Initial:', info.initial)
    console.log('States:', info.states)
    console.log('Transitions:', info.transitions)
    console.groupEnd()
  },

  getStateMachineInfo(el) {
    if (!el?._stateMachine) return null
    const sm = el._stateMachine
    return {
      current: sm.current,
      initial: sm.initial,
      states: Object.keys(sm.states),
      transitions: sm.transitions.map(t => ({
        trigger: t.trigger,
        to: t.to,
        key: t.key,
        modifier: t.modifier,
      })),
    }
  },

  // Navigation
  _navigationHistory: [],
  _historyIndex: -1,

  navigate(target) {
    if (!target) return
    _runtime.navigate(target, null)
  },

  navigateToPage(pageName) {
    if (!pageName) return Promise.resolve()
    // Track history
    if (this._historyIndex < this._navigationHistory.length - 1) {
      this._navigationHistory = this._navigationHistory.slice(0, this._historyIndex + 1)
    }
    this._navigationHistory.push(pageName)
    this._historyIndex = this._navigationHistory.length - 1

    _runtime.navigateToPage(pageName, null)
    return new Promise(resolve => setTimeout(resolve, 50))
  },

  goBack() {
    if (this._historyIndex <= 0) return
    this._historyIndex--
    const previousPage = this._navigationHistory[this._historyIndex]
    if (previousPage) _runtime.navigateToPage(previousPage, null)
  },

  goForward() {
    if (this._historyIndex >= this._navigationHistory.length - 1) return
    this._historyIndex++
    const nextPage = this._navigationHistory[this._historyIndex]
    if (nextPage) _runtime.navigateToPage(nextPage, null)
  },

  getCurrentPage() {
    if (this._historyIndex >= 0 && this._historyIndex < this._navigationHistory.length) {
      return this._navigationHistory[this._historyIndex]
    }
    return null
  },

  getNavigationHistory() {
    return [...this._navigationHistory]
  },

  getNavigationState() {
    return {
      currentPage: this.getCurrentPage(),
      history: this.getNavigationHistory(),
      canGoBack: this._historyIndex > 0,
      canGoForward: this._historyIndex < this._navigationHistory.length - 1,
    }
  },

  getPageContainer() {
    return _runtime.getPageContainer()
  },

  showView(view) {
    if (!view) return
    view.style.display = ''
    view.hidden = false
  },

  hideView(view) {
    if (!view) return
    view.style.display = 'none'
    view.hidden = true
  },

  switchToView(view) {
    if (!view) return
    const parent = view.parentElement
    if (!parent) return
    Array.from(parent.children).forEach(sibling => {
      if (sibling.dataset?.component) {
        if (sibling === view) {
          sibling.style.display = ''
          sibling.hidden = false
        } else {
          sibling.style.display = 'none'
          sibling.hidden = true
        }
      }
    })
  },

  getActiveView(container) {
    const searchContainer = container || document.body
    const views = searchContainer.querySelectorAll('[data-component]')
    for (const view of views) {
      if (this._isViewVisible(view)) return view
    }
    return null
  },

  getAllViews(container) {
    const searchContainer = container || document.body
    return Array.from(searchContainer.querySelectorAll('[data-component]'))
  },

  getViewByName(name) {
    return document.querySelector('[data-component="' + name + '"]')
  },

  _isViewVisible(view) {
    if (!view) return false
    const style = window.getComputedStyle(view)
    return style.display !== 'none' && !view.hidden
  },

  waitForNavigation(pageName, timeout = 1000) {
    return new Promise(resolve => {
      if (this.getCurrentPage() === pageName) {
        resolve(true)
        return
      }
      const startTime = Date.now()
      const check = setInterval(() => {
        if (this.getCurrentPage() === pageName) {
          clearInterval(check)
          resolve(true)
        } else if (Date.now() - startTime > timeout) {
          clearInterval(check)
          resolve(false)
        }
      }, 16)
    })
  },

  waitForViewChange(view, timeout = 1000) {
    return new Promise(resolve => {
      if (!view) {
        resolve(false)
        return
      }
      if (this._isViewVisible(view)) {
        resolve(true)
        return
      }
      const startTime = Date.now()
      const check = setInterval(() => {
        if (this._isViewVisible(view)) {
          clearInterval(check)
          resolve(true)
        } else if (Date.now() - startTime > timeout) {
          clearInterval(check)
          resolve(false)
        }
      }, 16)
    })
  },

  resetNavigationHistory() {
    this._navigationHistory = []
    this._historyIndex = -1
  },
}

// Expose Test API on window
if (typeof window !== 'undefined') {
  window.__MIRROR_TEST__ = __MIRROR_TEST__
}

// Register tokens in runtime
_runtime.registerToken('$yellow-bg', '#FDE70E')
_runtime.registerToken('$soft-bg', '#F4F4F4')
_runtime.registerToken('$ink-col', '#000')
_runtime.registerToken('$white-col', '#fff')
_runtime.registerToken('$muted-col', '#888')
_runtime.registerToken('$hairline-boc', '#e5e5e5')
_runtime.registerToken('$content-maxw', 1280)
