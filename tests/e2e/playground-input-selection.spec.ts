/**
 * Selection E2E Tests
 *
 * SKIPPED: These tests are for RatingGroup, SegmentedControl, ToggleGroup
 * which are not yet in the 11-eingabe.html tutorial.
 *
 * TODO: Re-enable when these components are added to the tutorial
 */

import { test } from '@playwright/test'

test.describe.skip('Selection Components (not in tutorial yet)', () => {
  test('RatingGroup - placeholder', async () => {
    // RatingGroup tests will be added when the component is in the tutorial
  })

  test('SegmentedControl - placeholder', async () => {
    // SegmentedControl tests will be added when the component is in the tutorial
  })

  test('ToggleGroup - placeholder', async () => {
    // ToggleGroup tests will be added when the component is in the tutorial
  })
})
