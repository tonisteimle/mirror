App w full, h full, bg #000, pad 20, abs
  Box ver center w full h full
    Box ver gap $l.pad pad 32 bg $default.bg rad 12 w 400 shadow lg
      H2 col $default.col center
      Box ver gap $s.pad
      Box ver gap $s.pad
        Text Anmelden
        Label col $default.col fs 14
        Input type email pad $m.pad bor 1 boc #444 rad 6 w full bg #27272a col $default.col
          Text E-Mail-Adresse
