/**
 * Property Writer: `bg` (background-color).
 * Generated from the color factory. No preview path.
 */

import { createColorWriter } from './_color-factory'

export const bgWriter = createColorWriter({
  name: 'bg',
  aliases: ['bg', 'background'],
})
