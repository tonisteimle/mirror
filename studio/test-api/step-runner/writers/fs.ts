/**
 * Property Writer: `fs` (font-size).
 * Generated from the simple-number factory. No preview path.
 */

import { createNumberWriter } from './_number-factory'

export const fsWriter = createNumberWriter({
  name: 'fs',
  aliases: ['fs', 'font-size'],
})
