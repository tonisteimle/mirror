/**
 * Property Writer: `rad` (border-radius).
 * Generated from the simple-number factory. No preview shortcut.
 */

import { createNumberWriter } from './_number-factory'

export const radWriter = createNumberWriter({
  name: 'rad',
  aliases: ['rad', 'radius'],
})
