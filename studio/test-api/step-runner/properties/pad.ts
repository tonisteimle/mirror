/**
 * Property Reader: `pad` (uniform padding)
 *
 * Generated from the uniform factory. See `_uniform-factory.ts` for the
 * resolution rules (single-value `pad N` only; uniform DOM only meaningful
 * when all four sides agree).
 */

import { createUniformReader } from './_uniform-factory'

export const padReader = createUniformReader('pad')
