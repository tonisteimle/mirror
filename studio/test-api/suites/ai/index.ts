/**
 * AI Features Test Suite Index
 *
 * Browser-tests für den LLM-Edit-Flow. Aktuell leer — die Vitest-Suite
 * deckt die Logik-Schicht (edit-flow, ghost-diff, llm-keymap, etc.) ab.
 * Browser-Integration-Tests werden in Phase 4 (T4.x) hier ergänzt.
 */

import type { TestCase } from '../../types'

export const allAITests: TestCase[] = []
