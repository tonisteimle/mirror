/**
 * Settings Panel Module
 *
 * Modal dialog for studio settings:
 * - Grid Settings (snap, size, visual)
 * - Smart Guides Settings (enabled, threshold, distances)
 * - Handle Snap Settings (enabled, grid size, token snapping)
 * - General Settings (move steps, position labels)
 */

export {
  SettingsPanel,
  createSettingsPanel,
  type SettingsPanelConfig,
  type SettingsPanelCallbacks,
} from './settings-panel'
