/**
 * Position Controls Module
 *
 * Provides UI components for editing x/y position values
 * with drag-to-adjust, spinners, and keyboard support.
 */

export { NumericInput } from './numeric-input'
export { PositionSection, createPositionSection } from './position-section'
export type { NumericInputConfig, PositionSectionConfig, PositionValue } from './types'
