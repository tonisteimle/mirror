{
  "framework": "react",
  "language": "typescript",
  "styling": "tailwind",
  "stateLib": "useState",
  "router": "none",
  "buildTool": "vite",
  "outputDir": "./generated",
  "constraints": {
    "noMirrorRuntime": true,
    "noServerComponents": true,
    "minimumDeps": true,
    "preserveSemantics": true
  },
  "notes": [
    "Single static page — no routing needed.",
    "Mirror source uses inline-Markdown (**bold**, *italic*) inside Text strings — render through dangerouslySetInnerHTML or a tiny markdown helper.",
    "Tokens go to tailwind.config.ts theme.extend, not CSS variables."
  ]
}
